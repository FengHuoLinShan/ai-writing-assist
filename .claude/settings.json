{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "if": "Bash(git push*)",
            "prompt": "git push 刚完成。检查最近提交是否修改了 facade.py / schemas.py / contracts.py / api.py 或 docs/modules/。如果有，请读取对应的 docs/modules/*.md 并更新到与代码一致。只更新文档，不修改代码。需要我更新文档时请说「更新文档」。",
            "statusMessage": "检查代码变更…"
          }
        ]
      }
    ]
  }
}
