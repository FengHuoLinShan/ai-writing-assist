- [x] `world.facade.find_entity_id_by_name` 精确名称匹配返回正确 UUID
- [x] `world.facade.find_entity_id_by_name` 别名匹配返回正确 UUID
- [x] `world.facade.find_entity_id_by_name` 未匹配返回 None
- [x] `world.facade.upsert_relationship` 新关系插入成功
- [x] `world.facade.upsert_relationship` 已有同 source+target+relation_type 关系时幂等更新 description
- [x] `character.facade.find_character_id_by_name` 精确名称匹配返回正确 UUID
- [x] `character.facade.find_character_id_by_name` 别名 JSON 数组匹配返回正确 UUID
- [x] `character.facade.update_character_location` 原子覆写 meta.current_location_id + last_updated_chapter + current_state
- [x] `outline.facade.merge_chapter_involved_ids` 新 ID 追加且去重
- [x] `outline.facade.merge_chapter_involved_ids` 无匹配章节卡时不报错
- [x] `ChapterStateExtraction` Pydantic 模型 new_relation 非法值降级为 'stationed_at'
- [x] `POST /api/writing/save-and-analyze` 正常流程：草稿保存 + 提案创建 + proposal_created=true
- [x] `POST /api/writing/save-and-analyze` LLM 超时降级：草稿保存成功 + proposal_created=false
- [x] `confirm_memory_proposal` 含 geo_mutations 时 character_shifts 分发成功
- [x] `confirm_memory_proposal` 含 geo_mutations 时 faction_shifts 分发成功
- [x] `confirm_memory_proposal` 含 geo_mutations 时 chapter involved 合并成功
- [x] `confirm_memory_proposal` 角色名未找到时跳过该条 + warning 日志
- [x] `confirm_memory_proposal` 分发失败不回滚 canonical memory 写入
- [x] 前端 writingView "保存并分析"按钮调用 API 并更新 state
- [x] 前端 memoryView 地缘提案卡片渲染 .geo-proposal-card 样式 + 中文标签
- [x] 前端 geoView 右侧面板显示势力列表和活跃人物列表
- [x] 前端 geoView 人物名点击跳转 router.go('character')
- [x] `GET /api/geo/location/{id}/factions` 返回控制/驻扎势力列表
- [x] `GET /api/geo/location/{id}/characters` 返回活跃人物列表
- [x] 未修改任何数据库表结构（DDL 无变更）
- [x] 跨模块通信仅通过 facade.py，无直接 import 内部模块
