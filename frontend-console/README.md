# 小说结构化创作控制台 — 前端

同时提供作者结构化创作工作台和 RP 私人互动故事。作者路径采用三主题换肤视觉系统
（sticky 晨光便签 / night 暗夜书房 / ink 水墨写意）；RP 路径使用独立、纯白、低干扰的故事壳。

后端 Outline/Memory 的唯一生产实现现归 Story 内部子域，原 API 前缀继续保持。前端
的 Evidence、账户设置和项目偏好调用分别使用 `/api/evidence/{indexing,compilation}`、
`/api/account/settings` 和 `/api/projects/{id}/author-preferences` canonical 路径。Scene 写作
若明确继续使用 stale adopted script，Writing wire 使用 `confirm_stale_story_assets=true`；
服务端 409 使用 `stale_story_assets` code，前端应保留编辑内容并要求作者确认或刷新。

## 快速启动

开发时使用 Vite dev server，支持 CSS 热更新和 JS/HTML 自动刷新。AI 地图册是普通 Vue SFC，图片通过同源鉴权 API 读取。

```bash
cd frontend-console
npm install
npm run dev
# 打开 http://localhost:8080
```

常用验证脚本：

```bash
npm run lint
npm run test
npm run test:watch
npm run test:e2e:functional
npm run test:e2e:smoke
npm run test:e2e:map
npm run test:e2e:visual
npm run test:e2e:visual:update
npm run test:e2e:real-llm
npm run test:e2e:worker
```

`npm run build`（vite build）可作 Vue 构建链冒烟验证；它会生成根级
`asset-manifest.json`，并在校验所有入口、动态一级路由和生成资源后写出
`asset-inventory.txt`。inventory 是发布时完整静态资源的受限合同，必须随构建产物一同交付。
`npm run lint` 使用 ESLint flat config 检查生产 JS、Vue SFC、Vitest、Playwright 与构建配置；
只启用 JS correctness 和 Vue essential 约束，不承担格式化。
构建会把仍由 `index.html` 直接加载的兼容运行时脚本复制为 `0644`；生产镜像还会统一保证
静态目录可遍历、文件可读，避免发布机的私有 `umask` 被继承为 Nginx 403。

账号弹窗作为模态边界：打开后将焦点置入弹窗、隔离作者壳背景并拦截壳层快捷键；关闭时恢复触发账户菜单的焦点。

作者列表中的共享行操作菜单支持方向键、Home/End 和 Escape 的 roving focus；Tab、焦点离开或外部点击会关闭菜单，选择操作前会恢复触发器焦点，并隔离壳层快捷键。

Vite 开发与预览服务通过 HTTP 响应头发送 CSP，并用 `frame-ancestors 'none'` / `X-Frame-Options: DENY`
禁止第三方页面嵌入控制台。部署 `dist/` 时，静态托管层必须保留 `vite.config.js` 中
`frontendSecurityHeaders` 的同等响应头；`frame-ancestors` 不能用 HTML `<meta>` 代替。

## 后端连接

前端默认请求同源 `/api`。开发时 Vite 将 `/api` 代理到
`http://127.0.0.1:${BACKEND_PORT:-8000}`；生产环境由静态托管层或反向代理将同源
`/api` 转发到后端。浏览器不需要直接访问后端端口。

远程调试或特殊测试环境可在页面加载前显式注入全局 `API_HOST`，支持
`http://localhost:8000` 或 `http://localhost:8000/api`。本地代理目标也可通过
`BACKEND_PORT` 或完整的 `API_PROXY_TARGET` 覆盖。

## E2E 测试

Playwright 的所有 profile 都 fail-closed：必须显式提供名称含独立 `audit` / `e2e` / `test`
标记的 PostgreSQL `DATABASE_URL`，并设置 `PW_REUSE_EXISTING_SERVER=0`。配置在创建
`webServer` 之前完成校验，因此缺失 URL、非 PostgreSQL、开发库名或复用未知服务都会在
migration 前失败。可通过环境变量避开端口冲突：

```bash
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 \
  BACKEND_PORT=8010 FRONTEND_PORT=8090 npm run test:e2e:smoke
```

启动命令会在后端启动前执行 `APP_ENV=test alembic upgrade head`；
`test:e2e:functional` 只收集功能测试，排除真实 LLM 和 worker 套件。完整套件在
pull request 上由 `Frontend functional browser` job 运行一次：使用全新的专用 PostgreSQL、
Compose 初始化的私有 MinIO bucket 和 Chromium，固定 workers=1、retries=0；失败时
保留 `test-results/` 诊断产物 14 天。`test:e2e:smoke` 和 `test:e2e:map` 保留为本地定向入口，
其用例已包含在完整套件中，不再单独占用 CI job。视觉、真实 LLM 和 worker suite
仍是显式验收入口。

```bash
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 npm run test:e2e:functional
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 npm run test:e2e:map
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 npm run test:e2e:visual
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 npm run test:e2e:real-llm
DATABASE_URL='<dedicated-postgresql-url>' PW_REUSE_EXISTING_SERVER=0 \
  LLM_SETTINGS_ENCRYPTION_KEY='<shared-fernet-key>' \
  WORKER_E2E_LLM_API_KEY='<provider-key>' npm run test:e2e:worker
```

如果默认端口已有旧服务，先停止旧服务，或像上面一样指定备用端口；测试配置不会复用它。
`APP_ENV=test` 只切换应用模式与测试路由，不会自动改写 `DATABASE_URL`。
worker profile 会随 Playwright 启动连接同一独立 E2E 数据库的真实 worker，并等待
`TaskWorker started` readiness 日志后才运行用例。它用 `WORKER_E2E_LLM_API_KEY` 写入当次
专用测试账户的 DeepSeek 连接；响应、trace 和日志不回显 key，teardown 会先清除该账户连接，
再严格永久删除测试项目。用例使用持久 Chromium profile，真实关闭并重启浏览器进程，且在
无页面期间从 task API 观察到 worker 状态或心跳推进后才允许继续。
该真实 Worker 门禁固定验证第一版 DeepSeek 模板，不接受自定义 provider/base URL/model；
`WORKER_E2E_TEST_TIMEOUT_MS` 与
`WORKER_E2E_TASK_TIMEOUT_MS` 可覆盖专用长任务预算。若本机同时运行开发 worker，应确保它不
连接同一个 E2E 数据库，避免抢占 E2E 创建的任务。
`scripts/e2e-servers.sh` 使用同一 fail-closed guard，会先校验并迁移当前 `DATABASE_URL` 再启动
backend；通用 `backend/scripts/dev_server.py` 不自动迁移。

视觉回归使用独立的 `playwright.visual.config.js`，只串行收集
`e2e/visual-*.spec.js`。配置固定 Chromium、`zh-CN`、`Asia/Shanghai`、DPR=1、
reduced-motion 与默认 1440×900 视口，并把失败产物与 HTML 报告分别写入
`test-results/visual/` 和 `playwright-report/visual/`。默认像素差异比例上限为 0.5%；
spec 可为特定页面追加 mask，但不得放宽全局阈值来接受未解释差异。

仓库只提交 darwin 基线；更新基线时需显式运行
`npm run test:e2e:visual:update`，随后逐张检查 actual / expected / diff PNG，不能只凭
测试通过接受差异。非 darwin 平台仅在显式设置 `VISUAL_BASELINE=1`
后生成本平台基线。写作页基线覆盖桌面三主题、专注模式、390×844 移动速记，以及保存失败与 AI 候选决策在
桌面/移动端的持续恢复入口。

AI 地图册自动测试使用固定 PNG、mock OpenAI 与 mock S3，不产生费用；付费 live smoke 默认跳过。
地图册组件回归见 `tests/vue/map/MapAtlasView.test.js`，浏览器主流程见 `e2e/map-atlas.spec.js`。

后端地址可用 `API_HOST` 覆盖，支持 `http://localhost:8000` 或 `http://localhost:8000/api`。
如果 `webServer` 超时，先运行：

```bash
cd ../backend
python scripts/doctor.py --json
```

## 文件结构

```
frontend-console/
├── index.html              # Vue shell 空挂载根与稳定脚本加载顺序
├── styles.css              # 基础布局、组件和页面样式（设计 Token 驱动）
├── editorial-theme.css     # 全站主题覆层（--nc-* 原语层；sticky 浅色 / night 深色 / ink 纸色）
├── state.js                # Proxy 状态、持久化与订阅；不直接投影 shell DOM
├── stateSlices.js          # 状态副作用与 listener 通知 helper
├── api.js                  # API 封装（auth/projects/world/evidence/writing/imports/tasks）
├── shared/accountStorage.js # 账号切换/退出时清理项目级浏览器缓存并保留主题
├── apiContracts.js         # 共享 API 契约注册表（高风险 wrapper 子集）
├── router.js               # Hash router 与 #workspace-content route-host 生命周期
├── commands.js             # 命令系统（全中文帮助）
├── app.js                  # 启动顺序、项目摘要恢复、SmartDedup 生命周期
├── shared/                 # 可复用业务组件与工具
│   ├── smartDedup.js       # 智能去重管理器
│   ├── referencePicker.js  # 作者向对象名称搜索与稳定 ID 回写
│   ├── confirmAsync.js     # 异步二次确认封装
│   ├── writingToolsResult.js # 工具结果应用到 Vue Writing workspace
│   ├── sceneLocator.js     # 其他视图仍可复用的 Scene 区间定位 helper；写作台不用它自动切换
│   └── ...                 # 其他共享模块
├── vue/                    # Vue 3 shell 与业务页面（ADR-0009）
│   ├── shell/              #   topbar/sidebar/命令栏/主题/快捷键/service hosts
│   ├── bridge/             #   组件访问稳定基建的唯一入口（可 DI 替身）
│   ├── mountIsland.js      #   Vue 根组件 → hash router 视图契约适配器
│   ├── viewLoaders.js      #   一级路由 → 按需 island import 注册表（不主动加载业务模块）
│   ├── components/         #   跨视图组件（WorkflowProgressCard 等）
│   ├── composables/        #   跨视图组合式函数（上传/轮询/保存按钮）
│   ├── *Island.js          #   各一级路由注册与首屏数据预取
│   └── views/              #   home/interaction 与作者 project/rag/world/outline/... SFC
│       ├── writing/home/   #     写作首页、作者任务页/表单与类型化来源跳转
│       ├── world/library/  #     资料库类型首页、同源 cards/list 与对象详情
│       └── world/pages/    #     资料页编辑/发布展示 helper 与关联图完整次级工具
├── views/                  # Vue 页之下的限定兼容 seam（不再注册页面 renderer）
│   └── writing/            # sceneAlerts.js / versionDiff.js 无 DOM 纯 helper
├── tests/                  # 测试目录
│   ├── vue/                # Vue shell、业务视图、controller 与 owner gate 测试
│   └── shared/             # shared 模块测试
└── README.md
```

命令面板使用作者可理解的入口；资料检索为 `:search <关键词>` 或 `/关键词`，不直接展示 RAG、Context、Generate 等内部架构名。

## 技术栈

- Vue 3 SFC shell + 业务页面（ADR-0009）：Vue 拥有静态外壳和所有实际路由目标的
  主 DOM；`today` / `generate` / `scene` / `llm` 只作兼容重定向，不拥有页面 DOM

## 写作台 Scene 交互

左侧目录严格按章节顺序只显示状态、章号、标题和字数，并在底部保留唯一
“新建章节”。当前 Scene 由作者在右侧副驾驶顶部手动选择；光标移动只更新
编辑位置，不改变 Scene。副驾驶只展示当前章的 `draft/canonical` Scene，并在项目
会话中按章记住选择；AI 参考、冲突检查和发布检查统一消费该选择。
桌面副驾驶可连续关联或快速新建 Scene；移动速记只允许切换本章 Scene，不提供
关联、新建或管理。两端共用“本场”摘要：默认只展示 Scene 主字段和
`structure_meta` 执行字段白名单，POV 可见知识与场景时点状态必须由作者显式点击后
读取。切换 Scene 会隔离旧请求的晚到响应；失败时保留静态摘要并可原位重试。
窄屏使用原生 `details` 渐进展开。
- hash router、Proxy state、API 和 toast/modal 作为集中式基础设施保留；router 的动态视图、
  loader、pending loader 和子标签注册表使用 `Map`，只接受安全的小写路由 key 并拒绝原型属性名；
  router 不再使用 DocumentFragment/KeepAlive，视图离开时卸载并由项目隔离 session 恢复有业务价值的状态
- AI 地图册由 `vue/views/map/MapWorkspaceView.vue` 独占页面 DOM；层级、候选/正式画廊、来源、停止恢复、图片编辑和标注都留在该 SFC。
- 世界书内的“关联图”复用 `GET /api/world/knowledge-graph`：默认从当前页面读取一跳，可显式扩展到两跳或全局；可访问节点列表是主交互，SVG 只作最多 40 节点 / 80 边的辅助示意。结果会明确显示截断/部分扫描，并不把关联表述为依赖或变更影响。
- 地图册 run 有空间资料摘要时才显示该摘要；它只表达核对/降级状态，不显示事实文本、来源键或 prompt，窄屏保持折叠可读。
- 图片 wrapper 返回 Blob 并创建短期 Object URL；切换项目或视图时释放，不暴露对象 key。
- 所有 UI 文字为中文
- 作者主流程的对象引用统一按名称搜索和选择；共享 `referencePicker` 仅把 ID 回写到现有隐藏字段/请求 payload。同名项用类型、状态和摘要消歧，无法解析的旧引用保留为“不可用引用”。Workflow、任务和原始 Scene ID 只位于折叠诊断区，并标记 `data-diagnostic-field`。
- 全站三主题换肤体系：sticky（晨光便签，浅色默认）/ night（暗夜书房，深色）/ ink（水墨写意，
  纸色）；色值集中在 `editorial-theme.css` 的 `--nc-*` 原语层，语义层全部转发，线条为 1px
  hairline；主题持久化 key 为 `nc-theme`，无存储时跟随系统 prefers-color-scheme

## 路由与设置

- 作者入口会校验本地当前作品是否仍属于当前账户：有效时直接进入
  `#workbench/{projectId}/writing?home=1`，无效时清除选择并回到作品档案；没有作品时优先显示“新建空白作品”
  与“导入已有作品”。Writing Home 复用 `today` 的摘要 loader，读取 `projects.getWorkspaceSummary`、作者任务、世界书工作稿和生成中心待处理
  页面建议：Writing Home 复用同一摘要与恢复接口；已有章节时正文续写始终是默认主操作，
  有效的项目级本地创作指针与服务器世界工作稿作为次级恢复入口；空白作品也默认「开始第一章」并直接进入正文，World Core 收为「先整理世界观」次操作；明确的导入任务、工作稿或建议仍按原优先级恢复。页面同时按
  唯一主行动之后展示最多 3 项“计划中的任务”，完整页固定为 `writing?home=1&panel=tasks` 的今天、收件箱、之后、已完成视图；
  作者任务可从世界 Page/Entity、章节和 Scene 就地建立，并只按封闭来源类型返回同项目资料；世界工作稿改名时仍按实际绑定的正式页名称建立任务，避免把不同名称误当成同一来源。失效来源保留任务本身并可清除，不保存任意 URL；
  PATCH 409 会保留标题、备注和日期，以 `cache: no-store` 重读最新版本基线，不自动重试；只有作者再次点击保存才发出新 PATCH。未保存的任务表单按项目隔离暂存在当前浏览器会话，切换任务范围、返回写作首页或刷新后可恢复；保存成功或确认放弃后清理。创建成功后只 replace 当前 query 清除来源参数，不触发竞争导航。
  页面继续按当前 Scene、当前章、项目级顺序展示最多 6 条 Writing / World / Outline 具体待决事项，并保留最多
  3 个可恢复长任务；还有隐藏事项时按后端类型化 target 提供不绑定单条 item 的领域处理范围入口。World 对象/别名/关系
  审核使用精确对象或组标识定位，不受默认 20 条分页影响。首页不改写作品；摘要失败时不把作品误判为空项目，仍保留进入写作与各领域待办页的降级入口。未知或失败任务只有经作者确认后才从首页隐藏，并明示可在对应页面找回。
- 桌面作者主导航固定为“首页、写作、人物与世界、故事结构、地图、查找”；项目切换器位于顶部，
  “导入与整理、项目偏好”收进“更多”，账户与模型连接位于头像菜单；AI 工具在写作与人物/世界页就地打开。390px 使用
  “写作、世界、结构、全部”四项带文字底栏，“全部”直接列出地图、查找和更多工具。
  `generate`、`settings`、`project-settings` 及其他旧 hash 深链继续有效，只是不再占据主导航；旧 `generate`
  仅把上下文带到对应 owner 页的 AI 抽屉，不再作为新入口。设置由同一外壳明确区分账户与当前项目范围。
- 作者壳层主题为顶栏三点切换器（`.topbar-theme` radiogroup + `button.theme-dot[data-theme-value]`，白/黑/朱砂圆点，active 描边环）：Tab 只进入当前主题，方向键循环切换；圆点保持 14px，命中区为桌面 28px / 触控档 42px。主题值、持久化（`nc-theme`，首次从旧 key `novel_theme` 迁移）和通知契约不变。≤760px 顶栏保留品牌标记、截断作品名、服务连接状态、主题与账户入口；≤360px 仅在移动速记头部同步本章与今日累计后隐藏顶栏字数胶囊。
- 作者壳层命令栏以可编辑 combobox 呈现最多六项建议；方向键只浏览建议，Enter 仅在显式选择后采用建议，Tab 仍补全首项，命令与 `/` 搜索行为不变。
- RP 旅程目录会为每个归档、恢复与永久删除操作公开对应旅程名；故事页为分支选择、抽屉关闭、加载/生成与失败状态提供程序化语义，内置主题菜单使用单一 Tab 停靠点、方向键/Home/End 导航、Escape 与焦点归还。阅读列默认收敛到 640px，流式正文用文字、左线和轻底色区别于已提交内容，逐 chunk 不排队平滑滚动，reduced-motion 停用可选动画；消息操作以正文色和较小字阶常显，导出完成给出反馈。流式正文和字数统计不使用 live announcement，既有故事、分支、归档及 API 契约不变。
- 公共访问先显示作者 / RP 双入口，选择只保存在当前浏览器会话；双入口与沉浸壳跟随当前三主题，夜间/纸墨主题不闪回白底。邮箱或微信登录成功后进入所选路径，登录页可返回重选。等待删除后的重新认证与账号删除重新认证继续提供命名的验证码输入、忙碌状态和成功/失败通告；认证请求、恢复期、注销与 HTTP/API wire 契约不变。
- 结构、Scene 与世界书的可切换子导航使用原生按钮，当前项公开为当前页；Scene 工作台自身仍是非交互当前项，避免同路由刷新。查找不设置内容级 tab，查找质量、重建与低频诊断只从“修复查找”次级入口进入，并在修复卡内返回查找。AI 地图册的候选、正式画廊与历史独立呈现，热点是可用键盘和触控打开的原生按钮；采用、拒绝与移出操作不会彼此替代。
- AI 地图册可选“生图前检查画面说明”：逐页自动保存、复制并选择站内或外部生成，确认后才调用图片服务。工作台可在共享 modal dialog 中上传 PNG/JPEG 为待采用候选，并调整地图的上级、层级和同级位置；上传失败保留表单和预览。对话框困住焦点，关闭/遮罩/Escape 共用未上传修改确认并恢复触发按钮，上传中只能明确取消；未保存画面说明仍会防止误离开。地图 UI 只消费现有三主题语义 token，图片校对画布保留中性深色；页签使用 2px accent 下划线，390px 的操作组可换行且页签、层级项、图片热点与展开入口保持触控命中高度。
- 作品目录的“全选当前可见作品”只选择当前搜索结果；作品卡和创建占位卡都支持鼠标、Enter 与 Space。回收站入口始终可见，单个/批量恢复与永久删除仍保留原有门禁；零选择时批量操作禁用，永久删除必须二次确认。回收站每页 20 条，桌面端大模态框用双列完整展示当前页，并提供上一页、下一页和总数。
- 桌面项目目录使用等宽紧凑卡片，同屏展示三个完整项目摘要；390px 下进一步压缩档案头部并隐藏纯装饰封面，首屏保留新建、导入、管理、搜索与首个项目摘要。项目能力不变。
- 根路由 `home` 展示作者与“进入互动故事”双入口；RP 使用 `journeys` 列表与
  `interaction/{journey_id}` 故事路由。作者一级路由包含 `project`、`writing`（含首页与 `panel=tasks` 模式）、`world`、
  `map`、`outline`、`rag`、`settings`、`project-settings`；`today` 与 `generate` 为兼容别名。`outline` 默认进入
  层级最高的 `story-outline`，旧 `scene` 路由跳转到 `outline/scenes`。快速切换或初始化期间
  使用浏览器前进/后退时，晚到请求不会提交旧路由或覆盖当前页面。router 不缓存活 DOM；
  Writing Home 只组合现有项目摘要、世界书列表、建议列表和 task API，不发起生成；待决建议进入摘要投影后不再重复出现在“未完成创作”。它按现有 task API 逐项恢复该项目的持久化任务。`outline` 离开时停止 workflow 轮询并从持久化记录恢复，`writing` 使用项目隔离的显式
  session snapshot 恢复作者编辑态。正文与 Scene 剧本只有在浏览器本地写入或服务端保存真实
  成功后才显示相应的“已备份/已保存”；两者都不可用时保留当前页面内存、明确提示刷新或离开
  的丢失风险，并由作者二次确认是否离开。
- 路由模块加载期间继续使用可访问的工作区骨架；快速导航、账号边界或 host 卸载后晚到的模块
  只可为未来访问完成注册，不能挂载或覆盖当前页面。已注册 renderer 优先于 loader；加载模块未
  自注册会进入安全失败态而非错误显示“正在开发中”，失败后的“重试”仅重跑当前路由渲染。
- 作者项目边界统一由 router 事务化处理：先运行当前页 `canLeave` 与项目导航 modal 预检；
  通过后在旧项目 state/DOM 仍有效时恰好调用一次 `onLeave`，立即切到不含保存、发布或 AI
  操作的中性骨架，再以 abort + generation 同步目标项目。mounted route、pending target 和
  failure route 分开保存，目标只有完成 `onEnter → render → onRendered` 才成为 mounted；
  半挂载失败会执行 `onLeave` 清理。临时跨项目失败只能重试目标或返回项目列表，不恢复旧项目
  DOM；同路由 refresh 重挂载后恢复工作区纵向滚动位置，同项目临时 refresh 保留编辑页，
  403/404/422 等项目失效则 fail-closed。纯呈现控件可通过 `commitCurrentQuery` 就地更新当前
  hash 与 router 内部 query，不运行 `onEnter/render`；业务代码读取当前 query 统一经 bridge
  `getRouteQuery()` 独立副本（全仓库唯一的读取防御点），Scene 工作台 query 写入收敛到
  `sceneModel.commitSceneRouteQuery` 单一入口；shell router 包装层透传 `getCurrentQuery`，
  设置页携带合法 `return_to` 时恢复隐藏作者壳；服务端操作的晚到响应只能在发起时
  路由/编辑器仍存活时刷新。
- RP 旅程列表默认折叠搜索；开场输入以淡字提示世界、身份、时间地点和愿望。故事页支持
  流式重连、草稿、安全 Markdown 正文显示、并列的复制/重新生成按钮、完整行动建议卡片点击
  填入、简单分支树、总回顾、看海开关与右侧生成段落定位轨。“记住这一点”只把当前选中的
  故事片段和 composer 文本预填到“必须继续记住”，保留输入并等待用户显式保存。RP 列表与故事页使用独占视口，
  新旅程的 `RpSourceSetup.vue` 按资料来源、作品/文件、整理与歧义、角色与开场四步渐进展开；
  非当前步只保留摘要和返回编辑，并复用既有 session 恢复步骤、revision、整理进度、章节剧情点与
  身份。该 session 随账户切换清理；作品、revision、轮询、歧义和剧情匹配的晚到响应均经
  generation + 作品/版本身份复验后才可回写。自然语言匹配仍只产生候选，点击确认后才写入既有 source setup；
  章节+offset 防剧透边界和
  创建 wire 不变。作品整理和故事生成使用用户在账户中连接的 AI 服务，请求经本站后端代理，Key
  不进入浏览器或作品。故事页的作品资料仅从更多菜单打开抽屉，
  用自然语言显示本轮引用理由、固定/忽略和显式版本升级，不暴露内部 ID 或 token。
  不显示作者侧栏或编辑档案装饰；移动端定位轨降级，composer 固定在可视底部，主故事区占满
  页面；390×844 不横向溢出。看海首次费用确认以触发按钮为锚点，按浏览器可视视口自动选择
  向上或向下弹出；Safari 地址栏、底部工具栏、软键盘、页面缩放或滚动改变可视区域时会重新
  定位，窄屏水平夹紧，内容过高则在确认框内滚动。
- 应用首屏、全局路由切换以及写作、大纲和 Scene 工作台的主加载边界使用共享骨架屏；状态容器保留屏幕阅读器可感知的具体加载文本，视觉占位标记为装饰内容，并遵守 `prefers-reduced-motion`。Scene 工作台初始空态直接提供正文整理与 AI 细纲入口，筛选空态可一键清除筛选；局部刷新保留现有列表并暂时禁用旧控件，失败时原位说明内容仍被保留并允许重试。按钮提交、任务进度和其他局部数据刷新继续使用各自的明确状态，不用通用骨架替代业务反馈。
- 项目级“智能去重”不再占用独立全局操作条，只在世界对象与故事结构页内显示；剧情线/篇章标题区只保留「新建」唯一主按钮和当前层级 AI 创作，AI 分析、正文整理与智能去重收入原生「分析与整理」，Scene 则使用「整理工具」。每个页面只保留一个入口，扫描状态跨页面持续，返回世界或大纲后恢复“查看智能去重/查看去重建议”。每行根据当前最高优先级待办切换主按钮；桌面端额外保留“编辑”，移动端收敛为主按钮与更多菜单。健康标签可点击，跨章建议刷新后从后端恢复。
- 项目级智能去重使用大尺寸双栏裁决工作台：左侧按 group 展示状态，
  右侧展示字段对比、合格主对象、逐成员动作、影响和证据。草稿以
  `projectId + scanTaskId + groupId` 隔离，不提供手填主对象 ID；窄屏时队列移到
  对比区上方。只提交全部成员已明确裁决的组，成功组锁定，失败/过期组
  保留裁决和错误以便重试。旧任务只有 `suggestions` 时仍使用 pair 兼容面板。
- World/Outline/Writing 的作者长耗时 AI 操作在提交前保存页内 `operation_id`；刷新或离开
  返回只查询原 task，404 不自动重放。进度和结果留在发起位置，默认不显示 raw task ID，
  任务完成不重挂整个页面，也不覆盖当前输入、筛选、焦点、滚动或多选。地图册图片生成继续
  使用自己的 run/page checkpoint 与重复计费确认，不叠加这套 receipt。
- 剧情线、篇章纲及其内部信息推进加载失败时显示可重试的内联错误，不把失败伪装成空列表；通用 API 409 显示“请求冲突”并继续附带后端领域消息。
- 大纲的剧情线和篇章纲页仍提供“AI 分析大纲”。作者先填写可选的
  分析目标与章节范围，再在通用 AI 参考弹窗中检查该范围内按顺序排列的 Scene、相关结构
  计划、人物和物品；确认后提交可恢复的 `outline_analyze` 任务。完成结果以转义后的只读
  Markdown 和已确认资料摘要展示，不提供应用入口，也不会直接修改大纲资产。同一项目只保持一个运行中的
  手动分析；后端允许时可二次确认取消，取消状态按终态恢复而不伪装成失败。新请求只在获得有效 `task_id`
  后替换上一份已完成报告，并以所属项目隔离持久化与轮询，避免晚到响应在项目切换后回写当前页面。
- 剧情线、篇章纲和 Scene 工作台分别提供 P20 当前层创作入口。表单先检查当前小说总纲，
  支持新增设计或修订所选；任务可恢复。三类结果都进入对应子视图的 `?review=ai`
  独立审阅页，以作者语言编辑名称、章节范围、目标、冲突、关键转折、信息推进或场景走向，不显示 JSON、引用 ID 和内部枚举；本机草稿按
  项目 + source task 隔离，刷新、前后退和切换作品可恢复，409 后保留内容并阻止继续采用旧建议。
  Scene 的隐藏结构引用在表单编辑期间原样保留，冲突安排和叙事位置以作者可读选项适配回原 strict schema；所有层级都在明确采用前不写结构资产。剧情线详情把伏笔与揭示合并成默认展开的信息推进时间线，并提供未归类计划分配；
  旧伏笔/揭示链接会定位、聚焦并短暂高亮对应剧情线。P20 不在
  生成中心出现。P20 与大纲分析的异步提交请求允许等待 5 分钟，后台 P20 模型步骤允许 30 分钟，
  任务轮询不设置整体截止时间。
- 故事总览、剧情线、篇章与 Scene 的活动 AI 任务统一显示在共享标题下方的
  `.outline-task-status`；并行任务按固定间距纵向排列，章节范围、取消、关闭与查看建议都在
  对应的 `WorkflowProgressCard` 内完成。切换结构标签与浏览器返回不会移动任务区，切换作品
  不会显示上一部作品的任务。
- `outline/story-outline` 的作者名称为“故事总览”。首次进入只显示一个「先确定故事方向」主操作区，AI 生成可编辑预览为主操作、手工创建为次操作；空历史不占首屏，正常状态的重新加载收入「更多」，加载失败仍原位重试。AI 生成设置先只询问故事方向、预计篇幅和本次规划范围，人物与世界资料收入默认折叠的可选区；不暴露 Top-K 等技术词，缺失内容在字段和可聚焦错误摘要中反馈。已有版本时手工编辑为主操作，AI 新方案为次操作；当前内容使用单一可扫描文档呈现，不再逐字段嵌套卡片。过往版本默认折叠且不重复当前版本，查看弹窗沿用同一阅读层级，采用仍需二次确认。AI 预览与 `?edit=1` 手工编辑页共用作者可读的结构化字段，不显示 JSON/Markdown 技术入口；两类未采用修改都按项目在本机暂存，刷新、返回与切换作品后可恢复，成功采用或明确放弃才清理。本地写入失败时手工编辑页不再宣称草稿已保留，而是提示离开或刷新风险并要求二次确认。手工保存、采用 AI preview 和采用历史内容仍使用 current revision 作为 CAS base 创建新 revision，后端 schema/wire 不变；AI 预览的 409 会显式保留本机修改，要求同步最新基准后才可重试。
- Scene 融合与拆分使用大尺寸字段对比表，完整展示 AI 建议、原 Scene 引用、叙事标签、POV 和章节映射；默认显示全部字段，可只看服务端初始预览中的差异。融合预览属于同步 LLM 请求，后端生成窗口为 30 分钟，浏览器等待 35 分钟以给持久化和响应传输留出余量，不受普通 API 的 15 秒超时限制。长来源证据按需展开，AI 建议始终可见。叙事标签统一用 `draft` 表示“未标注”，拆分时显式清空的字段会按空值保存。废弃融合来源需要在预览内再次确认；保存请求期间所有融合操作保持锁定，失败后恢复控件并保留当前编辑内容。
- 写作台“AI 工具”优先提供一次授权的“先整理场景骨架（推荐）”入口，完整世界与结构整理仍须作者明确选择；也保留其他分阶段提取入口。Scene 或完整导入完成后只局部刷新章节与 Scene；存在 Scene 时可直接前往 Scene 工作台，不重绘正在编辑的正文。深度导入和 Scene 自动提取任务以 `taskId + projectId` 持久化；查询与取消都显式携带 `novel_id`。Scene stage 百分比是基于历史实测的耗时估算，Phase 0 只显示准备状态。Scene 工作台轮询只局部更新进度卡，不重绘正在浏览的列表。运行中的进度卡可在二次确认后取消当前任务；瞬时查询失败保留恢复记录。失败、取消、降级、待恢复及带查看/审计/恢复操作的完成态保留到作者处理；只有无后续操作的成功态在 3 秒后自动清理。
- 同项目多个标签页或浏览器同时提交完整/分阶段自动提取时，服务端在项目级短事务锁内复用已有活动/待恢复 task；前端按返回的真实 `workflow_type` 连接原任务。恢复、放弃与取消按钮只按 task status API 的 `available_actions` 渲染。取消终态只承诺停止后续处理并保留已保存阶段，不伪称远端模型连接已瞬时中断。作者界面的失败、降级、质量与审计摘要会递归转换 `error_kind`，不直接展示内部错误码。
- 共享任务卡只在后端 `available_actions` 包含 `retry` 时显示重试；`restart_origin` 与深度导入 `resume/abandon` 继续走各自领域流程。
- 生成中心自定义模板可查看修订历史并把旧版本载入编辑器；载入不写库，仍需用户明确保存。
- 前端依后端契约分页或拦截超限请求：Scene 建议每次最多忽略 100 条；地图册单次计划最多 20 页，不通过多请求静默扩容。
- 生成中心 world 工作区每次最多附带 20 章索引，章节选择器会分批为全部章节加载摘录，选择上限不作为预览上限；人物和世界对象选择范围同样分页加载全项目资产，Scene 选择器读取全部活跃有序 Scene 并排除历史态。长对话只发送最近 40 条消息。同步聊天、只读收束、世界建议、Scene 融合和冲突建议统一等待 35 分钟，后端模型步骤使用 30 分钟上限；多轮世界建议的作者决策编译、最终生成和守卫重试共享该 30 分钟预算，异步生成只限制提交请求，任务轮询没有整体截止时间。当前页面历史不因请求上限被删除。本地 v2 会话按“项目 + world workspace + 来源页 + target + 流程 preset”隔离，缓存未发送输入、对话、选择项、suggestion ID 和有界收束草稿；收束草稿只含摘要、来源 refs、三类选择和作者消息，超限先删除可重建展开说明。刷新或离开时仍在等待的聊天助手气泡只在本地副本中转为可见的中断终态，不会自动重试，也不会进入下一次聊天请求；另可保存 schemaVersion=1、精确绑定当前 pending suggestion 的作者未应用提案编辑。未发送输入和 working copy 共用 512 KiB / 最多 5 个会话边界；只在 suggestion 匹配时恢复，成功应用或作者确认放弃后清理。项目级本地创作指针只保存允许列表内的结构化 route，不复制正文；只有作者编辑/发送、打开/保存工作稿、进入/应用建议时更新，轮询和迟到响应不会抢走入口。来源页失效时保留本地副本并要求作者重新选择，不自动降级为项目来源。它不缓存服务器页面正文或服务器工作稿，也不代表 canonical。旧 v2 session 缺少新增字段仍可读取，不迁移旧 v1 状态。上下文资料预览按项目隔离，并在当前标签页的 sessionStorage 中恢复刷新、target 往返和项目切换；会话副本上限 512 KiB，超限时只保留可重建的资料摘要，完整文本可由作者再次准备。
- 生成中心“角色视角正文”使用表单内唯一主操作，选择章节、Scene 和角色后只创建待审核候选。已有目标章时后端冻结并向模型提供该章完整 active 正文；候选必须是目标章完整替换稿，跨章 Scene 只作结构上下文。完成后“打开写作台审阅”进入通用采用/拒绝面板；角色知识检查通过只显示“未发现明显越权”，不冒充整体内容验收。章节、Scene、角色和作者指令保存在既有有界项目会话中；旧 Generate 深链跨世界/写作 owner 时替换到正确所属页，刷新、前进/后退和项目切换不串稿。
- 生成中心“角色视角正文”会先加载章节和角色；确认项目尚无章节时不展示不可满足的表单或生成动作，而是说明需要章节、Scene 与视角角色，并提供“去写作台创建第一章”与“先完善世界设定”入口。整体加载和 Scene 加载失败都可原位重试，不把失败误报为零章节；生成失败自动聚焦重试卡并保留表单，AI 参考弹窗或任务取消也不清空作者输入。
- 人物知识管理器把现有记录组织为按对象分组的“当前认知 / 较早记录”，显示可读对象类型和生效章；新增使用类型化对象选择器，同一对象与生效位置默认 PUT 修正，归档也使用 PUT，不在主流程显示 raw ID 或内部枚举。重复检查点会提示作者合并。角色视角 AI 参考弹窗完整展开送给角色模型的知识，并把导演约束单列为“仅供作者约束”；修正入口在新标签页打开该人物的既有管理器，保留当前表单，返回后由作者手动重新整理，不自动生成。
- `world` 默认进入“资料库”，可见子导航只保留“资料库、关系、需要决定”。资料库用带 `page/entity` 标记的同一 Card read model 展示资料页、工作稿和已采用人物或设定；不新增 Card 表。首页以人物、地点、势力／派系、物品、事件、规则、工作稿和“更多类型”进入结果页；对象数量复用现有 hot facets，页面与工作稿数量从完整列表计算，不能用前 50 张卡片估算。类型、搜索、资料形态、状态和 cards/list 布局继续写 URL，返回时按项目与查询恢复列表位置。World 子页经通用 shell slot 把动态工具卡 Teleport 到一级侧栏；资料库、关系和需要决定显示各自相关按钮，世界健康、页面中的未决项和更多类型使用可访问弹窗，760px 以下改为页面内“资料工具”入口，不挤占五项底部导航。统一“新建资料”先区分具体对象或资料页；对象类型必须显式选择，资料页仍先创建工作稿再发布。人物详情可按需维护现有 Character 档案，名称和别名仍只由 CoreEntity 管理。Page/Draft/Entity 深链、滚动恢复、未保存离开门禁和 E1 来源名称规则保持不变；高风险保存、发布、采用和忽略动作继续留在内容区。原对象库的图片、批量、回滚和人物认知从“人物与设定工具”继续可达；旧 `world/objects`、`world/aliases`、`world/relations`、`world/review` 与 review legacy 深链继续有效。发布结果不确定时仍保留幂等恢复语义。
- 世界书资料编辑支持稳定 section 排序和页面模板；新建页不会把兼容兜底 `custom` 当作可选分类，而是在同一弹窗中保留已填内容并创建、复用或恢复项目分类。分类管理只显示名称、说明、卡片标记和作者状态；内部键、排序号与 raw status 不进入主界面。“更多工具”复用覆盖式下拉，不改变工具栏高度，支持 Escape、外点关闭与焦点归还。分区主流程只显示序号、标题、中文内容形式与正文，可见范围、自动整理策略、稳定标识和局部引用标识收在默认折叠的“创作辅助与高级设置”中，保存仍使用原有 section wire 字段。编辑创设建议只开放标题、类别与页面概览，分区和资产关联原样写入工作稿，不提供 JSON 输入。AI 参考规则继续使用受限表单、发布与 dry-run trace；AI 对话、目标和模板选择已从世界书侧栏移除。“用 AI 完善此页”会在未保存时要求先保存，随后进入生成中心。“保存并发布”先保存工作稿并打开影响核对，只把当前项目 typed 页面引用列为“建议核对”，同时明示自由文本、outline、writing 与其他未登记领域未检查；空结果不写成“无影响”。确认携带 scope hash，引用图变化时保留工作稿并要求重查；预演不可用时仍保留经作者明确选择的发布出口。世界书、生成中心和通用 AI 参考弹窗只在作者显式选择已发布 Activation Profile 后发送 profile ID，不默认激活长期规则。
- 世界书顶部的“待我决定”从现有 `author-open-questions` 分区汇总已保存的未勾选项目；同一正式页存在工作稿时只读取工作稿，归档页不计入。入口显示项目数和来源页数，展开后可直接打开并定位对应分区；空态明确未保存编辑尚未纳入。该入口不新增 API、业务状态或发布门禁。
- 世界书“世界健康”面板以作者语言展示待决定、证据缺口和失效数，可运行当前工作稿/采用包的定向校验或全面校验，支持任务恢复、来源回到、最近回执和 warning 整体签收，不显示 raw ID/JSON/Prompt/token/hash。旧项目默认不开启强制门禁；作者二次确认启用后，发布和采用必须携带当前新鲜回执。
- 生成中心 world 工作区提供项目/来源页状态、对象/完善当前页/新建页面目标、大尺寸共创对话、章节/Scene/剧情线/人物/世界对象/简介/Profile 参考资料，以及可编辑的完整页面预览。「本轮要做什么」保留 3 个互斥目标；世界对象模板改用有标签和说明的原生选择器，不再平铺为第二排导航。右侧 rail 只管理“本轮参考资料”：复核、简介和正文选择常驻，重点资料与已发布创作规则渐进展开；窄屏首次进入默认折叠并排在聊天前，开合偏好按项目会话恢复。空结果不占位置；生成中、失败或已有建议时，审阅区在聊天上方使用主栏宽度，失败仍保留上一份可编辑建议。聊天与建议之间有次级“收束本轮”：只在作者点击时整理当前可见范围，显示覆盖、排除项与最多 7 张决定卡；“纳入／开放／放弃”先生成可编辑作者消息，确认加入对话前后都不创建建议或采用设定。完整且未过期的收束可直接前往既有故事总览承接核心前提、叙事读法、基调和读者承诺；导航只切换工作区，不搬运或改写世界事实，未应用整页提案仍沿用离开确认。来源页的新页面目标还可折叠开启“一跳探索”：只读列出最多 3 个带证据的相邻缺口，作者单选 1 个后才进入既有建议生成，未选项不进队列；输入或来源变化会使选择过期。若返回一份必要的来源页修订，界面将它与新页面分别标为待审，不自动应用或继续下一跳。接近 40 条只提示；刷新恢复选择，输入或来源变化后旧预览只读。多轮建议生成后以默认折叠的“AI 本次理解”展示目标、必须保留、可发展、已否定、未决、命名和知识表达边界；低置信或存在未决项只显示“请核对”，不显示分数。恢复或世界书建议审阅只消费服务端 `decision_state`，旧记录明确显示未保存摘要；纠正仍通过对话，但已有 pending 提案时必须显式选“修订此版”或“另起方案”。修订成功后展示决定摘要、上一版到当前版及关键差异，旧版在世界书审阅中作为不可采用的历史可回看。生成失败不清空当前对话或页面编辑，不提供可编辑决定/links JSON。页面应用只写服务器工作稿，成功后返回世界书；正式页仍需作者显式发布。
- world 共创输入使用带可见标签和持久提示的原生表单；发送按钮在编辑区外，390px 下整行展示并可滚动到固定底栏上方。等待回复依次说明理解目标、核对设定和组织回复；失败后聚焦“再试一次”，复用原作者消息而不重复插入。建议失败保留已有可编辑提案并提供原位重试；API/schema/wire 与项目隔离语义不变。
- `preset=world_core` 复用同一 Generate world 工作区，不新建工作台。空项目从 Writing Home 的「先整理世界观」次操作进入；四个快捷动作只预填 `expand / connect / pressure / consolidate` 对应的可编辑作者消息，不自动发送。只计成功的作者—AI 回合；每累积 3 个新回合才再提示保存，必须先由作者显式收束并通过 seed 覆盖、3–7 条规则、阻断矛盾和日常＋故障纵切门，才能手动保存 `world_design_checkpoint.v1`。checkpoint 内嵌完整 world-state taxonomy，未有证据的六循环、F01–F22、C01–C05 与 T01–T12 保持 `gap/not-run`；它不可采用，跨设备恢复只把决定摘要作为后续参考，不重放过时 AI 聊天正文。未显式保存时仍只保证当前浏览器恢复。
- Writing Home 对 pending `world_adoption_package.v1` 优先显示“去审查”，进入 World Bible 后分栏预览“流水线已写入”与“本次确认将写入”。前端只提交 preview hash；409 要求重新预览，不显示 raw ID/JSON，不绕过后端的 DB + World Bible 原子 apply。
- 生成中心的“加强复核”使用同一账户模型多检查一遍，不会暗中切换模型；会较慢，但仍保留当前对话、来源和目标。
- 生成中心精确上下文可显式选择最多 16 个其他已采用世界书页，当前页不重复出现。选择跟随当前有界会话恢复，只作聊天、收束与建议参考，不会合并、修改或自动采用页面。
- world 工作区的折叠“与外部模型交接”只做当前 target 的文本往返：完整收束预览可复制或下载
  同一 Markdown；回包单独粘贴，作者目标继续留在主输入框。浏览器按 Unicode 字符在 55,000
  上限前拒绝且保留原文，并用 Web Crypto SHA-256 在当前本地会话跳过完全相同的重复包；
  最多保存 20 条不含正文的包摘要。整理仍复用只读 convergence，不创建 suggestion。项目级来源
  明示无法证明全项目未变化，外部编号与检查声明不成为本地权威。当前不上传文件、不存导出记录、
  不做跨设备批次或语义去重。
- world 工作区的“准备视觉稿”只在完整、未过期的收束旁出现。作者先选择单一画面用途，编辑
  “必须保留／准确标签／仍开放／不要新增”，再在当前有界会话中确认；确认与复制／下载文本简报
  都不调用模型，也不创建 suggestion、地图或 revision。来源或作者决定变化时保留简报文字
  并禁用导出和跳转，重新收束后需由作者明确重做。确认后的“打开 AI 地图册”只导航到地图册工作台，
  不自动生成图片；作者仍需在地图册内核对版式、清晰度和画面偏好，再显式开始可能产生费用的任务。
- Owner AI 抽屉只显示一层「设定共创／写作建议／整理资料／查找资料」类别导航，并使用关联的 tab/tabpanel 语义；方向键、Home/End 只移动焦点，Enter/Space 才切换。抽屉避开固定顶栏与手机底栏，打开后焦点进入可见关闭按钮，ESC/关闭恢复到触发入口；关闭会移除 `owner_ai=1`，避免刷新后意外重开。嵌入的 Generate 不重复显示内部模式栏，独立兼容页仍保留旧模式导航和深链。写作建议先显示当前章与 Scene，并按已保存正文自适应为「续写这一章」或「生成整章建议」唯一主操作；其他写法渐进展开，缺少 POV 或未保存正文时原位解释并复用既有保存入口。生成中可收起抽屉查看持久进度，结果仍进入候选审阅，不覆盖工作稿；世界目标公开当前选择，对象模板和整理资料的常用任务使用原生选择器，任务字段可按可见名称访问。
- Owner AI 抽屉的「整理资料」使用原生表单，以「整理参考资料」为唯一主操作；常用任务收进表单内的可选选择器，只预填下方任务且允许继续改写，手机首屏优先露出任务输入和主操作。结果先按作者语言列出会交给 AI 的资料、加入理由、采用状态、裁剪提示和可核对来源；raw key、token 与内部枚举只在默认折叠的整理细节中出现。结果动作只在整理成功后出现，并以「查看完整资料」为主、「带到世界设定对话」为次；复制和导出只留在完整资料页，避免重复入口。render 失败保留摘要并只重试 render。未提交任务、预设和预览复用既有有界会话并按项目隔离，`owner_ai_mode` 支持刷新和浏览器历史恢复；旧 Generate 深链仍兼容，API/schema/wire 不变。
- 世界书默认使用中文类别和任务状态；投影恢复键、任务 ID、原始状态与后端 warning
  收入折叠的“诊断信息”，不占用作者阅读和编辑设定的主界面。
- 世界书展示模式和筛选分类公开当前选中状态；未保存修改确认、偏好持久化及 API 契约不变。
- 深度导入完成卡显示“自动归并 X 个重复对象，仍有 Y 组可稍后检查”；遗留项只引导到现有“人物与世界 → 智能去重”，不新建工作台或自动发起第二次全项目扫描。
- 写作台自动保存以编辑序号保护请求期间的新输入；保存失败会持续显示本机备份说明和“重试保存”，并在恢复前阻止切章与设为正式正文。章节切换期间隐藏旧正文并显示骨架；加载失败提供原位重试，失败目标不会写入项目会话指针。版本切换触发局部重绘时不会用旧响应覆盖正文。发布、生成、冲突检查和自动提取的生命周期反馈统一从页面上缘下滑到写作区顶部居中浮层，不挤动正文，底部状态栏始终吸底；同一生命周期的成功/失败不再重复发送全局 toast。后台 RAG / 记忆后处理完成只收起进度，不再次重载当前编辑器，且已 dispose 或已被新任务取代的轮询响应不会回写。恢复历史正文时保留选择当时的最新版本快照，发布前若其他会话已更新则提示 409 冲突。日目标只累计当地日期内各章首次基线后的正增量，切章、刷新和删后补回不重计，顶栏与底部状态栏共用同一数值。写作台从设置服务读取项目生效作者偏好，日目标、编辑器字体和默认专注模式遵循“项目覆盖 → 全局默认 → 系统默认”，只在接口不可用时兼容读取旧本地偏好。完整编辑只保留页内“AI 写作助手”和状态栏“专注模式”，移动速记仅在缺少对应控件时于页头/写作视图菜单显示入口；任一状态都不重复。正文 Scene 提取统一使用 imports 的“从正文提取 Scene”入口。顶部“AI 续写”锁定当前服务端 base draft，任务轮询不设前端总截止时间，完成后自动打开只读候选；审核条位于正文前、按状态只保留一个主操作，存在当前工作稿时可一步打开既有版本差异，采用/拒绝共用全局确认并就地保留失败；≤760px 自动收起章节目录并隐藏只读状态条。候选统一提供“采用到工作稿”和“拒绝建议”，拒绝仅软废弃并保留审计。普通正文候选不会因 POV 检查明确不适用而显示角色视角解析失败。
- 写作页头的渐进菜单公开展开状态；外点、动作或 Escape 会关闭，键盘动作关闭后归还触发器焦点；390px 菜单项保持 44px 触控高度。移动速记使用工作区剩余高度，发布、保存和「更多编辑」始终位于移动底栏上方。
- 章节前后导航、Scene 展开和大纲浮窗章节链接提供作者可读名称，并公开当前章节或展开状态；既有选择、收起与路由/API 契约不变。
- 写作页右侧“写作副驾驶”常驻显示当前 Scene 的结构警报和最近冲突检查；must/must_not 字面覆盖只由后端按当前 Scene 的精确正文范围产生，并诚实标为“字面预警”。范围缺失时检查降级而不扫描整章；编辑后旧检查立即标记过期，切换项目、章节、Scene 或草稿时晚到响应不会覆盖当前驾驶舱。警报不自动运行 LLM，也不修改正文。
- 冲突详情中的“定位正文”和“打开来源”只在持久化证据提供可用正文范围，或受支持的来源模块/打开目标时启用；无证据时以禁用的明确标签说明不可用，不改变既有定位和来源处理契约。
- 作者动作标签不形成新队列：Writing Home 读取现有领域队列的只读投影；写作字面预警和普通软判断标为“可以改进”，需要人工选择的 World/Outline 项标为“需要决定”。已处理、忽略和稍后项退出当前投影；A→B 与 B→A 关系在首页合并为一条对象对提醒，并汇总两个方向的待处理数，但不改变关系工作台的有向语义。首页待办列表下方只保留一个“查看更多”。冲突详情显示来源版本与当前章／场景的定向复检范围，并把来源缺口写成作者选择而非自动阻断。workspace-summary 与 Writing conflict wire 仅增加可选字段，数据库 schema 不变。
- 版本选择、历史入口和最近冲突检查以紧凑控件常驻在编辑器顶部操作行，无需滚动到正文底部；版本历史让其他版本可一步与当前打开版本比较，单独预览和软废弃的“移入历史”收入行内“更多”，任意两版比较按需展开。390px 的行菜单进入文档流，不遮挡或裁切操作。Diff 可临时比较工作稿、待审核、已发布和归档版本，先按段落对齐，再在变化段落内标记中文字符、标点和英文词，识别稳定段落移动并对超长输入安全降级；动态正文统一转义，比较本身不创建、恢复或采用版本。
- 390px 速记输入会实时同步同一编辑器状态；首次保存返回的 draft id/version 会立即回写，连续保存复用同一工作稿。「更多编辑」与「返回速记」可逆切换且保留尚未保存的正文；完整模式按项目写入既有恢复指针，刷新、浏览器前进后退和作品切换不会串用。
- RAG 索引维护的技术诊断区可按需加载隐私安全的检索追踪摘要，不展示 raw query 或正文。
- Writing Home 与 RAG 状态页不请求各自未使用的章节、Scene 或人物列表。World/Outline 的低频重型标签、AI 预览、AI 抽屉和 Scene 工作台使用 Vue 原生异步组件；默认骨架同步显示，加载失败自动重试一次。
- 写作输入先更新内存，250ms 后合并持久化本地备份与恢复指针，并在保存、切换、卸载和页面离开前强制 flush；会话内每项目只保留当前章加最近四章，最多五个最近项目，尚未完成备份的 dirty 快照不淘汰。
- RAG 索引维护的重建范围两端都留空时表示全项目重建；只要填写范围，就必须同时提供起始和结束
  章节，且两者都是大于等于 1 的整数、结束不小于起始。前端在调用 API 前拒绝不完整或反向范围，
  不得把无效输入静默丢弃后退化成全量重建。创建维护任务的写请求不随视图卸载取消；离页只停止轮询和晚到界面更新，任务号仍在响应到达后持久化。
- 小说检索查询卡使用原生表单，只把“查找资料”设为主操作；查找方式和正文版本常驻并显示用途，其余章节、资料范围和可见视角收进“更多条件”。待处理世界设定只有选中世界设定范围后才可用并显示原因；作者视角隐藏读者/角色专用截止字段。嵌入 Owner AI 抽屉时，查找 URL 只替换 RAG 条件并保留 owner 模式与当前写作位置，提交后抽屉不关闭且刷新恢复；共享检索会话在挂载时按项目清空，项目切换不串搜索词或结果。请求 payload 与后端契约不变。
- 小说检索的智能/字面模式都按章节聚合同章结果：智能模式解释为语义相关性检索，字面模式解释为完全一致文字匹配；结果卡显示该章聚合的相关片段数或出现次数。作者视角的正文卡消费 `parent_scene_contexts` 和 `writing_relevance`：每个被聚合的父 Scene 都单独展示序号、标题、目标/冲突/情绪摘要及写作关系，点击可直接打开对应 Scene。当前 Scene 只从同项目的写作台快照读取，不使用跨项目全局残留值；读者/角色视角隐藏作者专用 Scene 上下文，不显示“未关联 Scene”的误导文案。
- 小说检索页的“问世界”始终按当前项目作者可见的正式世界笔记和已发布正文回答，不沿用读者／角色高级筛选。界面显示逐条引用、不确定性和本次未查范围，引用先由后端重开并核对新鲜度；模型名、provider、source hash、snapshot 和内部 ID 不进入主界面。回答零写入，只有作者点击“保存为待处理建议”才创建一条世界书建议；停止与项目切换都会丢弃迟到响应，且不承诺瞬时中断远端 provider。
- 小说检索一次最多取回 100 条现有 evidence 命中，但首屏只挂载 20 张结果卡；“加载更多”
  每次再显示 20 条。章节范围的非整数、非正数或倒置条件会在请求前提示并保留给作者修正，不会被伪装成空结果。检索词、方式、正文版本、可见视角、章节和 scope 写入 hash URL，刷新与
  前进/后退会恢复并重新检索；同项目的 search/status 路由往返保留已执行结果和未提交筛选，项目切换才清空。显示游标和证据抽屉不写 URL。新查询会 abort 旧请求，并以
  project/lifecycle generation 拒绝晚到响应。证据抽屉另有独立 abort/generation/project/drawer
  门禁，关闭抽屉或切换项目后的旧正文、引用和导航结果不能覆盖当前抽屉。
- `settings` 是无项目也可访问的账户设置页：文本模型连接只显示固定模板和 Key 输入，并只读显示
  余额；独立图片连接固定为 gpt-image-2，且只显示“密钥连接成功”（可能有延迟，无充值入口）。DeepSeek 默认模板是 `deepseek-v4-flash`；Kimi K3 在
  后端真实兼容门禁通过前不会出现。图片连接默认折叠，只在地图册需要时展开。连接、作者偏好或项目偏好加载失败均在原位说明并可重试；校验错误贴近字段，未保存、保存中、已保存和失败状态会持续显示。`project-settings` 只管理深度导入参数和作者偏好；深度导入的专家参数整体默认隐藏，展开后再按作者问题分组并显示偏离默认的项目数，校验会自动展开专家区和出错分组并聚焦字段，且可返回写作工作台，
  不再提供项目级 provider/model/Key。作者偏好在账户和项目表单中把字体、专注模式及来源值本地化为中文，但保存、传输和存储仍使用稳定底层值（字体枚举与专注模式布尔值）。两个设置页在存在未保存输入时都会拦截离开。
- 设置外壳只保留一个页面标题，并以正常路由区分“账户设置 / 当前作品设置”；模型模板单选组和项目设置页签支持方向键、Home/End 的选择与回焦。390px 下设置表单改为单栏、操作按钮保留触控高度且页面无横向溢出。模型连接、项目继承、保存/重置、路由和 API 契约不变。
- 旧 `llm` 入口会按当前项目状态跳转到 `project-settings` 或 `settings`。
- 旧 `context` hash 不再是一级页面，路由层会重定向到 `generate?tab=task`。

## 内容优先布局

- 项目页采用编辑式“作品档案”布局：主题化纸面、accent 索引色、几何拼贴项目封面；
  当前项目置顶并占主版面，搜索、批量选择、导入和回收站仍沿用原有交互与接口。导入历史只为失败记录显示经清洗、限长的作者可读失败原因；完成或进行中的记录不会泄露错误字段。历史加载失败不会伪装成空记录，同项目刷新失败保留上次成功内容并提供安全重试。新建或编辑项目的必填标题校验、保存失败都会保留现有输入，且字段使用关联 label 提供可访问名称。抽屉已选文件可直接复用到“导入为新项目”确认，取消确认不清空文件选择；当前 UI 只接受已在锁定运行时验收的 txt/epub/html/htm，且不超过 50MB，mobi/azw3 保留后端白名单但在真实文件验收前不对作者开放。
  `760px` 以下改为单栏，390px 保持完整统计与主操作且不横向溢出。
- `editorial-theme.css` 将三主题视觉体系扩展到全部一级页面、子标签、弹窗、表格与辅助栏；
  `styles.css` 继续拥有结构布局，主题覆层不得改变路由、DOM 事件契约或 API/wire shape。
- 项目页以外的工作区使用克制的几何切线；一级空状态采用分栏海报式构图，
  窄屏自动收敛为单栏，不以装饰遮挡操作或业务反馈。
- 点缀装饰只允许三处落点：顶栏品牌区、写作页编辑区（右上与正文下方各一组）和左栏导航底部；
  一律取近底色、不接收指针事件，专注模式与 `760px` 以下隐藏；数据区零装饰。
- 功能控件必须保持可辨识：主操作使用主题 accent 实体面和通过 4.5:1 的主题前景色；普通按钮有明确边框；文本输入、
  选择器和编辑区使用 surface 底与完整边框，聚焦时显示 2px accent 焦点环。night 暗色主题
  保留同一层级，`760px` 以下常用控件高度不低于 `42px`，表单输入不低于 `44px`。
- 写作、Scene、世界书、地图册和生成中心采用统一的内容优先分栏；桌面端正文、主列表、编辑区或图片获得约三分之二的可用宽度。Scene 未选项时不渲染空详情栏，列表先使用完整工作区；选中后才恢复 68/32 编辑分栏。详情字段按「基本信息 / 创作要点 / 章节与来源」分组，始终提供「返回列表」；吸底操作栏以「已保存 / 保存修改 / 保存中」反映草稿状态，当前待办保持就地，合并/拆分收入向上展开的「更多」菜单。草稿未保存或正在保存时，待办与结构操作原生禁用，防止刷新覆盖草稿。窄屏详情作为模态层打开时暂时隐藏底部主导航，关闭后恢复。
- 辅助栏使用统一的主题化折叠控件，折叠选择按项目和页面保存在当前浏览器会话中；写作专注模式仍优先隐藏两侧栏。
- 中等宽度会重排第三栏，`760px` 及以下改为单栏、抽屉或手风琴；折叠控件完整支持浅色、暗色、键盘焦点和减少动效偏好。
- 390px 地图册保留浏览、采用和拒绝；精确标注拖动与蒙版编辑提示在桌面完成。世界书在 ≤760px 为单栏；Scene 工作台无 `scene_id` 时保持未选中列表，桌面端同样不保留空详情占位。
- 任务进度默认显示紧凑摘要、状态和细进度条；失败、恢复或需要用户确认的状态自动展开，用户手动选择在任务重绘时保持。
- 共享业务模态框使用带标题关联的 modal dialog 语义；打开后焦点进入内容或操作区，背景不可操作，Tab/Shift+Tab 不离开对话框，Escape 关闭后恢复到有效的原触发控件。连续替换模态内容时仍保留最初触发点；正文中的可编辑控件发生未保存变化时，关闭按钮、取消、遮罩和 Escape 都会先确认是否放弃，成功操作不重复确认。AI 参考资料可通过关闭、遮罩或 Escape 正常取消；晚到结果不会写入后续弹窗。
- AI 参考资料窗打开即执行零持久化预览，按“必须使用 / 系统找到 / 我添加的 / 本次不用”展示逐项资料；手动搜索和自然语言提议均需重新编译，预览指纹、blocker 或待处理 patch 会确定性控制“按这份资料开始”。所有作者手动模型 wire 统一携带 `context_confirmation_id`；本轮不新增数据库表。

## 安全与契约

- `index.html` 配置 CSP meta baseline：脚本和外部样式来源仅允许本源，连接仅允许本源及本地开发后端；`style-src` 暂保留 inline style 兼容。
- 封闭测试服的 `APP_ACCESS_TOKEN` 只保存在 `api.js` 当前页面的 module memory，不读写 Web Storage；刷新页面后需要重新输入。普通请求、导入上传和前端错误上报共用该内存令牌，被后端以 401 拒绝后立即清除并打开应用内密码模态框，避免依赖浏览器原生 `prompt()`；取消输入不会重试原请求。
- Vue 模板动态内容使用插值自动转义；命令式 seam 默认使用 `textContent`，必须拼 HTML 时先走 `esc()`。
- 世界关系审查预览使用 DOM 节点和 `textContent`，不把动态对象名称或关系类型送入 `innerHTML`。
- 右下角错误徽标是带计数和 dialog 状态的原生按钮；它按当前项目或未关联项目范围展示经脱敏的本地错误。打开的是非模态诊断面板，关闭会回到徽标；清空在面板内明确二次确认，只影响当前范围，不影响其他项目或已上报记录。程序化 `window.errorLog.clear()` 保持直接清空当前范围的兼容语义。
- 当前已落地共享 JS API 契约校验第一阶段：`apiContracts.js` 注册高风险 wrapper 的 method/path/query/body/timeout，浏览器 `api.js` 与 Playwright API helper 共用同一 registry 和序列化规则；Vitest 覆盖加载顺序、必填 body、method 固定与代表 endpoint 映射。
- TypeScript / OpenAPI codegen 仍是未来设计项；当前契约层不覆盖响应字段级 schema drift，设计记录见 `docs/frontend/typescript-api-contracts.md`。

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `?` | 打开快捷键帮助 |
| `:` | 聚焦命令栏 |
| `/` | 搜索 |
| `Esc` | 返回 / 关闭弹窗 |
| `j` / `k` | 上下移动选择行 |
| `n` | 新建 |
| `e` | 编辑 |
| `s` | 保存 |
| `g` | 生成 |
| `r` | 复查 |
| `c` | 确认 |
| `x` | 删除（二次确认） |

单键业务快捷键在表单、命令栏、快捷键帮助和既有业务 modal 中保持不活动；Enter 优先交给当前交互控件，非交互工作区仍保留打开/选择动作，只有已成功处理的快捷键才会消费原始按键。

## 管理页批量操作约定

- 列表型管理页支持多选、当前可见列表全选和批量工具条，包括项目、世界对象/待处理项/关系/别名、剧情结构、Scene 工作台、写作章节树和地图列表。
- 世界对象库与 Scene 工作台标题区提供“普通 / 热点”模式。URL 的 `mode` 优先；未指定时按项目和页面读取本地偏好，首次默认热点。模式切换保留搜索、类型、状态等通用筛选，并清除热点阶段/focus、分页偏移和批量选择。
- 世界对象热点模式使用后端全量智能排序，不再按自动入库批次切断顺序；顶部显示“重要 / 近期热点 / 其他”聚合和索引覆盖提示，表格与卡片共用热点标签。卡片/表格只切局部呈现并同步 hash，不重挂载世界对象页；筛选中尚未应用的输入按项目与当前 query 保留，外部深链仍以 URL 为准。普通模式保留原批次分组与既有排序。Scene 热点模式仍按剧情顺序，以“当前 / 后续 / 已写过 / 未定位”的文字和色点区分进度；Scene 筛选默认收起为含已启用条件数与未应用修改的摘要，进一步的整理条件收入「更多筛选」。已应用筛选和未应用输入均按项目保存在当前浏览器会话中，刷新、浏览器往返和作品切换后恢复且不跨作品串用。剧情进度与健康待办共用「场景概况」：桌面端完整展开，窄屏默认收起为当前剧情段、数量最多待办与其他类别数摘要；主动筛选后摘要跟随当前条件。
- 世界对象图片始终通过 owner + `novel_id` 门禁接口读取，前端不拼接或缓存 S3 key。卡片使用轻量
  缩略图并在失败时回退首字色块；整卡 Enter/Space 打开现有编辑弹窗的图片详情，上传控件、复选框
  和更多菜单不触发详情。卡片操作区统一贴底，首列更多菜单向右展开；窄屏详情改为单列并释放失效
  Object URL。
- “全选”只作用于当前可见列表或当前分页，不跨分页选择全部筛选结果。
- Scene 工作台零选择时不显示批量条；单选只显示当前主动作，至少双选后才显示机械合并和 AI 融合，并可明确退出选择。混合选择按待办类型分组且一次处理一组，未处理组保持选中。结构类提醒可标记为无需整理并从更多菜单恢复；正文定位和融合建议仍独立处理。窄屏选择区和批量按钮均保持 44px 触控高度。
- 世界对象的对象库与待处理对象/别名/关系子标签都在批量工具条提供显式全选；所有筛选区默认折叠，展开状态按项目缓存在浏览器本地，折叠后仍保留当前筛选条件。
- 待处理关系默认按有向对象对分组，别名按所属对象分组；每页可选 20 / 50 组，全选只作用于当前可见项，不隐式选中全部筛选结果。
- `world/review` 把对象、别名和关系收在同一待定工作台：桌面端为队列 + 决策栏，390px 为队列/详情分步页。队列条目统一以次级“查看并决定”进入，只有决策区的“采用别名 / 采用关系”保留主按钮；桌面选中后焦点进入决策区，窄屏进入全页决策并可返回原条目。当前项以前端 URL 参数 `review_item` 保存，刷新和浏览器往返可恢复，切换作品不会串用。别名以“名称 / 称谓 / 身份”、关系以七类最小语义分类作为 AI 检索层，详细类型继续保留作者表达；普通列表和审核卡用中文显示通用分类与目录内详细类型，未收录详细类型保留原文并标记“自定义”，缺分类统一显示“待分类”。普通别名和保守推荐关系可在决策栏直接采用；分别采用、换端点、归并和忽略收在次级编辑器。端点搜索调用项目对象接口，不受当前 20 条列表限制。已采用关系和别名也可在普通列表编辑双层类型或移动到其他有效对象。
- 关系卡显式提示反向候选/正式关系，抽屉实时预览采用结果。前端在确认前校验 20 个关系决策 / 50 条所选关系上限，成功后自动进入下一组；单项失败会在原卡片展示服务端原因。Outline 篇章/剧情线筛选默认折叠，摘要显示已启用条件数，应用或重置后收起并把焦点还给摘要；编辑中筛选按子页会话保留，分页只变更已应用 query，不会把尚未点“应用”的控件值偷偷提交。“分析与整理”执行任一动作后自动收起，避免弹窗关闭后继续遮挡内容。
- 别名和关系编辑器采用“通用分类 + 详细类型”双字段：作者未亲自选择通用分类时，详细类型变化会同步目录推荐；作者选择后不再覆盖。未收录类型仍可作为自定义详细类型保留，映射建议必须点击才会修改。缺分类的别名直接/批量采用，以及关系采用、归并、分别采用都会在请求前阻断；忽略不受影响。批量采用/忽略均一次确认、一次请求，上限 50 条；部分失败保留选中和编辑草稿，执行指纹过期时丢弃旧字段并按当前内容要求重新确认。由建议队列拥有的 compatibility shadow 别名不可在该工作台另行采用。
- 复核卡默认只展示作者可读的来源、Scene、章节、短引用与强度/置信度；Workflow、Scene UUID 和原始证据收进可复制的“诊断信息”。关系数值始终称为“强度”。
- 待处理别名与关系的高级筛选控件均有明确的中文可访问名称，并分别支持 `alias_kind` / `relation_kind` 分类筛选；既有 `type_kind=recommended|custom`、详细类型、分页和 URL 草稿恢复语义保持兼容。Outline 的 Workflow ID 仅在“更多筛选”内以“整理批次编号”提供，wire 值不变。
- 已采用对象库的多选工具条提供“融合”和“标记为别名”，不再提供“批量标记已检查”或“批量采用”。两种操作都要求选中至少两个同类型对象、明确选择保留对象并二次确认；融合会合并内容和关系，别名化只迁移关系和登记来源名称，来源对象均进入可审计历史态。
- 所有待处理世界对象都可在采用前微调名称、类型和概要；普通 candidate 通过 promote 请求在同一事务中编辑并采用，建议队列对象继续走队列裁决。待处理别名和关系以单项直接提交为主，会归档、忽略或复用已有资料的操作仍要求二次确认；未提交输入按项目和执行指纹保存在会话草稿中，指纹改变后不重放旧输入。三类队列加载失败都显示作者可读说明和“重新加载”，技术详情默认收进“诊断信息”。
- 世界对象的新建、编辑后采用和普通编辑共用项目类型目录：可复用已有自定义类型或创建新类型；对象库筛选同步支持自定义类型。已采用对象改类型会二次确认，后端专属依赖 blocker 在原弹窗内按类别和数量展示并保留输入。
- 待处理对象中，已有明确别名目标的条目按目标对象合并展示；没有明确目标但同类型名称高度相似的条目也会合并展示。该分组只影响当前页界面，不自动合并、采用或写库。
- 删除、忽略、永久删除等危险批量动作必须二次确认；执行后会显示成功/失败数量。
- 普通待处理对象的“合并”是行内主操作，不放在更多菜单里；没有明确目标对象时需要先搜索并选择目标。由建议队列拥有的兼容影子只通过建议采用/忽略，不直接修改影子对象。
- 查找、Context 和 owner AI 抽屉不强制提供批量操作，只保留清晰的空状态、错误状态和任务进度；查找质量与修复范围直接展示，检索记录、后台连接与失败重试收在“修复查找”的诊断详情中。诊断异常只给出重试或联系管理员路径，不要求作者理解 worker、embedding 或重启后端。
