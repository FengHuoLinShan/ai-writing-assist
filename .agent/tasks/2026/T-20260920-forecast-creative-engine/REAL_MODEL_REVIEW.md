# 真实 DeepSeek 小样本与 Codex 评阅（2026-09-21）

## 范围与账目

- 仅使用本任务专用 PostgreSQL 测试库和原创冻结前缀；未读取 Guimi、用户正文或生产库。所有新功能开关仅在评测进程打开。
- `deepseek-flash` 真实项目连接、真实任务 worker 与知识审查路径。首个 `consent-06` 创作运行采用供应商默认思考模式；其余运行固定为 `thinking: disabled`，因此首轮不可与后续同组比较。
- 9 次真实运行、71 个具有供应商 call ID 和用量的请求；输入 302,144 token、输出 105,350 token，未知用量 0。按 [DeepSeek 官方峰时价格](https://api-docs.deepseek.com/quick_start/pricing/)且不计缓存折扣，保守估算 **$0.2171**，低于用户授权的 $20 上限。此数不是供应商账单金额。各次回执见同目录 `artifacts/live-*.calls.json`；`live-consent-06-preflight-failed.json` 是零请求的处理器注册失败，仍保留。
- 我作为一个 AI 评阅者逐项回读冻结前缀和可见产物。此评阅不伪装成两位独立人工，也不填入 `creative_forecast.py` 的人工评分 CSV。没有 A/B/C/D 四臂配对，没有 120 前缀全量结论。

## 逐项结果

| 样本 | 真实请求 | 运行结果 | Codex 评阅 |
|---|---:|---|---|
| 创作 `consent-06` 默认思考 | 3 | partial | 12,000 token 输出被截断，未产生可比较试改。 |
| 创作 `consent-06` 关闭思考 | 2 | failed | 调查步骤提交越权 patch，服务端以 `GRANT_SCOPE_CONFLICT` 拒绝。 |
| 创作 `consent-06` 明确步骤权限 | 28 | budget_exceeded | 两份试改和检查曾生成，但反复规划耗尽单轮额度。一个试改将作者约束直接插成解释性旁白；另一个凭空赋予管理员“中止对照”的职权。后一问题虽被后续反证指出，先前检查仍给过通过。两份均不宜采用。 |
| 创作 `absence-06` | 1 | partial | 模型主动询问留白是否须保留，符合谨慎方向；当前评测却要求两份试改，把合理停手算作未完成。 |
| 创作 `rule-06` | 29 | budget_exceeded | 一次试改把“持续外部热源”替换为来源/位置条件，改变了已明示规则；后续检查指出，但运行仍耗尽额度。不得采用。 |
| 前瞻 `consent-06` | 2 | completed | 建议保留双方退出权，含低承诺收束方向；本样本可作为待作者选择的建议。 |
| 前瞻 `absence-06` | 2 | completed | 把第 5 句“第四册暂时借走”称为当前段落终点，漏掉第 6 句“看守人”猜测；自动知识审查仍判通过。位置判断不可信。 |
| 前瞻 `rule-06` | 2 | completed | 把原文“两张同样的封纸”说成“同一张封纸”接受两次测试；这改变了对照前提，自动知识审查仍判通过。 |
| 前瞻 `keepsake-06` | 2 | completed | 建议安静收束，未把普通收据改成秘密；可作为低承诺建议。 |

## 裁定与下一步

**本轮质量准入未通过。** 两个创作样本达到单轮预算仍无完整交付，前瞻四个样本中两个含可定位的原文事实错误。自动知识审查给出的 `passed` 不能替代内容评阅；当前也不能给出 C−B、C−D 或前瞻 B−A 的真实模型增量。剩余 $19.78 未用于继续批量调用，因为硬失败已足以阻断本轮准入，且当前评测工具只有离线汇总，缺正式四臂真实执行器。

后续先修正已观察的原文事实错误与创作规划收束/预算问题，再提供实际 A/B/C/D 与前瞻 A/B 的同资料、同模型、同参数执行路径；重新冻结可复现代码版本并跑全量。安静反例允许“无需修改/需要作者判断”的正确停手，不能强制两份试改。正式人工双盲要求如仍保留，应另行安排；本文件是用户授权的 Codex 评阅记录。
