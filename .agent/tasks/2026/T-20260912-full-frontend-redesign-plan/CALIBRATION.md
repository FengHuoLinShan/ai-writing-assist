# Luna首批校准记录

实际使用宿主的gpt-5.6-luna，主代理保持当前模型；没有切换产品模型或调用业务LLM。

| 样板 | 实际子代理档位 | 校准发现 | 处理 |
|---|---|---|---|
| P01a/P01b写作 | medium（首个A01代理沿用；followup不支持覆盖档位） | 输入Vue插值回写、空稿与保存基线需纠正 | 定点修复后主代理浏览器检验连续输入、换行、撤销、换章、失败保全；关键状态增加回归 |
| D01世界密集资料 | 新代理high | 局部列表/卡片/筛选5测试通过；深层跨域仍需后续联验 | 延续小包，每域单写；D02–D05分步完成后独立审查纠正决定/对象会话与比较上下文 |
| P03a/P03b候选 | medium（沿用作者代理） | CSS :global作用域、拒绝handler与目标资料身份需主代理复核 | 不以绿色单测替代实看；修正作用域、采纳/拒绝约束、领域对象与返回，再录中间帧 |

首批没有证明“一个长任务可无监督完成全部前端”。后续继续按独占文件、小包、共享基础主代理单写与独立评审；新建世界实施和独立浏览器评审位均为high。

宿主未提供每次followup的token/费用与可分离的模型耗时，故不填推测数值；各RESULT记录实际检查，不能把文件mtime或包含主代理等待的墙钟时间当成模型速度。未做跨模型性能比较或费用保证。
