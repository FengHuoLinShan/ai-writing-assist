# 智能整理验收记录

## 当前边界

工程实现及适用工程回归已完成。真人标注、实际作者处理时间尚未取得，因此语义自动采用资格仍关闭；模型置信度和开发模拟不计作人工准确率。

原始两个项目未修改；所有执行发生在本任务隔离副本。旧项目实际活跃候选为316项，演示项目106项，已合并/忽略对象下的历史别名不计作减负。

## 真实模型诊断样本

### 旧项目开发样本

任务状态：failed；模型请求计数：4。

- **韦尔奇·麦格文**：optional。原文逐字出现“死鬼韦尔奇·麦格文”，并由“这位同学”表明它指已死的韦尔奇·麦格文本人，指代对象可确定。但“死鬼”是对死亡状态的临时口语修饰，不是他人或本人反复使用的称呼；同一场景对外叙述仍称“韦尔奇”。因此只能作为局部、一次性的昵称别名推断保留，不能认定为稳定昵称。
- **韦尔奇·麦格文**：incomplete。All 1 structured output attempts failed: Schema validation failed (attempt 1): {"error_count":1,"errors":[{"loc":["[field]",0,"[field]"],"type":"literal_error"}]}

开发回执：`63591936-67e9-43c9-8cc6-cf04de48664c`。这些样本已用于发现问题，不能再冒充未见保留集。

### 与开发章节分离的关系样本

任务状态：failed；模型请求计数：3。

- **炼金室、值夜者小队**：optional。邓恩直接称该房间为“我们值夜者小队的‘炼金室’”；老尼尔在同一房间内说“这里是值夜者小队的总部，这是黑夜女神教会的地底”；克莱恩离开时从炼金室沿楼梯上行。原文语义足以直接认定炼金室位于值夜者小队总部地下空间，空间归属稳定；“炼金室”为局部场景称呼。
- **克莱恩·莫雷蒂、塔罗会**：decision。原文直接支持克莱恩定期参与和“倒吊人”“正义”在灰雾神殿举行的聚会，并以“愚者”身份接受询问与委托；但未出现“塔罗会”组织名或“成员”直称，成员关系由定期聚会和持续互动推断。克莱恩的真实身份对聚会参与者保密，故身份判秘密；若把该定期聚会视为组织，其参与关系在已提供章节中持续。
- **班森·莫雷蒂、梅丽莎**：incomplete。来源场景没有精确正文定位
- **克莱恩·莫雷蒂、邓恩·史密斯**：incomplete。来源场景没有精确正文定位

开发回执：`ce2d9469-3391-418d-ac20-9c2dc6dc58ea`。这些样本已用于发现问题，不能再冒充未见保留集。

### 演示项目场景样本

任务状态：done；模型请求计数：2。

- **穿越醒来：死亡现场的确认**：decision。本组包含受保护场景或引用范围调整，请核对后成组确认。

开发回执：`ef4e6145-9650-47a2-90a6-935e91e11136`。这些样本已用于发现问题，不能再冒充未见保留集。

## 已据实修正的问题

- 旧库缺少当前schema字段：只迁移了隔离副本。
- 已完成任务恢复时父页面反复刷新：改为仅在本次运行完成时刷新，浏览器回归已通过。
- 模型将系统分类当作必须逐字证明的内容：澄清事实证据与分类元数据的区别。
- 旧场景未打标但缺精确定位：纳入来源检查，按共享章节成组提案与事务提交。
- 不确定字段带解释导致字段名不能匹配：改为封闭字段枚举，解释独立保存。
- 较晚章节不能把事实提前；原文子片段不作为新证据触发重复模型调用。

## 人工验收尚缺的证据

- 两个项目中足量、未用于调试的冻结样本及逐项真人标注。
- 语义自动采用准确率至少98%，身份/来源/作者决定/可见性严重错误为零。
- 同一作者、同一任务的基线与新流程实际决定次数和耗时；目前不声称减少50%。
- 资格须绑定提示词、供应商配置指纹和模型；未通过或未知的类别保持建议。

## 工程证据

- `make test-ci TEST_WORKERS=2`：后端5278 passed /12 skipped，覆盖率85.34%；部署268 passed，Ruff、依赖审计、密钥卫生和文档检查通过。
- 聚合运行前端仅2处旧文案预期失败，已同步“待处理资料”的产品语义；补跑前端全量2471 passed /181文件。未重复已通过的后端门禁。
- PostgreSQL专项9 passed，包括并发基线漂移、重复执行及场景组原子撤销；运行于专用隔离数据库。
- 390px浏览器流程1 passed：一次授权、排除例外后成组采用、原文查看、刷新恢复（任务响应受控，真实领域行为另由PG验证）。
- 前端lint、生产build/发布资源校验、最终 `make docs-check BASE_REF=origin/main`、`git diff --check` 通过。
- 原始本地日志见 `backend/.test-artifacts/review-resolution/` 下 `test-ci-handoff.log`、`frontend-final.log`、`pg-checks-final.log`、`browser-checks-final.log`、`docs-final.log`；该目录不纳入Git。
- 所有实现位于 `codex/import-review-resolution` 隔离工作树，未提交、合并、推送或部署。原项目与其他任务WIP未修改。

## 交付判断

工程路径具备受控启用和恢复能力；完整产品验收仍因真人标签和耗时记录缺失而未完成。语义自动采用资格保持关闭，不能把此状态描述成已经实现50%减负。后续应先用未见样本完成真人三组对照，再按判断类别开放资格。
