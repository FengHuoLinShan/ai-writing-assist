---
id: T-20260911-import-review-resolution
title: 深度导入智能整理与确认减负
status: blocked
created: 2026-09-11T00:40:00+08:00
updated: 2026-09-11T10:09:09+08:00
---

# 深度导入智能整理与确认减负

## 恢复快照
- 工程实现及适用工程门禁已完成；真人质量与作者效率验收未完成，整体任务不能标记完成。
- 唯一实施工作树：`/Users/tywww/Desktop/项目/ai-writing-assist-review-resolution`；分支 `codex/import-review-resolution`，基线 `a89e8b813`。所有变更未提交。
- 原目录保留原 WIP：`.agent/TASKS.md`、Chrome UX审查任务及文档、`.zcode/`；原始两个项目数据未改。
- 下一步：由真人参与冻结未见样本的逐项标注和同任务计时，不能由 Agent 代签。工程独立检查已完成；整体状态blocked仅指质量与效率验收缺少真人输入。
- 已发异步问题，询问用户本轮参与真人验收或另行安排；未收到回答。语义自动采用资格空集保持关闭。

## 目标与授权
- 一次范围授权、可靠事实自动采用、关键问题成组决定、可选建议独立、失败可恢复、应用可撤销。
- 覆盖新导入和存量候选、定向场景修复、Assistant与工作台同一领域操作。
- 复用现有单Agent/确定性任务/Evidence/World/Story；事实组最多3个模型步骤，场景组最多2步，恢复不重置预算。
- 原计划质量门槛：普通事实自动采用准确率至少98%，严重身份/来源/作者决定/可见性错误零；人工决定次数及处理时间降低至少50%。模型判断、合成测试均不能替代真人验收。
- 不写原项目数据、不复活原取消任务、不重导整本；不提交、合并、推送、部署；不更新全局记忆。

## 已实现
- Imports共用范围冻结、来源检查、按需语义判断/反证/一次返修、问题组、采用及汇总；新导入Phase2后接入、存量独立任务、停止恢复、显式撤销。版本化授权与提示词schema；旧权限与检查点不升级。
- World按32项依赖拆包，候选逐字段门禁、别名采用、确定性冗余消解和前后回执。保留正式校验、作者裁定与警告签收；整理不自动发布世界书页面，也不再把可选结果装入旧式全量采用包。
- Evidence以授权正文精确物化、哈希/身份/时间重验；不引入后章或外部百科，来源失效保持未完成。
- Story边界冲突仅影响交界章节；来源问题与字段不确定分开。按共享章节成组核验覆盖和修复，受保护或范围变化先给具体预览；来源映射和撤销按组事务，保留后续作者编辑与受保护引用。需拆分/合并拓扑的情况转既有场景工作台。
- Assistant注册整理/采用操作及受可见性约束的结果读取，复用同一任务；页面一次授权、成组决定并排除例外、来源展开、窄屏、停止恢复和撤销。修复终态任务恢复触发父页面无限刷新的问题。
- 首页注意事项只消费当前资产/来源/协议指纹有效的分类；未分类进口候选仍是尚未整理，人工及其他来源候选不被后台整理吞掉。
- 三份提示词与两个输出契约；人审评测统计使用已有DatasetSplit，不把模型标签计入资格。`review_resolution_quality.QUALIFIED_RUNS`有意为空，普通语义自动采用尚未开放。

## 数据与真实模型证据
- 旧项目：原库 `ai_novel_engine`，项目 `64e1691c-07ef-47d7-8616-0935bb1fb3e9`。原255关系/69别名中8别名属于已合并/忽略对象；活跃整理范围316项（61别名+255关系）。
- 演示项目：原库 `ai_novel_acceptance_guimi`，项目 `937c86f1-a2c3-4db5-963d-f3181095f339`。活跃106项（11对象+37别名+58关系）；原45别名包含历史对象。58场景标记，31边界/27降级可交叠；原导入cancelled，不是正常完成质量基线。
- 仅在隔离副本执行：`ai_novel_review_resolution_old_20260911`、`ai_novel_review_resolution_demo_20260911`；迁移副本到当前head `20260912_web_search_consent`。为专用worker烟测取消了副本内旧任务；绝不应用到原库。
- 真实模型诊断及回执见 [VALIDATION.md](artifacts/VALIDATION.md)。已发现泛称/短期修饰、秘密身份、缺精确场景来源、结构化输出不合法等真实失败，并修正契约/提示词。
- 26–35章样本最初与开发章节分离，后用于诊断，现不再是未见保留集。场景样本2次调用返回待决定，但基于随后收紧前的契约，不能当当前自动采用验收。
- 原工作树venv含BGE模型依赖，隔离CI venv仅有CI extras；真实模型脚本通过当前worktree PYTHONPATH使用原venv。曾缺main guard导致多进程重复启动，已修复；早期结果不计完整验收。

## 工程验证
- `make test-ci TEST_WORKERS=2`最新执行：文档、密钥卫生、依赖审计、Ruff、部署268项通过；后端5278 passed /12 skipped，覆盖率85.34%。前端2469 passed/2 failed，均为产品文案从“需要决定”改为“待处理资料”的旧预期，已同步；只补跑失败所在前端阶段，不重复已通过后端。
- 补跑前端全量2471 passed /181文件；frontend lint和生产build/资源校验通过。最终 `make docs-check BASE_REF=origin/main` 与 `git diff --check` 通过。未重复运行已通过的后端/部署门禁。
- PostgreSQL专用库 `ai_novel_review_resolution_pg_test_20260911_2`：事务/CAS/幂等/组原子撤销9 passed，日志 `pg-checks-final.log`。
- 浏览器专用库 `ai_novel_review_resolution_e2e_20260911`：一次授权、选择例外、查看原文、采用、刷新恢复、390px流程1 passed（10.8s），日志 `browser-checks-final.log`。任务响应受控，真实领域事务另由PG测试覆盖；不称真人验收。
- 本地原始日志/脚本：`backend/.test-artifacts/review-resolution/`（忽略目录）；截图：`frontend-console/test-results/import-review-resolution-一次授权、成组选择、原文查看与窄屏刷新恢复/review-resolution-narrow.png`。

## 未完成与解除条件
- 两个项目未见冻结资料的三组对照（当前/确定性/完整）、足量逐项真人标签、作者实际决定数和耗时尚缺；无法宣称98%或50%达标。
- 需要真人参与上述验收；通过类别才可绑定prompt/profile/model版本开放资格。已用于调试样本须移出保留集。
- 尚未进行真实项目新入口启动、提交、合并、推送、部署，均保持用户原授权边界。
