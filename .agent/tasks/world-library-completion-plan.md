# 世界资料库补全计划

状态：计划已编制并完成文档门禁；功能补全尚未开始。

## 目标与验收

依据审查报告形成可执行补全计划，覆盖四项剩余缺口、交付依赖、接口/schema 风险、作者体验及验收。正式计划位于 `docs/product/world-library-completion-plan.md`，此处只记录恢复信息。

## 上下文与授权

用户本轮要求“根据审查报告做补全计划”，本轮范围为规划与文档。保留 `codex/world-library-review-fixes` 上轮未提交修复，不改业务代码或真实数据，无新的提交/推送/合并/部署授权。原工作区未改动，未启用子代理。

## 决策与发现

- 沿用原先已确认的作者目录、局部编辑、定向复核范围。
- R0 固定修复基线；R1 世界状态与长期决定；R2 历史与聊天恢复；R3 来源定位/失效后再跨域语义检查；R4 长文大库和完整体验验收。允许独立加载优化提前，不设置并行 Agent。
- 当前代码实证：Generate 重建 seed、保存服务只校验父类型；历史服务有分页/搜索但聊天同步；影响来源只有 Page/Entity 可跳转，Story/地图版本使用 status，正文聚合为章；worldIsland 仍加载全量页面/稿件。
- 地图后端当前在 World，不存在独立 modules/map；计划按实际所有权编排。
- 工程、真实模型质量、作者喜好与部署分开验收；上轮已有测试不替代后续改动验证。

## 验证

开始 `make docs-check` 与收尾 `make docs-check BASE_REF=origin/main`、`git diff --check` 均通过；纯计划不运行业务测试。未刷新远端，本地 origin/main 基线为 a8de5aa9e98301943e4311aa1e5a356dd1fb177e，实施前重验。

## 恢复快照

阅读正式计划和审查报告，核对 Git/WIP 与授权。用户下一步若要求执行，先固定审查修复基线，再做 R1 完整链；若要求调整计划，只改对应文档。不要把本任务笔记解释为提交、合并或真实模型调用授权。
