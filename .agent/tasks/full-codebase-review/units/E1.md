# E1 槽位报告 — R14 主单元：backend/tests + 根 conftest + backend/evals（199 路径）

日期：2026-09-11。基线：`main` HEAD `e7d0b8d5b`（与 baseline.md §1 一致）。本报告只读审查，唯一写入即本文件。

## 审查方法声明

199 文件不逐行全文精读，采用系统性方法：

1. **结构扫描（全部 199 文件）**：行数/测试函数数分布（`grep -c "def test_"`）、marker（e2e/real_llm/external_data）、fixture 与 mock 面、时间/随机/环境隔离模式（rg 模式扫描）。
2. **全文精读（关键对象）**：`backend/conftest.py`、`backend/tests/AGENTS.md`、`test_performance_probe.py`、`test_world_testonly_route.py`、`tests/support/inventory.py`、`tests/support/http.py`、`tests/utils.py`、e2e `config.py`/`conftest.py`（结构+守卫段）、`account_project_preferences/conftest.py`、`evals/__init__.py`、`evals/datasets/README.md`、`evals/tests/test_rp_context.py`（契约段）。
3. **抽读关键断言段**：`test_test_harness.py`（守卫段）、`test_outline_api.py`（patch 段）、`tests/modules/outline/*`（patch 段）、`evals/tests/test_generation_runners.py`/`test_project_executor.py`（mock 面）、`test_conflict_checks_real_llm.py`（失实断言段，writing 模块文件、D4 主审，本次仅取证 D3b-1）。
4. **横查工具**：pytest 配置（`backend/pyproject.toml` testpaths/markers/timeout）、Makefile 全部 eval-* 目标与 CI workflow 消费链、`git log -L` 追溯 testpaths 收窄历史、handler/路由注册面与测试引用对照。
5. **禁令遵守**：未运行任何测试/构建/make；无网络；未读 `.env` 与密钥值。注意：本次审查中我自身使用的 `rg -r` 标志曾造成显示替换（`-r` 是 replace），发现后已用无 `-r` 的精确检索复核受影响结论（story/legal/chaos 覆盖判断均以复核后结果为准）。

## 覆盖行

格式：`path,审查状态,入口/消费者(可空),发现ID或无发现理由`

```csv
backend/conftest.py,已读(全文),全部 fast 层测试的 fixture 源;E2E 另有独立 conftest,无发现:DI/override 每例重置、SQLite 事务回滚与 UUID-as-CHAR(32) 适配齐备
backend/evals/__init__.py,已读(全文),导出 DatasetCase/DatasetManifest/EvalResult,无发现:developer-only 语义评测工具包声明清晰
backend/evals/ask_world.py,已读(结构+抽读),make eval-ask-world 与 CI backend-ci.yml:71 直接调用,无发现:CI 唯一活跃 eval 入口
backend/evals/cache.py,已读(结构),generation/judge/report 等 5 模块消费,无发现
backend/evals/cli.py,已读(结构+抽读),Makefile eval-* 各目标 `python -m evals.cli`,E1-2:子命令经裸 python 无锁定运行器(F5-9 同型,此处记 evals 侧事实)
backend/evals/codex_executor.py,已读(结构),generation/review 消费;本地 Codex CLI 固定模型,无发现:不读项目 Key、不回退未批准模型
backend/evals/corpus.py,已读(结构),corpus-manifest/prepare-rag 消费,无发现:仅记录哈希不复制源文
backend/evals/datasets/README.md,已读(全文),数据集目录边界声明,无发现:本地语料路径为本机用户显式指定,不入库
backend/evals/datasets/baselines/ask-world-model-probes-v1.jsonl,已读(性质),ask-world 评测基线(5.7KB 哈希化记录),无发现
backend/evals/datasets/baselines/ask-world-v1.jsonl,已读(性质),ask-world 评测基线(34KB),无发现
backend/evals/datasets/baselines/fast-v1.jsonl,已读(性质),fast 门槛基线(3.2KB),无发现
backend/evals/datasets/baselines/rp-context-v1.jsonl,已读(性质),rp-context 对照账本(570B),无发现
backend/evals/datasets/baselines/rp-long-memory-review-calibration-v1.jsonl,已读(性质),人工评审校准基线,无发现
backend/evals/datasets/baselines/rp-long-memory-v1.jsonl,已读(性质),RP 长期记忆基线 v1,无发现
backend/evals/datasets/baselines/rp-long-memory-v2-holdout.jsonl,已读(性质),RP 长期记忆 v2 holdout,无发现
backend/evals/datasets/baselines/rp-long-memory-v2.jsonl,已读(性质),RP 长期记忆基线 v2,无发现
backend/evals/datasets/baselines/rp-long-memory-v3-production-holdout.jsonl,已读(性质),v3 生产 holdout,无发现
backend/evals/datasets/manifests/fast-v1.json,已读(性质),fast 套件清单(哈希清单),无发现
backend/evals/generation.py,已读(结构+抽读),cli generate/judge/pilot 共用引擎,无发现:场景覆盖校验/缓存 provenance 断言齐备
backend/evals/metrics.py,已读(结构),7 个 evals 模块消费的确定性指标,无发现
backend/evals/project_executor.py,已读(结构+抽读),runners 被评系统执行器,无发现:novel-scoped 客户端有测试
backend/evals/qc.py,已读(结构),eval-qc 子命令,无发现
backend/evals/ragas_adapter.py,已读(结构),ragas 文本收集指标适配(隔离本地评估器),无发现
backend/evals/readiness.py,已读(结构),baseline-check 子命令,无发现
backend/evals/report.py,已读(结构),eval-report 子命令,无发现
backend/evals/review.py,已读(结构+抽读),review-export/import/model 三子命令,无发现:datetime.now 仅记录评审时间
backend/evals/review_resolution.py,已读(结构),评审分歧裁决,无发现
backend/evals/rp_context.py,已读(结构+抽读),evals/tests/test_rp_context.py 契约对象,无发现:仅存 hash/SourceRangeRef/rubric
backend/evals/rp_long_memory.py,已读(结构),eval-rp-long-memory compile 门禁+测试,无发现(random 已按 seed 固定);结构观察:3947 行单文件见共享事实
backend/evals/runners/__init__.py,已读(结构),runners 包聚合,无发现
backend/evals/runners/context_planner.py,已读(结构),eval-context-planner 消费,无发现
backend/evals/runners/outline.py,已读(结构),eval-run outline runner,无发现
backend/evals/runners/rag.py,已读(结构),eval-rag runner,无发现
backend/evals/runners/scene.py,已读(结构),eval-run scene runner,无发现
backend/evals/runners/world.py,已读(结构),eval-run world runner,无发现
backend/evals/scene_gold.py,已读(结构),scene-gold-repair 子命令,无发现
backend/evals/schemas.py,已读(结构),evals 内 20 处消费的 schema,无发现
backend/evals/tests/__init__.py,已读(全文),包标记,无发现
backend/evals/tests/test_ask_world.py,已读(结构),ask_world 契约测试(13 测试),E1-2:不在任何自动门禁收集范围
backend/evals/tests/test_corpus_metrics.py,已读(结构),corpus/metrics 测试,E1-2:同上
backend/evals/tests/test_generation_runners.py,已读(结构+抽读),generate/judge 2430 行测试;假 executor 无网络,E1-2:同上
backend/evals/tests/test_project_executor.py,已读(结构+抽读),novel-scoped 客户端与评审模型策略,E1-2:同上
backend/evals/tests/test_review.py,已读(结构),评审导出/导入一致性,E1-2:同上
backend/evals/tests/test_review_resolution.py,已读(结构),裁决逻辑,E1-2:同上
backend/evals/tests/test_rp_context.py,已读(结构+抽读),hash-only 契约+盲评汇总;testing-guide.md:64 显式命令,E1-2:仅手动命令层
backend/evals/tests/test_rp_long_memory.py,已读(结构),compile 门禁契约(29 测试),E1-2:同上
backend/evals/tests/test_scene_gold.py,已读(结构),scene-gold 契约,E1-2:同上
backend/evals/tests/test_schemas_qc.py,已读(结构),schema+QC,E1-2:同上
backend/tests/AGENTS.md,已读(全文),tests 目录局部规则,无发现:与根 conftest/e2e 门禁一致
backend/tests/CLAUDE.md,已读(全文),`@AGENTS.md` 导入,无发现
backend/tests/__init__.py,已读(全文),包标记,无发现
backend/tests/account_project_preferences/__init__.py,已读(全文),包标记,E1-1
backend/tests/account_project_preferences/conftest.py,已读(全文),factory 别名 fixture,E1-1
backend/tests/account_project_preferences/test_api.py,已读(结构),13 测试;API 层偏好/连接合同,E1-1:不进 fast 层/CI
backend/tests/account_project_preferences/test_effective.py,已读(全文),仅 docstring 空壳,E1-7:可删空壳文件
backend/tests/account_project_preferences/test_image_connection.py,已读(结构),1 测试;图像连接,E1-1
backend/tests/account_project_preferences/test_llm_connections.py,已读(结构),14 测试;唯一被 Makefile:71(test-real-kimi)点名的文件,E1-1
backend/tests/account_project_preferences/test_owner_isolation.py,已读(结构),3 测试;owner 隔离,E1-1:安全相邻覆盖不进门禁
backend/tests/account_project_preferences/test_repos.py,已读(结构),7 测试;仓储层,E1-1
backend/tests/account_project_preferences/test_services.py,已读(结构),15 测试;含 effective 合并逻辑,E1-1
backend/tests/chaos/core_paths_chaos.py,已读(结构+抽读),自跑脚本 `python -m tests.chaos.core_paths_chaos`(需本地 8000 服务);docs/superpowers 报告引用,E1-9:非 pytest、不被收集、放置位存疑
backend/tests/conftest.py,已读(结构),空/不存在实质内容,无发现
backend/tests/e2e/__init__.py,已读(全文),包标记,无发现
backend/tests/e2e/config.py,已读(全文),require_e2e_database_url fail-closed 门禁,无发现:专用库标记校验与文档一致
backend/tests/e2e/conftest.py,已读(结构+抽读),E2E fixture 源,无发现:collection 强制 e2e marker、隔离全局 DatabaseManager
backend/tests/e2e/samples/lotm_chapter_1.txt,已读(性质+首部),seed_data.py 消费,E1-9:内容为《诡秘之主》第一章原文节选入库(8.9KB)
backend/tests/e2e/samples/lotm_chapters_1_2_3.txt,已读(性质+首部),seed_data.py 消费,E1-9:同上(27.6KB)
backend/tests/e2e/samples/lotm_empty.txt,已读(性质),seed_data.py 消费,无发现:0 字节空文件合法用例
backend/tests/e2e/seed_data.py,已读(结构),E2E/手动验收种子,无发现
backend/tests/e2e/test_00_fresh_migrations.py,已读(结构),PG critical 子集成员,无发现
backend/tests/e2e/test_01_import_flow.py,已读(结构),E2E 导入全流程,无发现
backend/tests/e2e/test_02_world.py,已读(结构),E2E 世界域(21 测试),无发现
backend/tests/e2e/test_08_rag.py,已读(结构),E2E RAG,无发现
backend/tests/e2e/test_09_context.py,已读(结构),E2E 上下文,无发现
backend/tests/e2e/test_11_writing.py,已读(结构),E2E 正文,无发现
backend/tests/e2e/test_20_security.py,已读(结构),E2E 安全(注入/越权/XSS),无发现
backend/tests/e2e/test_21_pagination.py,已读(结构),E2E 分页,无发现
backend/tests/e2e/test_22_novel_evidence.py,已读(结构),E2E novel 证据,无发现
backend/tests/e2e/test_assistant_concurrency.py,已读(结构+抽读),E2E 助手并发(真实 worker),无发现;注意 :224 sleep(1.1) 依赖既有 1s 退避,串行层可接受的 flaky 候选(共享事实)
backend/tests/e2e/test_assistant_migration.py,已读(结构),助手迁移合同,无发现
backend/tests/e2e/test_assistant_review_runtime.py,已读(结构+抽读),双 lease 共享预算;patch.object 均带 autospec,无发现
backend/tests/e2e/test_context_retrieval_trace_queries.py,已读(结构),PG critical 子集;陈旧 trace 查询,无发现
backend/tests/e2e/test_context_terminal_concurrency.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_db_response_transaction_boundary.py,已读(结构),事务边界,无发现
backend/tests/e2e/test_focused_completion_concurrency.py,已读(结构),两连接字段漂移/撤销门禁,无发现
backend/tests/e2e/test_import_upload_commit_visibility.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_interaction_generation_concurrency.py,已读(结构+抽读),1012 行并发;sleep(0.05) 轮询在 e2e 串行层可接受,无发现
backend/tests/e2e/test_interaction_long_context_real_kimi.py,已读(结构),付费长上下文校准(real_llm 门禁),无发现:缺 flag 即 collection 前失败
backend/tests/e2e/test_outline_generation.py,已读(结构),E2E 大纲生成,无发现
backend/tests/e2e/test_project_author_tasks.py,已读(结构+抽读),项目删除竞态;patch 无 autospec 处均为 HTTP PATCH 断言语境,无发现
backend/tests/e2e/test_project_task_gate_concurrency.py,已读(结构),PG critical 子集;项目删除 vs atlas 上传/清理竞态,无发现
backend/tests/e2e/test_review_resolution_concurrency.py,已读(结构),导入裁决并发,无发现
backend/tests/e2e/test_rp_agreements.py,已读(结构),RP 长期约定兼容,无发现
backend/tests/e2e/test_rp_source_versions.py,已读(结构),PG critical 子集;source revision 并发唯一性,无发现
backend/tests/e2e/test_scene_chapter_link_concurrency.py,已读(结构),Scene-章节链接并发,无发现
backend/tests/e2e/test_scene_memory_checkpoint_concurrency.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_smart_dedup_group_savepoint.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_task_coalescing_concurrency.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_unified_map_concurrency.py,已读(结构),地图 CAS/生命周期,无发现
backend/tests/e2e/test_world_alias_projection.py,已读(结构),>1000 对象共享读路径,无发现
backend/tests/e2e/test_world_authority_constraints.py,已读(结构),世界权威约束,无发现
backend/tests/e2e/test_world_cocreation_checkpoint_concurrency.py,已读(结构),共创检查点并发,无发现
backend/tests/e2e/test_world_generated_columns.py,已读(结构),生成列行为,无发现
backend/tests/e2e/test_world_object_image_quota_concurrency.py,已读(结构),图片配额并发,无发现
backend/tests/e2e/test_world_validation_concurrency.py,已读(结构),世界校验并发,无发现
backend/tests/e2e/test_writing_route_commit_visibility.py,已读(结构),PG critical 子集,无发现
backend/tests/e2e/test_writing_version_concurrency.py,已读(结构),PG critical 子集,无发现
backend/tests/fixtures/synthetic_ten_chapters.txt,已读(性质+首部),合成十章样本(原创内容),无发现
backend/tests/integration/test_authoring_lifecycle.py,已读(结构),testing-guide 关键集成 8:导入→生成→采用→发布,无发现
backend/tests/integration/test_context_loaders.py,已读(结构+抽读),28 测试;1 处 mock.patch 带 autospec,无发现
backend/tests/integration/test_memory_service.py,已读(结构+抽读),6 处 patch 全带 autospec,无发现
backend/tests/integration/test_novel_id_api_params.py,已读(结构),novel_id 参数合同,无发现
backend/tests/integration/test_novel_id_isolation.py,已读(结构),跨项目隔离关键测试,无发现
backend/tests/integration/test_security.py,已读(结构),XSS/注入/超大输入,无发现
backend/tests/modules/outline/generation/test_context_builder.py,已读(结构+抽读),13 patch 全带 autospec(fixture 级隔离 world facade),无发现
backend/tests/modules/outline/generation/test_parser.py,已读(结构),解析器测试,无发现
backend/tests/modules/outline/generation/test_persister.py,已读(结构),持久化测试,无发现
backend/tests/modules/outline/test_generator.py,已读(结构+抽读),3 patch 全带 autospec,无发现
backend/tests/prompt_contracts/test_prompt_contracts.py,已读(结构),Prompt 清单合同(docs-check 配套),无发现
backend/tests/support/__init__.py,已读(全文),包标记,无发现
backend/tests/support/assistant_browser_app.py,已读(结构),Playwright 助手测试 test-only harness(独立 PG 门禁),无发现:sleep 为 harness 就绪等待
backend/tests/support/http.py,已读(全文),XhrAsyncClient:非 GET 自动带 CSRF marker,无发现:与生产 CSRF 校验镜像
backend/tests/support/inventory.py,已读(全文),AST/源码缓存清单(跳过点目录/__pycache__),无发现;E1-3 建议的修复素材
backend/tests/test_api.py,已读(结构+抽读),40 测试;根级 API 冒烟(记忆/公共路由等),无发现
backend/tests/test_outline_api.py,已读(结构+抽读),API+事务混合测试;5 patch 全带 autospec,无发现
backend/tests/test_world_testonly_route.py,已读(全文),`/api/world/_test/*` 环境门禁与 novel_id 校验,无发现:APP_ENV=test 专用生产面被正向测试
backend/tests/unit/__init__.py,已读(全文),包标记,无发现
backend/tests/unit/test_account_llm_migration.py,已读(结构),账户 LLM 迁移守卫,无发现
backend/tests/unit/test_active_project_route_closure.py,已读(结构),活动项目路由闭合(404 面),无发现
backend/tests/unit/test_alembic_vector_indexes.py,已读(结构),向量索引迁移合同,无发现
backend/tests/unit/test_app_logging.py,已读(结构),应用日志,无发现
backend/tests/unit/test_app_timing_middleware.py,已读(结构+抽读),21 测试;计时中间件,无发现
backend/tests/unit/test_architecture_docs.py,已读(结构),架构文档清单合同,无发现
backend/tests/unit/test_asset_state_simplification_migration.py,已读(结构),资产状态迁移守卫,无发现
backend/tests/unit/test_base.py,已读(结构),ORM base,无发现
backend/tests/unit/test_character_facade.py,已读(结构),角色 facade(23 测试),无发现
backend/tests/unit/test_ci_change_selection.py,已读(结构),CI 变更分类脚本合同,无发现
backend/tests/unit/test_compiled_context.py,已读(结构),编译上下文序列化,无发现
backend/tests/unit/test_config.py,已读(结构+抽读),85 测试;env/.env 解析(monkeypatch 隔离),无发现
backend/tests/unit/test_constants.py,已读(结构+抽读),shared/constants 全量断言,E1-7:F1-4 死常量清理须配对收缩
backend/tests/unit/test_constraint_engine.py,已读(结构),约束引擎,无发现
backend/tests/unit/test_container.py,已读(结构),DI 容器,无发现
backend/tests/unit/test_context.py,已读(结构),93 测试/2514 行;evidence context 主体,无发现;成本观察见共享事实
backend/tests/unit/test_context_tier_rendering.py,已读(结构),分层渲染,无发现
backend/tests/unit/test_crud.py,已读(结构),CrudService 基础,无发现
backend/tests/unit/test_database.py,已读(结构+抽读),38 测试;@patch 全带 autospec,无发现
backend/tests/unit/test_db_response_transaction_boundary.py,已读(结构),SQLite 侧事务边界镜像,无发现
backend/tests/unit/test_deep_import_settings.py,已读(结构),deep import 设置,无发现
backend/tests/unit/test_dependencies.py,已读(结构),core dependencies,无发现
backend/tests/unit/test_dev_schema_guard.py,已读(结构),开发库 schema 守卫,无发现
backend/tests/unit/test_dev_server.py,已读(结构+抽读),dev server 合同;patch 带 autospec,无发现
backend/tests/unit/test_dev_stack_entrypoint.py,已读(结构),start.sh 合同(21 测试),无发现
backend/tests/unit/test_doctor.py,已读(结构),doctor 诊断,无发现
backend/tests/unit/test_domain_errors.py,已读(结构),DomainError 映射,无发现
backend/tests/unit/test_entity_context_filter.py,已读(结构),实体上下文过滤,无发现
backend/tests/unit/test_entity_facade.py,已读(结构+抽读),14 patch 全带 autospec,无发现
backend/tests/unit/test_enums.py,已读(结构+抽读),shared/enums 17 枚举全量断言,E1-7:F1-1(14 死枚举)清理须配对
backend/tests/unit/test_event_facade.py,已读(结构),事件 facade,无发现
backend/tests/unit/test_evidence_fusion_contract.py,已读(结构),facade/contracts 访问守卫(testing-guide 载明),无发现
backend/tests/unit/test_facade_public_api.py,已读(结构),facade 公共面守卫,无发现
backend/tests/unit/test_http_rate_limit.py,已读(结构),限流,无发现
backend/tests/unit/test_imports_extra.py,已读(结构),77 测试/1362 行;imports 兜底面,无发现;归属横查,领域细节 D5a/D5b
backend/tests/unit/test_infra_embedding.py,已读(结构+抽读),81 测试;embedding 基建(mock 化),无发现
backend/tests/unit/test_infra_llm.py,已读(结构),LLM 客户端基建合同(retry/流/schema),无发现
backend/tests/unit/test_infra_tasks.py,已读(结构),63 测试/2162 行;任务基建,无发现
backend/tests/unit/test_llm_max_tokens_migration.py,已读(结构),max_tokens 迁移守卫,无发现
backend/tests/unit/test_logging_context.py,已读(结构+抽读),patch.object 带 autospec,无发现
backend/tests/unit/test_manual_llm_context_gate.py,已读(结构),手动 LLM 上下文门禁,无发现
backend/tests/unit/test_map_atlas_migration_guard.py,已读(结构),地图迁移守卫,无发现
backend/tests/unit/test_memory_extra.py,已读(结构),27 测试;continuity 兜底面,无发现
backend/tests/unit/test_non_deep_llm_token_inheritance.py,已读(结构),token 继承,无发现
backend/tests/unit/test_novel_evidence_migration.py,已读(结构),novel 证据迁移守卫,无发现
backend/tests/unit/test_novel_scoped_llm_usage.py,已读(结构),novel-scoped LLM 用量,无发现
backend/tests/unit/test_performance_probe.py,已读(全文),performance_probe 安全合同,无新发现:第 2 例为本机已知环境失败(baseline §5,根因在 tools/performance_probe.configure 的读取路径与 tmp 注入面不匹配,归 F6 工具文件,建议与 F4-1 同批看)
backend/tests/unit/test_project.py,已读(结构+抽读),31 测试;9 @patch 全带 autospec,无发现
backend/tests/unit/test_rag_extra.py,已读(结构),62 测试;RAG 兜底面,无发现
backend/tests/unit/test_rag_facade_extra.py,已读(结构),RAG facade 兜底面,无发现
backend/tests/unit/test_rag_services.py,已读(结构+抽读),49 测试;8 autospec,无发现
backend/tests/unit/test_relation_alias_kinds_migration.py,已读(结构),别名关系迁移守卫,无发现
backend/tests/unit/test_repository_security_automation.py,已读(结构),仓储安全自动化合同,无发现
backend/tests/unit/test_retired_owner_paths.py,已读(结构),退场前缀 404 守卫(testing-guide 载明),无发现:属守卫非失实
backend/tests/unit/test_run_worker.py,已读(结构+抽读),20 测试;worker 入口;27 autospec,无发现
backend/tests/unit/test_scene_spans_migration.py,已读(结构),scene_spans 迁移守卫,无发现
backend/tests/unit/test_secret_hygiene.py,已读(结构),secret 卫生门禁,无发现
backend/tests/unit/test_shared_types.py,已读(结构+抽读),shared/types 别名断言,E1-7:F1-5(整文件死)清理须配对
backend/tests/unit/test_shared_utils.py,已读(结构+抽读),parse_uuid/is_valid_uuid/parse_llm_json,E1-7:F1-2 死符号清理须配对
backend/tests/unit/test_squashed_schema_migrations.py,已读(结构),squash 后 schema 合同,无发现
backend/tests/unit/test_story_outline_migration.py,已读(结构),story outline 迁移守卫,无发现
backend/tests/unit/test_task_novel_id_migration.py,已读(结构),任务 novel_id 迁移守卫,无发现
backend/tests/unit/test_task_owner_migration.py,已读(结构),任务 owner 迁移守卫,无发现
backend/tests/unit/test_task_runtime.py,已读(结构),任务运行时,无发现
backend/tests/unit/test_test_harness.py,已读(结构+抽读),测试基建自守卫(autospec/async fixture/conftest 卫生/marker 严格性),无发现;守卫盲区见 E1-8
backend/tests/unit/test_test_infrastructure.py,已读(结构),根 fixture 行为自检(savepoint 回滚等),无发现
backend/tests/unit/test_token_estimation.py,已读(结构),token 估算,无发现
backend/tests/unit/test_world_extra.py,已读(结构+抽读),110 测试/1769 行;4 @patch 全带 autospec,E1-7 相关:大量断言与 D2 组死代码候选的状态面,清理时配对核对
backend/tests/unit/test_world_services_character_and_relation.py,已读(结构),60 测试;角色/关系服务,无发现
backend/tests/unit/test_world_services_revision_event_helpers.py,已读(结构),45 测试;revision/event helper,无发现
backend/tests/unit/test_writing.py,已读(结构+抽读),59 测试;7 autospec,无发现
backend/tests/utils.py,已读(全文),跨模块测试工厂(_create_project/_create_entity),无发现
```

## 发现

### E1-1 | P1 | `backend/tests/account_project_preferences/`（52 个测试）不进入任何自动门禁

- **位置/符号**：`backend/pyproject.toml` `testpaths`（未含 `tests/account_project_preferences`）；该目录 8 文件/52 个有效测试（`test_api.py` 13、`test_llm_connections.py` 14、`test_owner_isolation.py` 3、`test_repos.py` 7、`test_services.py` 15、`test_image_connection.py` 1）。
- **问题与触发**：`make test`/`make test-fast-coverage`/`make test-ci`/CI Backend quality 全部走 pytest 默认 testpaths，该目录永远不被收集；唯一被点名的单测 `test_llm_connections.py::test_balance_failure_is_auxiliary_and_does_not_disconnect` 仅挂在付费门禁 `make test-real-kimi`（Makefile:71）。结果：testing-guide.md:43-44 声称的“账户连接/全局偏好与项目偏好/有效组合共享覆盖面”在任何常规验证中都不执行，owner 隔离与偏好合同回归靠零。
- **调用链证据**：`backend/pyproject.toml:107-117` testpaths 仅列 modules/infrastructure/tests/{unit,integration,modules}/prompt_contracts + 3 个根文件；`git log -L '/testpaths/,+12:backend/pyproject.toml'` 显示收窄发生在 `41412c2ec`（"test: separate fast, E2E, and manual validation layers"，原 testpaths 为整个 `tests`）；`.github/workflows/backend-ci.yml` 后端质量 job 只跑 `make lint`/`test-deploy`/`test-fast-coverage`；`rg -n "account_project_preferences" Makefile .github/workflows` 仅 Makefile:71 一处。
- **现有契约**：testing-guide.md:43-44（覆盖面声明）；baseline.md §5 `make test` 计数（5288 passed，与不含该目录一致）。
- **最小方案**：`backend/pyproject.toml` testpaths 增加 `"tests/account_project_preferences"`；如该目录有 SQLite 不兼容用例，逐例加显式层 marker 而非整目录排除。同步 testing-guide.md 措辞。
- **预期收益**：52 个测试恢复门禁执行；owner 隔离/偏好/effective 组合回归回到 fast 层（估算 +52 例，fast 层耗时增幅小，均为 SQLite 单测）。
- **风险**：可能暴露该目录中已漂移失败的用例（首次接入门禁需先确认全绿）；个别用例若依赖已删除行为需按 D1 结论修。
- **依赖**：无；可与 E1-2 同批（均为 testpaths/收集面调整）。
- **验证命令/断言**：`cd backend && uv run --locked --extra ci -- pytest tests/account_project_preferences -q`（先手动确认 52 通过）；`make test` 后收集数较基线 +52；`make test-fast-coverage TEST_WORKERS=2` 通过。
- **回滚**：还原 pyproject.toml 一行。
- **裁定**：实施候选（验证基建缺口，非功能缺陷）。
- **优先级**：P1（对应 testing-guide“Missing module-level basic tests”同级：等价于门禁缺失；方案零风险可回滚）。

### E1-2 | P2 | `backend/evals/tests/`（124 测试）无自动门禁；`make eval-fast` 绕过锁定运行器

- **位置/符号**：`backend/evals/tests/`（10 文件/124 测试，全确定性、无网络、无付费调用）；Makefile `eval-fast` 目标。
- **问题与触发**：evals/tests 不在 fast 层 testpaths，也不在 CI；唯一入口 `make eval-fast` 为手动目标，且命令是裸 `pytest evals/tests -q`——没有 `$(BACKEND_LOCKED_CI_RUN)`（与 F5-9“eval 目标绕过锁定运行器”同型）也没有 fast 层超时。evals 生产代码（~1.5 万行）的回归只靠两处窄口径：CI `make eval-ask-world`（backend-ci.yml:71，仅 world ask_world API 测试 + evals.ask_world 主路径）与手动 eval-fast。
- **调用链证据**：`backend/pyproject.toml:107-117`（无 evals）；`.github/workflows/backend-ci.yml:71`（唯一 CI eval 调用）；Makefile eval-fast 定义（裸 pytest）；evals/tests 均以假 executor/假客户端运行（test_generation_runners.py 用 `CodexExecutionMeta`+本地假类，无 subprocess/网络）。
- **现有契约**：testing-guide.md:64 仅给手动命令 `uv --directory backend run pytest -q evals/tests/test_rp_context.py`；审计结论“evals 与 Makefile eval-* 为 CI 活跃调用”（见历史候选复核 2，需修正）。
- **最小方案**：两步——(a) eval-fast 命令加 `$(BACKEND_LOCKED_CI_RUN)` 与 `--timeout=$(FAST_TEST_TIMEOUT_SECONDS)`（与 F5-9 合并实施）；(b) 评估把 `evals/tests` 纳入 fast 层 testpaths（124 个确定性测试，依赖在 `ci` extra 内已有）或至少加入 `make test-ci` 链。若产品上坚持 evals 独立分层，则在 testing-guide 载明“evals/tests 不在任何自动门禁”这一事实。
- **预期收益**：evals 工具链 124 测试从“可能数月不跑”变为门禁覆盖；evals 生产代码改动（如 rp_long_memory 3947 行文件重构）有回归保障。
- **风险**：fast 层耗时增加（evals/tests 体量中等）；若存在隐藏失败需首跑确认。
- **依赖**：F5-9（同一 Makefile 批次）。
- **验证命令/断言**：`cd backend && uv run --locked --extra ci -- pytest evals/tests -q`（全绿）；`make eval-fast` 使用锁定运行器（`make -n eval-fast` 断言含 `uv run --locked --extra ci`）；纳入 fast 层后 `make test` 收集数 +124。
- **回滚**：还原 Makefile/pyproject 对应行。
- **裁定**：实施候选。
- **优先级**：P2。

### E1-3 | P2 | F4-1 修复落点精化：`test_identity` 应复用 `tests.support.inventory.production_python_files()`

- **位置/符号**：`backend/infrastructure/tasks/test_identity.py:15,26`（`BACKEND_ROOT = Path(__file__).resolve().parents[2]`；`BACKEND_ROOT.rglob("*.py")`）。文件归 F2（infrastructure 测试），本条为 F4-1 的实施增量，不重复立案。
- **问题与触发**：`rglob` 不跳过隐藏目录，`backend/.venv`（约 1.4 万文件）全量 `ast.parse` → fast 层 per-test 120s 超时判负；单独跑 1623.9s（baseline §6）。
- **调用链证据**：`backend/tests/support/inventory.py:20-31` 的 `repository_python_files()` 用 `os.walk` 显式跳过点目录与 `__pycache__` 并做进程级缓存；`production_python_files()`（:47-55）在其上再排除 `tests` 路径段/conftest/`test_*` 文件——与该测试想要的“生产 enqueue 调用面”语义完全一致。`test_test_harness.py:153` 的 autospec 守卫等已在用同一清单，说明该基建是既定模式。
- **现有契约**：F4-1（findings-ledger P1）；baseline.md §6。
- **最小方案**：将 `for source_path in BACKEND_ROOT.rglob("*.py")` 的自管遍历与 `"tests" in parts` 过滤替换为 `from tests.support.inventory import production_python_files`，删除本地 `BACKEND_ROOT` 常量；或最小差异版仅加 `if ".venv" in source_path.parts: continue`。推荐前者（复用现有标准实现，缓存还加速）。
- **预期收益**：该测试从 27 分钟/超时判负回到秒级；fast 层基线恢复全绿（剩 performance_probe 已知环境项）。
- **风险**：极低；注意 `production_python_files()` 同样排除 evals 生产文件名不含 test_ 的文件——本测试恰要扫描生产 enqueue 调用，evals 目录内如有 enqueue 调用会被排除。核对：evals 无 `enqueue_task(` 调用（`rg -n "enqueue_task" backend/evals` 为空），语义无损。
- **依赖**：无（独立小批）。
- **验证命令/断言**：`cd backend && uv run --locked --extra ci -- pytest infrastructure/tasks/test_identity.py -q`（应数秒内 8 passed）；`make test` 不再出现该失败；断言 `missing == []` 与 `direct_construction == []` 仍对生产源成立。
- **回滚**：还原测试文件。
- **裁定**：实施候选（F4-1 的落地形态）。
- **优先级**：P2（随 F4-1 定级 P1 的实施批次处理）。

### E1-4 | P2 | `/api/story` 路由组 HTTP 层零测试

- **位置/符号**：`backend/modules/story/api.py`（prefix `/api/story`，app/main.py:769 注册）：`GET /character-cards`、`GET /character-cards/{id}`、`GET /script-files/{id}`、`GET /scenes/{id}/story-context`、`GET /scenes/{id}/scripts`、`POST /tasks/character-card|reaction|script|one-click` 等。
- **问题与触发**：全仓测试（fast 层、`backend/tests`、story 模块测试、e2e）中无任何 `/api/story` HTTP 调用（`rg -ln "/api/story"` 于测试文件零命中）。story 服务/任务层有直接测试（test_story_service/test_story_tasks/envelope 合同），但 HTTP 边界的鉴权、422、novel_id 过滤、任务 202 语义无后端门禁；前端 Playwright（E3 域）不构成后端合同门禁。
- **调用链证据**：`rg -ln "/api/story" backend --glob '*.py' | grep test` → 空；`backend/modules/story/tests/` 六个测试文件均不使用 async_client（rg "async_client" 零命中）。
- **现有契约**：testing-guide“Per-Module Tests”三层要求（Repository/Service/API）——API 层缺口。
- **最小方案**：补一个 `story` 模块 API 合同测试文件（参照 `outline_state/tests/test_ai_api.py` 形制）：每端点 1 正常流 + 404/422/跨 novel_id 各 1；任务类端点沿用现有 enqueue mock 模式（带 autospec）。
- **预期收益**：9+ 公开端点获得 HTTP 合同回归；补齐三层测试结构的已知缺口。
- **风险**：低；如 D3 组审查中已知某端点行为与 schema 不符，先记功能缺陷再写断言，不以测试追认错误。
- **依赖**：D3a/D3b 结论（避免把已裁定的死路由写成合同）。
- **验证命令/断言**：`make test TESTS="modules/story/tests"`；新增文件断言各状态码与 novel_id 隔离。
- **回滚**：删除新增测试文件。
- **裁定**：实施候选（先与 D3 报告合并核对端点存留）。
- **优先级**：P2。

### E1-5 | P3 | `/api/legal/terms|privacy` 零测试

- **位置/符号**：`backend/modules/account/legal.py:29,40`。
- **问题与触发**：两个公开 HTML 端点无任何测试。静态内容、无数据访问，风险低。
- **最小方案**：并入 account 模块 API 测试（状态码 200 + content-type）。
- **收益/风险**：小；成本低于 30 分钟。
- **验证**：`make test TESTS="modules/account/tests"`。
- **回滚**：删测试。
- **裁定**：实施候选（顺手批）。
- **优先级**：P3。

### E1-6 | P3 | 失实测试普查：确认 D3b-1；`world_entity_fusion_suggestions` handler 包装层无直接测试

- **位置/符号**：(a) `backend/modules/writing/tests/test_conflict_checks_real_llm.py:209,211,329` 断言 `continuity_location_mismatch`；(b) `backend/modules/world/tasks.py:438` `handle_world_entity_fusion_suggestions`（confirmation 必需校验、novel_id 校验、进度上报、`prepare_confirmed_ai_action` 编排）。
- **问题与触发**：(a) 全仓检索 `continuity_location_mismatch` 仅命中该测试文件，生产（`conflict_ai.py`/`services.py` 只产 `forbidden_present`/`required_missing`/`outline_scene` 等）无此 kind 产出点——即 D3b-1/D4-1 已记事实的本次独立复核成立：该 real-LLM 验收跑起来必然失败于规则层。(b) 该 handler 的服务内部逻辑有测试（`test_entity_fusion.py` 打 evidence/服务函数），但 handler 包装（错误路径、confirmation 门禁、进度）与 `world/api.py:3105,3129` 的入队路由无 HTTP/handler 级测试；43 个已注册 handler 名单中这是间接覆盖最弱的一个（其余 42 个均有直接或同链路测试引用，见共享事实）。
- **调用链证据**：`rg -rn "continuity_location_mismatch" backend`（仅测试文件）；`services.py:1510-1663` kind 产出段；`rg -n "run_full_pipeline_phase|world_entity_fusion_suggestions"` 消费面核对。
- **现有契约**：D3b-1（dup 佐证）；README 对 world fusion 的“仅建议不改实体”承诺。
- **最小方案**：(a) 随 D3b-1 裁定（补实现或删 seam）同步修正/移除断言，不得先删断言。(b) 补 handler 级测试：缺 novel_id/缺 confirmation_id 的两个 ValueError 分支 + happy path 进度断言（mock facade，autospec）。
- **收益**：消除假验收承诺；handler 错误路径可回归。
- **风险**：低。
- **验证**：`make test TESTS="modules/world/tests modules/writing/tests"`。
- **回滚**：删测试/还原断言。
- **裁定**：(a) 归 D3b-1 批次；(b) 实施候选。
- **优先级**：P3。

### E1-7 | P3 | 死代码配对测试清单（清理批次必须同步处理）

- **位置/符号**：`backend/tests/unit/test_enums.py`（断言 shared/enums 全部 17 枚举，其中 14 个生产零引用=F1-1）；`test_shared_utils.py`（`parse_llm_json`/`is_valid_uuid`=F1-2 死符号）；`test_constants.py`（F1-4 死常量）；`test_shared_types.py`（F1-5 整文件死）；`backend/tests/account_project_preferences/test_effective.py`（仅 docstring 的空壳文件）；另 `test_world_extra.py` 内与 D2 组死代码候选相关的断言面在清理时需逐例核对。
- **问题与触发**：这些测试是“测试保障为假”模式的温和形态：给死代码持续绿灯，且是 F1 组删除批次的具体施工面。
- **现有契约**：findings-ledger F1-1/F1-2/F1-4/F1-5；审计规则“测试迁移不得删断言”的适用边界：随宿主符号一起删除不属于削弱断言。
- **最小方案**：各清理批次把上述测试文件列为同批改动项（删符号=删对应测试段）；`test_effective.py` 直接删除。
- **收益**：清理后测试套不残留“测空气”的维护面。
- **风险**：需逐符号重验零引用（裸符号名跨模块同名，按 X1-S3 方法用限定路径 grep）。
- **依赖**：F1 各批次的实施顺序。
- **验证**：`make test` + `make lint`。
- **回滚**：还原对应提交。
- **裁定**：随 F1 批次的施工清单。
- **优先级**：P3。

### E1-8 | P3 | autospec 守卫盲区：`monkeypatch.setattr` 无签名校验（567 处）

- **位置/符号**：守卫本体 `backend/tests/unit/test_test_harness.py:131-164`（AST 扫 `repository_test_python_files()`=全部测试文件，含 evals/tests）；盲区：`monkeypatch.setattr`（`backend/tests`+`backend/evals`+模块测试共 567 处）不在守卫范围，也没有 spec 参数。
- **问题与触发**：AGENTS.md autospec 硬规则针对 `@patch/mock.patch`，当前全后端测试面合规（按文件粒度 grep 核对 + 守卫存在双重确认，零违规）；但 `monkeypatch.setattr(module, "func", fake)` 与无 autospec patch 是同一失真类别（被替换函数签名漂移时测试不失败），未纳入守卫。
- **调用链证据**：全后端 `for f in $(rg -l "from unittest.mock import.*patch|mock\.patch|@patch\(" ...)` 按文件统计 patch 调用数 vs autospec 数，无缺口文件；`rg -c "monkeypatch\.setattr"` = 567。
- **现有契约**：testing-guide“Mock conventions”仅约束 patch。
- **最小方案**：不强制改 567 处；在 test_test_harness 增加一条软守卫或文档注记：新增 `monkeypatch.setattr` 替换生产可调用对象时优先 `@patch(autospec=True)` 或 `mock.patch.object(autospec=True)`。至少把盲区写进 testing-guide。
- **收益**：防同类失真面继续扩大。
- **风险**：无（仅守卫/文档）。
- **验证**：`make test TESTS="tests/unit/test_test_harness.py"`。
- **回滚**：还原守卫测试。
- **裁定**：实施候选（低优先）；结论“mock autospec 面当前合规”作为共享事实。
- **优先级**：P3。

### E1-9 | P3 | 仓库卫生：`tests/chaos` 放置位与《诡秘之主》原文样本入库

- **位置/符号**：(a) `backend/tests/chaos/core_paths_chaos.py`（485 行 requests 自跑脚本，`python -m tests.chaos.core_paths_chaos`，需本地 8000 服务；不被 pytest 收集）；(b) `backend/tests/e2e/samples/lotm_chapter_1.txt`（8.9KB）/`lotm_chapters_1_2_3.txt`（27.6KB），首部为《诡秘之主》第一章原文（“克莱恩·莫雷蒂……”），由 `seed_data.py` 消费。
- **问题与触发**：(a) 以 tests/ 命名和位置存在但不被任何层收集，易被误认为受门禁保护（对照 baseline 环境事实：它依赖本地运行的服务，属手动诊断）；docs/superpowers 报告引用它，非死代码。(b) 受版权保护的原文文本作为已提交测试夹具入库，与 D5a-1（同 IP 作为事实开发语料、prompt 硬编码）同主题，属法务/卫生注意项；0 字节 `lotm_empty.txt` 是合法空文件用例。
- **现有契约**：docs/superpowers/reports/2026-06-28-ideal-user-paths-chaos-*；testing-guide “external_data 为用户提供语料”的边界（本样本是提交入库的，不是用户运行时提供的）。
- **最小方案**：(a) 移至 `backend/scripts/tools` 或在文件头/testing-guide 标注“手动诊断脚本，不在 pytest 层”。(b) 由主 Agent 裁定：换成合成样本（`tests/fixtures/synthetic_ten_chapters.txt` 已有原创样板）或保留并记录许可依据；不作为优化批次阻塞项。
- **收益**：收集面/工具边界清晰；消除版权暴露。
- **风险**：(b) 涉及 e2e 种子替换需同步 seed_data 断言。
- **验证**：`make test --collect-only -q | rg chaos`（应无）；e2e 层手动确认种子替换后导入仍通过。
- **回滚**：git 还原。
- **裁定**：实施候选（(a)）/ 待产品裁定（(b)）。
- **优先级**：P3。

## 历史候选复核

1. **“evals 与 Makefile eval-* 为 CI 活跃调用”（audit“不建议简化”清单）→ 部分成立/需修正**。复核：CI（`.github/workflows/backend-ci.yml:71`）唯一消费 `make eval-ask-world`（且 continue-on-error，仅 main push）；其余 16 个 eval-* 目标全为本地手动工具；`backend/evals/tests`（124 测试）不在 CI 也不在 fast 层。“不删除 evals”的结论维持（全部模块有消费链，E1 逐模块 census 无死模块）；“CI 活跃调用”的范围修正为单目标，收敛事实记入 E1-2。
2. **A6-1（imports 旧签名 shim，唯一调用方 test_workflow.py）→ 仍成立**。复核当前树：`EntityExtractionPhaseRunner.run_full_pipeline_phase/run_stage_only`（workflow_entity_phase.py:30,290）与 `StructureAnalysisPhaseRunner` 同名方法（workflow_structure_phase.py:141,315）的生产调用为零；唯一消费者仍为 `backend/modules/imports/tests/test_workflow.py:340-385`；类本身有生产使用（workflow.py:125-126）。与 D5a 裁定一致；删除时同批迁移 test_workflow 相关段。
3. **A1-5（imports handler 测试专用 else 分支）→ 已由 D 组定案（仍成立，定位 imports 4 handler）**。本槽位核对：涉及文件为模块内测试（D5a/D5b 主审），无 backend/tests 侧同类新增；引用结论，不重复。
4. **A5-12（latch 近似）/A2-5（低收益）/A2-8 → Reviewer 剔除项**。本槽位无新增测试面证据推翻剔除结论；引用维持。
5. **F4-1（test_identity 扫 venv）→ 复核成立并精化修复方案（E1-3）**：baseline §6 的“测试文件内加 venv 排除”可升级为复用 `tests.support.inventory.production_python_files()`（现成、缓存、语义正合），验证命令见 E1-3。
6. **testing-guide.md:43-44（account_project_preferences 为共享覆盖面）→ 与现实不符（E1-1）**：文档声明该目录是覆盖主体，但门禁从不收集它；修 testpaths 后文档无需改，修文档不改 testpaths 则是掩盖缺口。
7. **“生产禁检测 Mock/测试替身走 DI”现状 → 合规**。全后端测试面 patch autospec 零违规且有 AST 守卫（test_harness.py:153）机器强制；生产代码未见 `isinstance(x, Mock)` 类守卫（抽查 conftest 消费面与 writing/imports 抽样）。历史担忧在当前树不成立。
8. **D3b-1/D4-1（continuity_location_mismatch 失实断言）→ 本次独立复核成立**（证据见 E1-6a），维持“按 D3b-1 裁定处理”。
9. **D6a-1/D6b-1/D8b-1/D5a-2/D5a-3（仅测试消费的死生产符号）→ 引用 D 组定案**。本槽位补充的同类“测试侧配对物”见 E1-7（shared 层 4 份 + 空壳 1 份）；领域侧配对测试随各 D 批次迁移。

## 共享事实（供 W3 与 P4 使用）

### 测试能力地图（分层/耗时分布）

- **fast 层**（`make test`，per-test 超时 120s，`testpaths`=modules/infrastructure/tests{unit,integration,modules,prompt_contracts}+3 根文件；`make test-fast-coverage` 加 `-n 2 --dist=loadscope` + 85% 覆盖率门）：基线 2 failed/5288 passed/12 skipped（226s，baseline §5）。两个已知失败：performance_probe（本机环境项）、test_identity（F4-1，修复见 E1-3）。
- **fast 层不收集**：`backend/tests/account_project_preferences`（E1-1）、`backend/evals/tests`（E1-2）、`backend/tests/chaos`（手动脚本）、`backend/tests/e2e`（by design，marker 排除）。
- **显式层**：`test-e2e`（RUN_E2E_TESTS=1+专用 PG，fail-closed URL 校验）；`test-postgresql-critical`（12 个指定文件，串行/超时 120/零重试）；`test-real-llm`/`test-real-kimi`/`test-interaction-long-context`/`test-map-atlas-live-image`/`test-manual`（付费/真实语料，缺 flag 在 collection 前失败）。
- **重量级 fast 测试文件**（行数×测试数，评估合并/拆分时参考）：test_context.py 2514/93、test_infra_tasks.py 2162/63、test_world_extra.py 1769/110、test_infra_embedding.py 1716/81、test_world_services_character_and_relation.py 1686/60、test_imports_extra.py 1362/77、test_rag_extra.py 1314/62、test_writing.py 1147/59、evals/rp_long_memory.py（生产）3947 行。`*_extra.py` 命名族（world/imports/rag/rag_facade/memory）是历史兜底倾倒面，P4 若做套件重组可按主题拆分，但无行为风险，不单独立项。
- **handler 覆盖对照**：唯一注册 handler 名 43 个（`@task_handler`，含字符串常量与 docs-check 的 47 口径差异为多行/重复注册计数）。测试直接引用任务名字符串的 31 个；其余 12 个逐一复核：9 个有间接覆盖（outline_analyze、rag_reannotate、rag_retry、scene_fusion_preview、story_outline_generate、import_review_resolution、interaction_agent_story_generate、interaction_continuity_review、story_reference_review、writing_targeted_revision、imports_completion_review 中经 `review_completed_import`→`test_change_sources.py` 的路径）；仅 `world_entity_fusion_suggestions` 的 handler 包装层+入队路由无直接测试（E1-6b）。
- **路由组覆盖对照**：app/main.py:754-773 注册 18+ 路由组。零后端测试的前缀：`/api/story`（E1-4）、`/api/legal`（E1-5）。`/api/novels/{id}/memories`（continuity）有 test_api.py+continuity/test_api.py 覆盖；`/api/interactions`、`/api/outline`、`/api/assistant` 有模块测试。
- **114 表对照**：按表名在测试中检索不可靠（测试经 ORM 模型类引用，表名仅出现在 e2e 裸 SQL）；逐表覆盖归属 F4 的权威清单，本槽位不复核计数，仅记录方法限制。

### mock 违规面清单

- **当前合规**：`@patch`/`mock.patch` 全后端测试面（含模块测试、infrastructure、evals/tests）按文件粒度核对 patch 调用数 vs `autospec=True` 数，无缺口；且 `test_test_harness.py:153 test_all_unittest_patch_calls_use_literal_autospec_true` 以 AST 守卫强制（扫描范围=根 conftest+全部 test_*+tests 路径段文件，含 evals/tests，经 inventory 缓存清单）。此前的多行 rg 初筛出现的“违规文件”经逐点核对全部带 autospec（多行调用），不构成发现。
- **盲区**：`monkeypatch.setattr`（567 处）无 spec/守卫（E1-8）。
- **测试专用生产面**：`/api/world/_test/entities/{id}/text-archive`（APP_ENV=test 门禁，有隔离与门禁正反测试）——P4 清理时不要当死路由删除。

### 漏测清单

1. `tests/account_project_preferences` 52 测试不进门禁（E1-1，P1）。
2. `evals/tests` 124 测试不进任何自动门禁（E1-2）。
3. `/api/story` HTTP 层零测试（E1-4）。
4. `/api/legal` 零测试（E1-5）。
5. `handle_world_entity_fusion_suggestions` handler 包装+入队路由（E1-6b）。
6. imports 优雅失败→resume→重跑路径无测试（D5b-1 已记，此处确认测试面缺失部分属同一批）。
7. `tests/chaos` 场景脚本不被收集（E1-9a，定位问题非覆盖问题）。

### 失实测试线索清单

1. `test_conflict_checks_real_llm.py:209,211,329` 断言生产不可产的 `continuity_location_mismatch`（=D3b-1，复核成立；该文件是 real-LLM 付费门禁，不污染 fast 层基线，但验收必然失败于规则层）。
2. 除上述外，本次“测试独有字符串/行为”普查未发现新实例（方法：rule kind、任务名、kind/status 字面量对照生产产出点；受字面量方法限制，语义级失实仍依赖各 D 组读码结论）。
3. `test_retired_owner_paths.py`/`test_active_project_route_closure.py` 为退场面守卫（断言 404），是有意的守卫测试，非失实。

### 其他 P4 相关事实

- 根 conftest 关键 fixture：`db_session`（savepoint 回滚）、`project_factory`/`test_project_id`、`account_llm_connection`（无网络 DeepSeek 种子）、`test_entity_id`/`test_character_id`、`async_client`/`raw_async_client`（后者不带 CSRF marker）；PG UUID→CHAR(32) 的 SQLite 编译适配不可删（testing-guide 载明 NUMERIC affinity 风险）。
- F1 清理批次（shared/enums、shared/types、shared/utils、shared/constants 死面）的测试侧施工清单见 E1-7。
- E1-1/E1-2 可与 F5-9 合并为一个“测试收集面与运行器一致性”批次：pyproject testpaths 两行 + Makefile eval-fast/其他 eval 目标运行器统一。

## 受阻

无。（限制说明：未运行任何测试套件（按禁令），全部结论基于配置、源码与基线记录；114 表逐表覆盖归属 F4 权威清单；`make test` 首次纳入 account_project_preferences/evals/tests 后的实际通过率需实施批次首跑确认。）
