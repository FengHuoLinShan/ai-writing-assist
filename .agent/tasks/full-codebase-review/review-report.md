# 全代码库优化审查总报告

日期：2026-09-11；同日经 3 个独立只读复核子代理与主 Agent 再审修订。原审查产物为 W1 六槽、W2 十八槽、W3 四槽，共 28 份槽位报告，另有 W0 由主 Agent 直接完成。现有产物没有保存可独立核验的“子代理运行 → 槽位”映射，因此本报告不再把无法由仓库证据复算的子代理数量写成完成证据。授权链：用户“开始计划”启动 W0/W1，“/goal 执行全部计划”授权全部剩余波次。本报告是计划 §1 定义的第一个独立验收结果；**优化实施未开始、未授权**。

## 1. 基线与范围

- **冻结审查基线**：分支 `main`，HEAD `e7d0b8d5b`，`origin/main` = `2c462f2c7`（本地领先 1 未推送提交）。下文覆盖、发现和门禁数字只对应这一固定基线及当时工作树。
- 他人 WIP 全程未动：imports api.py/test_import_api.py（D5b 标注审查）、styles.css（D8c 标注审查）、`.agent` 两文档。审查零代码改动，产物全部为 `.agent/tasks/` 下新增/本地文件，无提交/推送/部署。
- 计划 §10.1 规模事实在执行中全部复核有效；一处修正：历史"16 份 stable_hash"实为更多（census 见 §4）。
- **复核时现场（2026-09-11）**：主工作区已是 `codex/frontend-fixes-20260911@bfc766ec1`。当前有 2454 个跟踪文件；相对冻结账本新增 `frontend-console/tests/uiSizeContracts.test.js`，另有 19 个账本路径的 Git blob 已变化。它们不纳入旧账本的 2453/2453 结论；任何实施批仍须基于目标 HEAD 与 WIP 重新取证。

## 2. 覆盖账本（§1 完成标准第 1、2 条）

- `coverage-ledger.csv`：在冻结基线下，**2453/2453 个 Git 跟踪路径均已归属并填入审查状态**，当时与 `git ls-files -z` 比对零缺漏、零重复、零未归属。F6 的 docs/历史目录采用“前缀+计数+抽样”分类，因此“路径均有账本行”不等同于“每份历史资料逐句阅读”。
- 生产源码逐文件语义阅读：后端 9 模块 + infrastructure + core/shared/app、前端 shell/bridge/composables/shared 与 176 个 vue/views、54 个 alembic 版本均有对应槽位记录。个别超大测试文件采用“结构清单+关键段精读”，F6 资料目录使用上述分类方法；具体深度以各槽位的方法声明为准。
- 槽位完成度：F1–F6、D1、D2a–c、D3a/b、D4、D5a/b、D6a/b、D7、D8a–c、E1–E3、X1–X4 共 **28** 份槽位报告齐备于 `units/`。

## 3. 依赖与所有权图（指针）

- 114 表→模块→模型类所有权图、迁移链现状（head=`20260913_schema_parity_repair`，线性无分叉）、repositories 语义差异三档：units/F4.md 共享事实。
- 19 路由注册面、47 任务 handler 清单、LLM 客户端获取路径现状、状态枚举地图：units/F2.md；组合根/DI/生命周期：units/F1.md。
- 前端契约地图（手写:契约≈254:153）、island↔路由↔装载图、bridge seams：units/F3.md；发布契约/镜像清单/CI 步骤图：units/F5.md；文档权威源地图：units/F6.md。

## 4. 已裁定发现

- 原审查汇总为 **241 条**（P0=0，P1=4，P2=58，P3=179），这一总数在报告、任务记录和发现账本之间一致；但当前 `findings-ledger.md` 不是完整逐 ID 清单，未保存全部去重映射，尚不能从仓库材料机械复算 241。具体批次只采用重新核实的发现 ID；后续应补完整去重清单后再把该总数作为机器可验证指标。
- 主 Agent 原记录对 9 条头部发现做过抽查（F4-1、F3-1、F6-1、D5a-1、D5b-1、D3b-1、D8b-1、D8c-3、X1-1）；本次又复核了四条 P1 与 R/B 组关键证据。各发现记录了回滚建议，但涉及既有重复资产、数据库 migration 或新功能的项不能只靠 `git revert` 完整回退，须在实施计划中补数据或迁移回退方案。
- **安全结论的证据边界**：冻结基线的静态走读未发现 P0 级安全、数据丢失或隔离问题；novel_id/owner、图片上传、confirmation、RP、地图 CAS、固定 SHA 发布链在代码层闭合。PG、浏览器、部署、动态性能与真实模型未运行，因此这不是当前版本的运行时安全验收。
- **P1 四条**：F4-1 测试扫描 `.venv`、D5a-1 生产 prompt 注入第三方小说内容、D5b-1 显式恢复的窄进程死亡窗口可保留 `phase="failed"` 并再次失败、E1-1 的 52 个测试脱离自动门禁。四条当前代码证据仍成立；原批次表遗漏了 F4-1，且把 D5b-1 概括成所有优雅失败均可 resume，已在修订批次中纠正。

## 5. 旧候选复核结果（约 70 项）

- 仍成立（多数形态修正）：A1-1 部分、A1-2、A1-5、A1-6、A1-7、A2-1、A2-2 残余、A2-4 缩小、A3-1、A3-2、A3-5（24→64 处）、A5-1 扩大、A5-2、A5-4、A5-5、A5-8、A5-10、A6-1（4→5）、A6-3/4/7、A7-1 缩小、A7-3、A7-4 形态更新、A8-2、A8-3、A8-4 部分、A8-5/X1-9、X1-3、X1-6 不立项但证据完备、Wave0 四目录跟踪现状。
- 已解决：X1-7、A4-1、A4-5、A8-6 大部分、迁移压缩（20260703 已 squash）、镜像 test 文件、根 .test-logs、A3-5 story 面、A4-3 符号。
- 误报/不成立：A1-3、A1-4、`deep_import_phase01` 删除（真实验收工具链）、`tools/deepseek_scene_probe` 删除（DECISIONS 依据）、`frontend-console/docs` 未跟踪说、A1-1"恒 403"、A4-8 字面、A3-4 不存在、mock autospec 合规担忧（AST 守卫零违规）、"evals 全部 CI 活跃"（仅 eval-ask-world）。
- 功能变更/待查：X1-8（运行时 session 标记）、A5-6/A5-11/A5-14、A1-8（底稿缺失按现状关闭）、A3-6/A9-12（前提未满足）。

## 6. 架构裁定（P3）

按 §5 P3 条件（外部兼容可证明、事实源明确、依赖方向更清楚、可回滚）裁定：

- **实施候选（进 B3/B4）**：snapshot/checkpoint helper 收敛（归 llm_runtime 与 tasks facade，现有 owner）；story 入队流合并；conflict/generate 同步双轨退役；手写 manager 工厂化（保留语义清单前置）；smartDedup composable 化（1238 行本体保留）；views/ 迁移。
- **保留现状（不为完整报告虚构重构）**：状态词表三套（实为不等价状态机，收敛=破坏表达）；interaction/assistant 状态枚举；world 资产双词汇（收敛点已建，残余渐进）；InteractionView 2620 行拆分；X1-8 flag；A1-3 组织读点；UUID 类型统一。
- **补证据（不进可执行队列）**：D3b-2 memory 双投影、D2a-8 O(n²)、D6a-2/D6b-4 N+1、D8b-9 大列表、D7-7 selected 全量加载——全部"收益待测"，需专门授权的隔离性能会话（probe 修复+数据集）后再裁。
- **超范围/需确认**：alembic squash（部署窗口取证）；D3b-1/D4-1 补实现 vs 删 seam（产品裁定）；X3-3 world 任务用户取消（产品裁定）；D6b-5 搬家语料特判（产品裁定）；E1-9 语料样本（法务）。

## 7. 批次依赖与回滚

原 `batch-plan.md` 的大组仍作为发现到候选方向的映射，但本次复核证实 R1/R2/R4/R6、B1a/B1b、B2d 不是可独立验收的原子批，B4 也缺少逐项契约、验证与回滚。后续执行以新增的 [实施计划](implementation-plan.md) 为准：先补 F4-1 与 E1-1 两个门禁缺口，再处理生产 prompt 与窄恢复缺陷；机械清理按领域拆分；共享 hash/schema/snapshot 先证明兼容；产品裁定、性能补证据和数据变更不进入默认实施队列。

## 8. 实际验证结果

- 冻结基线的 W0 结果：docs-check、ruff、前端 eslint/build 全过；vitest 183 文件 2480 测试全过；后端 fast 层 5288 过/2 失败/12 跳过，其中 `test_identity` 独立跑完为 1 passed / 1623.90s。以上均不是当前 `bfc766ec1` 分支的验证结果。
- 全程子代理只读，未运行任何测试/构建/数据库命令；主 Agent 仅运行门禁、`test_identity` 定性复跑（27 分钟通过实锤）与只读抽查。
- 文档门禁：本报告落盘后运行 `make docs-check` 与 `git diff --check`（结果见 TASK.md 验证节）。

## 9. 未验收项（如实列出）

- 未运行：`make test-postgresql-critical`、`make test-e2e`、浏览器功能/视觉 e2e、`make test-deploy`/镜像测试、动态性能基准（probe 本机必败且需隔离数据集）、真实模型/付费验收。均按计划 §8 属实施期或专门授权会话；审查结论中涉及运行时行为的部分以代码证据+静态推演为限，已逐条标注。
- 当前分支新增/变更的 20 个前端文件未纳入冻结账本；若从当前分支实施相关前端批，先做增量复核。优化实施、合并、推送、发布均未因本次复核获得授权。
