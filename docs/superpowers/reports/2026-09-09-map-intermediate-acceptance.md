# 地图中级版本验收记录

## 交付

完成纯空间节点管理、实时来源失效提示、候选差异比较、查找定位、专注浏览与图片选择预览。
独立 review 还修复了空间节点切换到不支持层级、子孙路径身份更新、旧图片节点的专注状态残留，
并补齐局部 CSS 变量默认值。沿用 World 地图所有权、owner/novel_id、CAS、不可变版本和图片私有存储。

目标用户为长期创作作者。缩短首图完成、核对与返回写作的路径是产品假设；本次技术验收不是用户访谈。

## 真实项目

复用 `诡秘之主_第一部 小丑_前60章`，专用库 `ai_novel_acceptance_guimi`。没有重导入或建立同名项目。
原三点廷根图保留为历史，新版为 12 图元（7 个地点/地标、5 个街道）及区域父节点。
引用经地图保存接口调用 Evidence 重新验证，当前地图没有来源警告。

使用内置 gpt-image 生成廷根辅助底图、水仙花街联排住宅、黑荆棘所在建筑。通过既有上传候选、
采用、图片关联和三点校准路径持久化，三个展示层读回均为 ready，鉴权图片字节与服务端 hash 一致。
正文未确定的坐标、方向和距离只作示意；配图中的材质、光线和未记载外观明确属于视觉补全。

真实浏览器完成上传采用、点击/搜索定位、配图展示、来源跳转第 30 章、返回地图、说明修改保存、
刷新恢复和深浅主题检查。1280×720 专注图面起点为 222px、高 480px；390×844 图面高 300px，
页面宽度等于视口，无页面横向溢出。原图元与底图在刷新后完整保留。

数据库在改动前与就绪后分别备份。通过原备份的 `writing_drafts` COPY 数据与当前 60 条记录
比较，所有行完全一致，合并 SHA-256 均为
`56eb4cc287f45e39400d8bd80752fbfae4f47c31e073a56cc205255a26a71d12`。

## 自动验证

| 验证 | 最终结果 |
|---|---|
| `make test-ci TEST_WORKERS=2` | 通过：文档、secret hygiene、依赖审计、Ruff、部署契约、覆盖率和前端测试 |
| 后端快速层 | 4892 passed、12 skipped；覆盖率 85.99% |
| 部署契约 | 268 passed |
| 前端 Vitest | 163 文件、2252 passed |
| 地图浏览器 | 7 passed，独立 `ai_novel_map_intermediate_e2e_20260909` 库 |
| 地图 PostgreSQL 并发/不可变检查 | 3 passed，同一专用测试库 |
| 独立 review | 后端与前端定向验证、lint、build；修复后主代理重新运行完整门禁 |

首次完整检查受本机演示 `.env` 影响；已将演示配置移出默认加载位置，并显式传给演示服务，
不修改测试以适应演示配置。review 后完整测试发现 `--map-zoom` 缺少 CSS fallback，补齐后完整门禁通过。

## 保留与边界

本机长期演示包位于桌面 `NovelCraft-诡秘地图演示-20260909`，包含三分钟演示步骤、截图、
生成素材、服务端读回图片、资料身份、地图 JSON、前后数据库备份与验证日志。
代码在 `codex/map-dev` 的本地提交中；本轮未推送、合并 main 或部署生产。

项目没有站内模型连接。站内直连付费生图、自动关系提取和模型输出质量不在实测覆盖内；
本轮图片为真实 gpt-image 产物，应用上传/采用/校准链路已实测。公开读者、RP 地图、
故事历史回放、真实比例和行程计算继续不在本轮范围内。
