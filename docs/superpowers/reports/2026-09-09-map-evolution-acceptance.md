# 地图持续创作能力验收记录

## 当前结论

六个方向的主体实现已集成：手工标记可选择正文依据并生成局部空间候选，字段差异和实际采用范围
可核对，写作与地图双向跳转，四级地图及沿街/入口/朝向关系、图片版本对照与轻量路线排演。
仍有站内 gpt-image 真实调用等待 OpenAI 图片连接；不能把现有图片的上传验收当作该调用已通过。

目标用户为长期创作作者。减少切页查找和改动不确定性是产品假设，本次验证的是操作与数据行为，
不是用户访谈结论。地图和排演属于可编辑派生资产，不改写世界正史。

## 真实诡秘项目

- 复用 `937c86f1-a2c3-4db5-963d-f3181095f339`，专用数据库 `ai_novel_acceptance_guimi`。
  未重导入或创建同名项目。当前 API 8001、前端 8080；未部署到生产。
- 文本连接使用用户授权的本机 DeepSeek，正常账户连接验证通过。
  第一次真实任务完成但没有合格关系，未将它算作生成质量通过。发现并修复首轮缺少明确 schema、
  单引文无法解释多来源的契约问题，后续任务保留可解释统计；第一次舍弃的具体原因无法追溯。
- 复测任务 `0ad44afb-a231-422e-82fa-705a5c35d9dd`：2 个图元、4 段资料、503 字符，
  1 次结构化输出、0 次格式重试，收到/通过 1 条、舍弃 0 条，无裁剪和候选警告。
  关系为“黑荆棘安保公司位于佐特兰街”，两个第 16 章来源分别核验引文。
- 在真实浏览器展开字段差异，先核对再确认采用 1 项；已保存版
  `684a3249-fece-4980-ab83-739ab5c810f6` 保留全部 12 个图元的原有字段与位置。
  底图因新增空间关系退出叠加；两张地点配图继续 ready。服务端可追溯到原校准版
  `5337eae7-f3b2-4007-99d2-db0d6ac15e19`，不冒充图片生成来源。
- 核对全部图元和原锚点未改变后，经正常 layout 预览与版本保存 API 重新校准，最终保存版为
  `befa47f8-c258-4acd-973e-1d2e4a728aab`，3 个图片层均 ready，来源警告为空。
  此步骤为真实 API 验证，新的校准对照界面尚待浏览器确认框解除后补验。
- 第 30 章“本章地图”找到水仙花街2号，打开后选中该标记，返回时仍为第 30 章。
  正文来源搜索、回读高亮范围、关联进入本地地图草稿和撤销已实际操作；未留下重复依据。
- 临时排演水仙花街2号到黑荆棘安保公司时，明确提示缺少道路资料，不凭同城或示意线条推算路径。
  390 像素视口无整页横向溢出；正向通路、键盘/触摸与控制点行为另有定向测试。

## 数据保护

执行前备份 `backend/.test-artifacts/map-evolution-20260909/before-evolution.dump`。
仅精确领取指定地图任务，未启动全队列 worker。采用后与该备份逐行比较：

| 内容 | 前后数量 | 结果 |
| --- | --- | --- |
| writing_drafts 完整记录 | 60 / 60 | 全部字段一致 |
| publish_chapter 任务完整记录 | 60 / 60 | 全部字段一致，仍 pending |

正文 SHA-256 为 `56eb4cc287f45e39400d8bd80752fbfae4f47c31e073a56cc205255a26a71d12`；
任务记录 SHA-256 为 `9ae7ce61386b6809ca238714ba1045c88a426afc92f2260de52407f4f7515c64`。
证据在同目录 `integrity-after-real-adoption.json`。自动测试使用独立测试库，禁止清理此演示库。

## 自动验证与独立复核

主集成 `0f3c74db7` 的 `make test-ci TEST_WORKERS=2` 退出码 0：

| 门禁 | 结果 |
| --- | --- |
| 后端快速层 | 4958 passed，12 skipped，覆盖率 86.20%（门槛 85%） |
| 前端 Vitest | 166 文件，2295 项通过 |
| 部署静态测试 | 268 项通过 |
| 文档、凭据、Ruff、依赖漏洞检查 | 通过 |
| 专用 PostgreSQL | 19 项通过，含地图并发、版本不可变、项目任务竞态、Context 路径 |
| Playwright 地图 | 7 项通过，单 worker，无重试 |

7 项浏览器测试中，空间图 2 项走真实测试 API，图片地图册 5 项使用网络替身；不能据此声称
真实图片模型通过。日志与浏览器截图在 `backend/.test-artifacts/map-evolution-20260909/`。
后续生成摘要/校准界面增量经过 91 项定向测试和 lint/build，独立 review 的 3 项 P2 已修复并复测；
主分支集成后另外运行地图编辑器、正文依据与章节地图 55 项通过，真实页已读到正确生成摘要。
真实验收发现的“撤销后旧备份复活”已修复：自动清理前核对本页已知快照，不删除其他标签页随后
写入的不同内容；独立 45 项回归通过。最后代码 `581e44a12` 上前端全套 **2312 项通过**，
ESLint 和生产构建通过。此最后代码尚未重新完成被原生确认框阻断的真实浏览器复核。
独立子代理分别审查并修复后台来源/采用边界、写作恢复、画布操作、校准追溯和异步界面问题。
非阻塞提示为 SQLite 资源警告与已归档依赖提示，未通过无关改动消除它们。

## 未完成项与演示边界

站内图片生成仍需可用的 OpenAI 图片连接；DeepSeek 文本连接不能代替图片凭据。
现有三张真实 gpt-image 图片及上传/采用/校准证据属于之前的中级验收，见
`2026-09-09-map-intermediate-acceptance.md`，不得合并为站内直接图片 API 成功记录。
当前工作仅在本地主题分支，尚未推送、合并、跑远端 CI 或部署。
最终浏览器复核遇到原生“放弃本机编辑”确认框阻断控制，浏览器接口未能处理且应用控制权限拒绝
代点，已请用户取消。数据仍在；不将剩余界面行为记作通过。

本机长期材料已增补到 `/Users/tywww/Desktop/NovelCraft-诡秘地图演示-20260909/map-evolution/`，
包含最新数据库备份、正常 API 校准预览/读回、完整性记录、真实候选/来源截图和本轮日志。
原中级版本材料保留。使用入口、启动命令和简短演示顺序仍在该演示目录的 `README.md`。
