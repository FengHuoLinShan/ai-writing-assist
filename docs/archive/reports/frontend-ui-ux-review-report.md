# NovelCraft 前端 UI/UX 审查报告

> 说明：本报告为只读审查产物，未修改任何项目代码或文件。

---

## 一、整体评价

当前前端是一个功能完整、模块众多的单页应用，覆盖了项目、写作、世界对象、地图、大纲/场景、RAG、上下文编译、生成中心、LLM 设置等核心链路。设计系统已具备基础 Token（颜色、字号、间距、圆角、阴影），整体走 Apple 极简 + 杂志留白风格，暗色模式也已接入。

但产品在**信息架构、视觉层级、组件一致性、移动端适配和可访问性**方面仍处于“可用但不够好用”的阶段。主要表现为：

- 大量信息以密集表格呈现，行动按钮堆叠，认知负荷高。
- 右侧 `contextual-notes` 留白区几乎没有实际内容，造成布局失衡。
- 部分关键状态（loading、empty、error、disabled 原因）表达不足。
- 移动端/tablet 上表格横向滚动、表单拥挤、复杂工作流难以操作。
- 色彩对比度、焦点状态、键盘可访问性存在 WCAG 风险。
- 文档（README）与实际视觉主题不一致，会给新用户/开发者造成预期错位。

---

## 二、核心问题清单（按优先级）

| 优先级 | 问题 | 影响 |
|--------|------|------|
| P0 | 表格行内操作按钮过多（世界对象、大纲、场景工作台） | 用户难以快速定位主要操作，误触风险高 |
| P0 | 右侧 `contextual-notes` 面板长期为空，却固定占位 | 浪费水平空间，破坏杂志布局意图 |
| P0 | 部分文字对比度不足（`--text-tertiary #94A3B8` 在 `#FFFFFF` 上约 2.6:1） | 可访问性不达标，阅读吃力 |
| P0 | 移动端表格被迫横向滚动 | 小屏体验差，难以完成核心任务 |
| P1 | 导航图标语义不清，部分只有 tooltip | 新用户不易理解各模块作用 |
| P1 | 表单输入框仅底部边框，选中状态依赖下划线变色 | 边界不明显，易与分隔线混淆 |
| P1 | 缺乏骨架屏/加载占位，只有文字“加载中...” | 感知性能差，用户不确定系统是否卡住 |
| P1 | 生成中心流程步骤与实际交互脱节 | 用户不清楚当前在哪一步、下一步该做什么 |
| P1 | 禁用按钮缺少原因提示 | 用户不知道为何不能操作 |
| P2 | README 描述为“终端深色主题”，实际为浅色主题 | 文档与实现不一致 |
| P2 | 快捷键/命令栏 discoverability 低 | 高级功能被浪费 |
| P2 | 颜色/字号/间距在部分视图中未严格遵循 Token | 视觉不统一 |

---

## 三、页面级优化建议

### 1. 项目页（`projectView`）

**当前问题**
- 有项目时首屏直接展示卡片网格，缺少“产品价值”说明。
- 卡片 hover 才显示编辑/删除，移动端无 hover，操作不可见。
- “导入小说到当前项目”按钮与卡片列表层级并列，概念混淆。
- 导入记录表格列宽分配偏技术（时间、章节数、状态），缺少文件大小/失败原因。
- 回收站入口（左上角小按钮）位置隐蔽。

**优化方向**
- 在网格上方增加 1 行项目统计 + 快速引导（“继续写作 / 导入 / 生成结构”）。
- 将项目卡片改为始终可见的“更多”下拉或右下角操作区，替代 hover 显示。
- 把“导入”提升为与“新建项目”并列的主操作，而不是折叠按钮。
- 导入记录增加“失败原因折叠展开”与“重试”入口。

**需要补充的状态**
- 项目为空时：已有 empty state，可补充推荐模板/示例项目。
- 后端未连接时：应置灰“新建/导入”并提示先启动后端。

**移动端注意事项**
- 卡片网格已变为单列，但操作按钮 hover 逻辑在触屏上失效，必须改为常驻或点击展开。

---

### 2. 写作台（`writingView`）

**当前问题**
- 三栏布局（Scene 树 / 编辑器 / Scene cockpit）在 13–15 寸屏幕上偏挤。
- 编辑器使用等宽字体 + 细边框，长时间写作舒适度不足。
- “AI 工具 · 待定”文案直接暴露实现未完成感。
- 版本选择器与删除按钮紧邻，误删风险高。
- 冲突检查条默认“暂无检查记录”文案薄弱。

**优化方向**
- 编辑器区域提供可切换的“写作模式”：精简工具栏、增大行高、使用衬线正文字体。
- 将“AI 工具”下拉改为具体动作分组（生成、提取、检查），移除“待定”占位文案。
- 版本历史折叠到抽屉或弹窗，删除版本增加二次确认或“撤销”提示。
- 冲突检查条改为可视化徽章：0 冲突 / N 冲突 / 检查中。

**需要补充的状态**
- 自动保存状态（“已保存 / 保存中 / 离线草稿”）。
- 未保存离开提示已有 `beforeunload`，但 UI 上可常驻一个“未保存”圆点。
- 当前 Scene 与光标位置联动加载地图摘要时，缺少 spinner 或渐显。

**移动端注意事项**
- 三栏在 600px 下已堆叠，但 Scene cockpit 会推到编辑器下方，建议窄屏下默认收起右侧 panel，通过按钮唤出。

---

### 3. 世界对象页（`worldView`）

**当前问题**
- 筛选区字段过多（类型、状态、来源、workflow_id、复核、入库方式、搜索），横向拥挤。
- 表格每行操作按钮最多可达 6–7 个（编辑、打开地图、提升、合并、回滚、知识、删除），视觉噪音大。
- 自动入库批次与手动对象用 `details` 折叠，缺少状态徽章和批量操作。
- 候选清洗表格中“建议动作”用纯文本颜色表达，不够直观。

**优化方向**
- 筛选区采用“常用筛选外露 + 高级筛选折叠”模式，减少首屏字段。
- 表格行操作改为“主操作外露 + 更多下拉”：主操作保留“编辑”，其余收入 `…` 菜单。
- 用图标 + 颜色标签表示“建议动作”（创建新对象、合并、别名、忽略）。
- 自动入库批次增加“新”badge 和一键“全部复核通过/忽略”。

**需要补充的状态**
- 空状态时除了提示，增加“自动提取”快速入口。
- 加载表格时增加骨架行，而不是整个区域空白。

**移动端注意事项**
- 筛选字段在 720px 下应垂直堆叠；表格横向滚动体验差，建议窄屏切换为卡片列表。

---

### 4. 大纲页（`outlineView`）

**当前问题**
- “场景卡”子视图直接显示“已迁移到场景工作台”，这是一个死胡同页面，不应作为默认子标签。
- 剧情线/篇章纲/伏笔/揭示四个子视图高度同质化，都是表格 + 新建按钮 + 筛选。
- 默认子标签是 `scenes`，但内容只是跳转按钮，用户首次进入会困惑。
- 伏笔/揭示的状态变更直接内联 `<select>`，缺少确认和变更记录。

**优化方向**
- 将“场景卡”子标签移除或重命名为“去场景工作台”快捷入口，不作为默认标签。
- 默认进入“剧情线”或“篇章纲”，并根据项目当前阶段智能推荐。
- 四个子视图统一卡片化：左侧列表 + 右侧详情面板，替代宽表格。
- 伏笔/揭示的状态变更改为点击后弹确认 toast，或在行内显示“保存”按钮。

**需要补充的状态**
- 空状态时给出示例（“一个标准长篇通常有 3–5 条剧情线”）。
- 结构资产图谱可视化（可选 P2）。

**移动端注意事项**
- 表格字段过多，窄屏下卡片化是必然选择。

---

### 5. 场景工作台（`sceneWorkbenchView`）

**当前问题**
- 健康过滤器、管理筛选、融合工具栏、列表、详情五层垂直堆叠，首屏信息过载。
- “整理”按钮实际触发的是拆分，命名与行为不符。
- 详情面板字段全是 `<textarea>` 和 `<input>`，缺少结构化预览。
- 手动融合弹窗中 4 个主要按钮并列，用户决策压力大。

**优化方向**
- 将筛选器折叠为“管理筛选”抽屉，默认只显示健康过滤。
- “整理”按钮改名为“拆分/整理”，或拆分为两个明确动作。
- 详情面板增加“Scene 摘要卡片”预览，编辑态与只读态分离。
- 融合结果弹窗按钮按决策风险排序，并将“放弃融合”与保存类按钮用视觉分组。

**需要补充的状态**
- Scene 列表加载骨架屏。
- 融合选择少于 2 个时，明确提示“再选 1 个即可融合”。

**移动端注意事项**
- 窄屏下详情面板已是 fixed drawer，需增加“返回列表”按钮和手势关闭。

---

### 6. RAG 检索页（`ragView`）

**当前问题**
- 页面充满技术指标（维度、worker、降级率、缓存命中），对小说作者不友好。
- 诊断卡片信息密度过高，8 个指标平铺。
- 搜索测试结果用 result cards，但状态/来源信息不够突出。
- “重建索引”“预热检索引擎”“重试失败向量”三个按钮并列，用户不知道先点哪个。

**优化方向**
- 状态页采用“健康状态卡片 + 可展开诊断详情”分层展示。
- 将技术指标默认折叠，外露“索引是否可用 / 已索引章节数 / 最近更新”。
- 搜索页增加结果高亮、来源标签（正文/世界对象/剧情线）、相似度可视化。
- 操作按钮按使用频率排序：测试搜索 > 重建索引 > 预热 > 重试失败向量。

**需要补充的状态**
- 索引为空时给出明确引导：“导入正文后索引会自动生成”。
- 重建索引过程中增加进度条和取消入口。

**移动端注意事项**
- 诊断 8 列网格在窄屏下已变为 110px 自适应，但仍可能换行混乱，建议改为 2 列。

---

### 7. 上下文编译页（`contextView`）

**当前问题**
- 表单字段多达 9 个，全部平铺，对新手压迫感强。
- “相关对象/人物”要求输入 UUID，极不友好。
- 输出区初始为灰色提示，没有示例。

**优化方向**
- 字段分组：必填（任务描述、范围）、可选（对象/人物/章节）、高级（预算、揭示模式）。
- 对象/人物输入改为可搜索选择器（即使 v1 用简单下拉也比 UUID 输入好）。
- 提供“常用模板”按钮（生成剧情线 / 润色正文 / 检查冲突），自动填充任务描述和范围。

**需要补充的状态**
- 编译结果中 truncated/evicted 段落增加视觉提示和原因说明。
- 复制/导出按钮在渲染前禁用，已有实现，但可增加提示。

**移动端注意事项**
- 两栏布局在窄屏下堆叠良好，但表单字段过长，建议分组折叠。

---

### 8. 生成中心（`generateView`）

**当前问题**
- 左侧生成类型与实际输入区、右侧流程步骤、结果区四块信息分散。
- 6 步流程是静态展示，与实际状态机（提交任务 → 轮询 → 完成）不完全对应。
- 结果区初始为空，用户不知道会发生什么。
- 成功后的引导只有 toast，没有在页面内给出“去候选清洗 / 去大纲”直达链接。

**优化方向**
- 将流程步骤与真实状态绑定：未开始 → 确认资料 → 生成中 → 预览候选 → 写入正史。
- 结果区默认展示上次生成记录或“生成后将在此显示进度”占位。
- 完成后在结果卡片内增加“查看候选”“查看大纲”直达按钮。

**需要补充的状态**
- 生成中增加不可取消的提示或取消入口。
- 失败后提供重试和查看错误日志入口。

**移动端注意事项**
- 两栏布局在 600px 下会堆叠，但生成类型网格只有 2 列且 gap 小，建议单列。

---

### 9. LLM 设置页（`llmSettingsView`）

**当前问题**
- “供应商模板”下拉与下方的模板卡片列表功能重复。
- API Key 输入框只有“清除”复选框，缺少显示/隐藏切换。
- 保存按钮使用 `class="btn primary"` 而非统一 `btn-primary`，样式可能不一致。
- Base URL / 模型 / 显示名称输入没有分组。

**优化方向**
- 保留模板卡片网格作为主要选择方式，移除重复下拉，或改为当前选中标签。
- API Key 增加“显示/隐藏”切换按钮，提升可用性。
- 表单分组：连接信息（Base URL、Model）、认证（Key）、显示（Label）。
- 保存后增加测试连接按钮。

**需要补充的状态**
- 连接测试成功/失败状态。
- Key 未配置时的空状态提示。

---

## 四、组件级优化建议

### 按钮
- **统一变体**：当前有 `btn-primary / btn-ghost / btn-text / btn-danger / btn-warning / btn-sm / btn-icon / btn-fab`，但部分代码使用 `class="btn primary"`（LLM 设置），应统一。
- **禁用状态**：除 `opacity` 外，增加 `cursor: not-allowed` 和 `title` 提示禁用原因。
- **主次分离**：表格行最多保留 1 个主按钮，其余收入“更多”菜单。

### 表单
- **输入框**：底部边框样式在浅色背景下边界感弱，建议增加 `background: var(--bg-hover)` 和 `border-radius`，或保持底框但增加 focus ring。
- **下拉选择**：`form-select` 已有圆角背景，但 label 与输入框间距不统一，建议统一 `form-group` 的 `gap`。
- **必填项**：统一用 `*` 或标签后缀标识，目前部分表单有 `*` 部分没有。

### 卡片
- `card` 已统一，但部分页面内联 `style="margin-bottom:8px"` 过多，建议增加 `.card-compact / .card-flat` 等变体。
- 可点击卡片缺少 `cursor: pointer` 和 focus 样式。

### 导航
- **侧边栏图标**：建议为所有一级导航增加文字标签或图标+文字组合（类似“场景”），纯图标对中文作者不够直观。
- **子标签**：`subnav` 当前是文字下划线，可增加 pill 样式变体，强化当前位置。
- **面包屑**：仅显示“项目 · 模块”，建议增加子视图和当前对象名（如“世界对象 > 候选清洗”）。

### 弹窗
- **关闭行为**：点击遮罩可关闭，但表单填写中误触关闭会导致数据丢失，建议未保存时提示。
- **按钮顺序**：确认/取消顺序应保持一致（左取消右确认）。
- **尺寸**：表单弹窗固定 560px，在移动端会贴边，建议增加窄屏 padding 或全屏 drawer。

### 列表/表格
- **表格**：增加 `.table-compact` 和 `.table-cards` 变体，窄屏自动切换。
- **行操作**：统一为“主操作 + 更多”。
- **排序/筛选**：表头支持排序时增加可点击状态和箭头。

### 提示信息
- **Toast**：已有 role/aria-live，建议错误 toast 增加“查看详情”操作。
- **空状态**：统一使用 `.empty-state`，但应增加插图（可用 SVG 或 emoji 统一风格）和主要 CTA。
- **Alert**：`.alert-warning` 已存在，建议在需要用户注意的位置统一使用，替代内联 `style="color:var(--warning)"`。

---

## 五、设计系统建议

### 颜色
- **对比度修复**：将 `--text-tertiary` 从 `#94A3B8` 调深至 `#64748B`（4.6:1 以上），或限制其使用场景（仅用于图标/装饰）。
- **语义色扩展**：增加 `--accent-soft-hover`、`--error-soft-hover` 等状态色，避免 hover 时直接跳转到重色。
- **边框层级**：`--border / --border-subtle / --border-dim` 命名混乱，建议统一为 `--border-default / --border-subtle / --border-strong`。
- **暗色模式**：Leaflet 地图在暗色下仍使用默认瓦片，建议检测暗色模式时切换深色地图主题或增加覆盖层。

### 字号
- 当前 Major Third 阶梯跨度大（14 → 16 → 20 → 24 → 36），缺少 18px。建议增加 `--text-lg: 18px` 用于卡片标题，现有 20px 用于页面标题。
- 表格内文字大量 12px，建议最低保持 13px，11px 仅用于标签/时间。

### 间距
- 统一 `card` 内边距为 `--space-5`（20px），避免部分卡片 12px、部分 24px。
- 表格行高统一为 `44px`，当前各行高度不一。

### 圆角
- 已有 `--radius-md: 6px`、`--radius-lg: 8px`、`--radius-xl: 12px`，但按钮用 `8px`、输入框用 `8px`、卡片用 `12px`，建议统一规则：按钮 8px、输入框 6px、卡片 12px、弹窗 16px。

### 阴影
- 阴影强度与层级关系可更明确：`shadow-md` 用于卡片，`shadow-lg` 用于下拉/抽屉，`shadow-xl` 用于弹窗。

### 状态样式
- **Loading**：建议增加骨架屏组件 `.skeleton`（脉冲渐变块），替代纯文字。
- **Empty**：统一 `.empty-state` 的图标、标题、描述、CTA 四段式结构。
- **Error**：统一错误卡片 `.error-card`，包含图标、标题、详情折叠、重试按钮。
- **Success**：toast 已足够，但关键操作后可增加 inline checkmark 动画。
- **Disabled**：所有禁用控件增加 `not-allowed` 和原因提示。

---

## 六、用户路径优化建议

### 路径 A：首次进入 → 创建项目 → 开始写作
1. **当前痛点**：项目页直接展示网格，没有解释产品能做什么。
2. **优化**：首屏增加“ NovelCraft 能帮你做什么”3 步引导（组织世界 → 规划结构 → 协作写作），并提供“从示例开始”。
3. **创建项目弹窗**：已有题材/语言/基调，可增加“目标篇幅”和“是否需要 AI 辅助导入”开关。

### 路径 B：导入小说 → 自动生成结构
1. **当前痛点**：导入后弹窗询问“是否启动深度导入”，但用户不清楚深度导入会做什么、多久完成。
2. **优化**：将深度导入改为明确步骤选择（场景提取 → 世界对象 → 剧情线），并展示预计耗时。
3. **进度可见**：任务进度条固定在底部，已较好；可增加“完成后通知我”或邮件/桌面通知。

### 路径 C：日常写作 → 保持设定一致
1. **当前痛点**：写作台右侧 cockpit 信息多但分散，冲突检查入口不突出。
2. **优化**：将“剧情设定冲突检查”提升为常驻顶部操作，检查结果以徽章展示；右侧 cockpit 默认展示当前 Scene 关键信息，点击展开完整。

### 路径 D：管理世界对象 → 清洗候选
1. **当前痛点**：候选清洗和世界对象在两个子标签，用户需要来回切换。
2. **优化**：在世界对象列表顶部常驻“N 个候选待处理”入口徽章；候选清洗支持批量确认/忽略。

### 全局减步
- 删除/废弃/永久删除等危险操作统一为二次确认弹窗，已较好；可增加 3 秒撤销 toast。
- 模态框表单提交后自动关闭，但如果失败应保持弹窗并显示错误。

---

## 七、优先级执行路线

### 第一阶段：低成本高收益优化（1–2 周）
1. **修复对比度**：调整 `--text-tertiary` 或限制使用场景。
2. **统一按钮/表单 class**：修正 `btn primary` 等不一致用法。
3. **表格行操作瘦身**：世界对象、大纲、场景工作台改为“主操作 + 更多菜单”。
4. **项目卡片操作常驻**：移动端和桌面端都可见编辑/删除入口。
5. **空状态增强**：为每个主要页面补充插图/emoji + 明确 CTA。
6. **禁用状态提示**：为禁用按钮增加 title/aria-disabled 说明原因。

### 第二阶段：页面结构和交互优化（2–4 周）
1. **右侧留白区治理**：决定 `contextual-notes` 是否启用；若不用则移除固定宽度，释放空间给 workspace。
2. **世界对象/大纲/场景工作台卡片化**：窄屏默认卡片列表，桌面端可选表格/卡片切换。
3. **写作台体验升级**：编辑器增加专注模式、自动保存状态、AI 工具命名清晰化。
4. **生成中心流程对齐**：将 6 步静态流程改为动态状态机，结果区增加直达链接。
5. **RAG 页作者友好化**：默认折叠技术指标，外露健康状态和行动引导。
6. **上下文编译简化**：字段分组 + 常用模板 + 对象/人物搜索选择器。

### 第三阶段：设计系统和体验细节优化（4–6 周）
1. **骨架屏组件**：为表格、卡片列表、详情面板统一设计。
2. **暗色模式地图适配**：Leaflet 瓦片或覆盖层适配。
3. **键盘可访问性**：侧边栏、表格行、模态框焦点管理、Esc 行为一致性。
4. **导航重构**：考虑图标+文字侧边栏、面包屑增强、场景/大纲关系更清晰。
5. **设计 Token 文档化**：将 CSS 变量与组件用法形成文档，避免未来漂移。
6. **响应式断点细化**：增加 768px/1024px/1440px 三档布局策略。

---

## 八、最终总结：最值得优先处理的 5–10 个 UI/UX 优化点

1. **P0 | 降低表格行操作密度**：世界对象/大纲/场景工作台的行内按钮改为“主操作 + 更多菜单”，立即减少误触和视觉噪音。
2. **P0 | 处理右侧空白栏**：`contextual-notes` 要么投入使用（放置帮助、快捷操作、最近活动），要么在桌面端移除固定宽度。
3. **P0 | 修复文字对比度**：`--text-tertiary` 在白色背景下不满足 WCAG AA，需调深或降级使用场景。
4. **P0 | 移动端表格卡片化**：当前 600px 以下表格横向滚动体验差，优先在世界对象和场景工作台实现卡片列表。
5. **P1 | 写作台增加专注模式与保存状态**：编辑器可切换清爽模式，顶部常驻“已保存/未保存/自动保存中”状态。
6. **P1 | 统一空状态与加载状态**：为每个页面补充结构一致的 empty state 和 skeleton loading。
7. **P1 | 生成中心流程可视化**：让 6 步流程与真实任务状态同步，结果区完成后给出模块直达链接。
8. **P1 | 导航图标语义增强**：所有侧边栏项建议增加文字标签或更直观的图标，减少 hover tooltip 依赖。
9. **P2 | 表单输入框增强边界感**：统一使用带背景或清晰 focus ring 的输入框，避免仅底部边框造成的识别困难。
10. **P2 | README 与实际主题对齐**：更新 README 中“终端深色主题”描述，避免新用户/开发者预期错位。

---

以上建议均基于现有代码结构和产品目标，未涉及具体代码实现，可作为下一阶段 UI/UX 迭代的参考清单。
