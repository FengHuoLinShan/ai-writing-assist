- [x] `RouteCalculationResult` 契约定义在 `geo/contracts.py` 中，包含 is_reachable、total_hours、path、reason 四个字段
- [x] `RouteQueryRequest` 和 `RouteQueryResponse` Schema 定义在 `geo/schemas.py` 中
- [x] `_parse_time_to_hours` 能正确解析 "3天"、"12小时"、"2d"、"8h"、纯数字格式，解析失败降级为 24.0
- [x] `_compile_active_graph` 加载静态边时排除 `blocked_path` 和非 `canonical` 状态的边
- [x] `_compile_active_graph` 通过 `TimelineFacade.get_geo_effects_up_to_chapter` 获取时间线事件，按 order_index 顺序应用 edge_mutations
- [x] `action='block'` 从邻接表移除边，`action='connect'`/`'reveal_hidden'` 添加/恢复边
- [x] `calculate_route` 使用 Dijkstra 算法（heapq 优先队列）求解最短路径
- [x] 起终点相同时返回 total_hours=0.0、path=[source_id]
- [x] 不可达时返回 is_reachable=False、total_hours=inf、reason 非空
- [x] `TimelineFacade.get_geo_effects_up_to_chapter` 只返回 canonical 状态且 chapter_index <= X 且 geo_effects 非空的事件
- [x] `GeoFacade.calculate_route` 正确代理 GeoTopologyService
- [x] `POST /api/geo/calculate-routing` 端点正确返回 RouteQueryResponse
- [x] 跨模块通信只通过 facade/contracts，geo 不直接导入 timeline 内部模块
- [x] 不修改任何现有 DDL（数据库表结构）
- [x] 单次路径计算耗时 ≤ 50ms（节点 ≤1000，边 ≤5000）
- [x] 所有新增代码有对应的单元测试覆盖
