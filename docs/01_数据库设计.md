# AI 长篇小说结构化创作引擎：数据库设计 v2.0（实际实现）

## 1. 数据库设计原则

- **候选与正史分离**：AI 生成内容先进入 candidate / proposal。
- **状态优先于删除**：status 字段枚举（draft / candidate / canonical / deprecated / ignored / conflicted / pending / processing / done / failed）。
- **Prompt 合并，数据拆分**：生成阶段可以合并，入库按对象类型存储。
- **JSONB 存弹性字段**：场景卡放 chapter_cards.scene_cards，地缘历史状态放 geo_locations.content_json.era_states。

## 2. 技术选型

- PostgreSQL 17（Docker 部署）
- pgvector：向量检索、对象去重、RAG embedding
- pg_trgm：名称相似、别名相似、重复对象检测
- JSONB + GIN：灵活字段和数组查询
- SQLAlchemy async（生产） / SQLite 内存引擎（测试）
- Pydantic v2 schema 校验

## 3. 完整表清单（16 张业务表 + 1 张系统表）

### 3.1 projects（小说项目）

```sql
CREATE TABLE projects (
    id UUID PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    genre VARCHAR(64),
    tone VARCHAR(64),
    language VARCHAR(16) DEFAULT 'zh' NOT NULL,
    target_length VARCHAR(32),
    current_stage VARCHAR(32),
    default_reveal_policy VARCHAR(32) DEFAULT 'author_safe' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.2 world_entities（世界对象正史库）

```sql
CREATE TABLE world_entities (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    entity_type VARCHAR(32) NOT NULL,     -- location/faction/item/event/rule/power_system/secret/legend/resource/character_ref
    name VARCHAR(255) NOT NULL,
    summary TEXT,
    public_info TEXT,                      -- 公众已知信息
    hidden_truth TEXT,                     -- 仅作者视角
    content_json JSONB DEFAULT '{}',
    importance FLOAT DEFAULT 0.5 NOT NULL,
    importance_level VARCHAR(16) DEFAULT 'normal' NOT NULL,  -- core/important/normal/temporary/alias
    reveal_level VARCHAR(16) DEFAULT 'author_only' NOT NULL, -- author_only/hinted/revealed/fully_known
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    embedding_text TEXT,
    embedding vector(1024),
    created_by VARCHAR(64),
    approved_by VARCHAR(64),
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.3 relationships（对象间关系）

```sql
CREATE TABLE relationships (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    source_type VARCHAR(32) NOT NULL,
    source_id VARCHAR(36) NOT NULL,        -- 通用引用 ID（UUID hex）
    target_type VARCHAR(32) NOT NULL,
    target_id VARCHAR(36) NOT NULL,
    relation_type VARCHAR(32) NOT NULL,
    description TEXT,
    visibility VARCHAR(20) DEFAULT 'author_only' NOT NULL,
    strength FLOAT DEFAULT 0.5 NOT NULL,
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.4 entity_aliases（对象别名）

```sql
CREATE TABLE entity_aliases (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    entity_id UUID NOT NULL REFERENCES world_entities(id) ON DELETE CASCADE,
    alias VARCHAR(255) NOT NULL,
    alias_type VARCHAR(20) DEFAULT 'name' NOT NULL,  -- name/title/nickname/alias/translation/abbreviation
    source_chapter_index INT,
    confidence FLOAT DEFAULT 0.8 NOT NULL,
    status VARCHAR(16) DEFAULT 'confirmed' NOT NULL,
    created_at TIMESTAMP DEFAULT now()
);
```

### 3.5 entity_candidates（候选对象池）

```sql
CREATE TABLE entity_candidates (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    name VARCHAR(255) NOT NULL,
    entity_type VARCHAR(32) NOT NULL,
    summary TEXT,
    source_text TEXT,
    source_chapter_index INT,
    importance_score FLOAT DEFAULT 0.5 NOT NULL,
    confidence FLOAT DEFAULT 0.5 NOT NULL,
    candidate_reason TEXT,
    suggested_action VARCHAR(32) DEFAULT 'needs_user_decision' NOT NULL,
        -- create_new / merge_with_existing / alias_of_existing / ignore / temporary_only / needs_user_decision
    suggested_existing_entity_id VARCHAR(36),
    embedding_text TEXT,
    embedding vector(1024),
    status VARCHAR(16) DEFAULT 'pending' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.6 characters（人物档案）

```sql
CREATE TABLE characters (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    world_entity_id UUID,
    name VARCHAR(255) NOT NULL,
    aliases JSONB DEFAULT '[]',
    role VARCHAR(64),
    appearance TEXT,
    personality TEXT,
    desire TEXT,
    fear TEXT,
    secret TEXT,
    weakness TEXT,
    current_goal TEXT,
    current_state TEXT,
    current_emotion TEXT,
    stance TEXT,
    voice_style TEXT,
    behavior_rules JSONB DEFAULT '[]',
    relationship_summary TEXT,
    meta JSONB DEFAULT '{}',
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.7 character_knowledge（人物知识边界）

```sql
CREATE TABLE character_knowledge (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    character_id UUID NOT NULL REFERENCES characters(id),
    target_type VARCHAR(32) NOT NULL,
    target_id VARCHAR(36) NOT NULL,
    knowledge_level VARCHAR(16) NOT NULL,   -- unknown / rumor / partial / full / false_belief
    known_content TEXT,
    misconception TEXT,
    source_chapter_index INT,
    source_memory_id UUID,
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.8 geo_locations（地理扩展）

```sql
CREATE TABLE geo_locations (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    world_entity_id UUID NOT NULL REFERENCES world_entities(id),
    location_level VARCHAR(32) NOT NULL,
    parent_location_id UUID,
    x FLOAT,
    y FLOAT,
    position_label VARCHAR(64),
    scale_label VARCHAR(64),
    terrain TEXT,
    climate TEXT,
    access_level VARCHAR(32) DEFAULT 'normal' NOT NULL,
    summary TEXT,
    content_json JSONB DEFAULT '{}',        -- 含 era_states
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.9 geo_edges（地理关系边）

```sql
CREATE TABLE geo_edges (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    source_location_id UUID NOT NULL,
    target_location_id UUID NOT NULL,
    relation_type VARCHAR(32) NOT NULL,     -- road_to/river_to/inside/north_of/south_of/near/hidden_path/blocked_path/borders
    direction_label VARCHAR(64),
    distance_label VARCHAR(64),
    travel_time TEXT,
    difficulty TEXT,
    visibility VARCHAR(32) DEFAULT 'public' NOT NULL,
    condition_text TEXT,
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.10 geo_eras（宏观历史时期）

```sql
CREATE TABLE geo_eras (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    name VARCHAR(255) NOT NULL,
    order_index INT NOT NULL,
    summary TEXT,
    start_event_id UUID,
    end_event_id UUID,
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.11 memory_records（长期记忆）

```sql
CREATE TABLE memory_records (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    memory_type VARCHAR(32) NOT NULL,       -- chapter_state/event/character_state/knowledge/foreshadowing/resource/outline_drift/geo_history
    target_type VARCHAR(32),
    target_id VARCHAR(36),
    chapter_index INT,
    title VARCHAR(255),
    summary TEXT NOT NULL,
    content_json JSONB DEFAULT '{}',
    visibility VARCHAR(20) DEFAULT 'reader_known' NOT NULL,
    known_by_character_ids JSONB DEFAULT '[]',
    related_entity_ids JSONB DEFAULT '[]',
    related_character_ids JSONB DEFAULT '[]',
    related_thread_ids JSONB DEFAULT '[]',
    importance FLOAT DEFAULT 0.5 NOT NULL,
    status VARCHAR(16) DEFAULT 'canonical' NOT NULL,
    source_text_excerpt TEXT,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.12 memory_update_proposals（记忆更新提案）

```sql
CREATE TABLE memory_update_proposals (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    chapter_id UUID,
    chapter_index INT,
    proposal_type VARCHAR(32) NOT NULL,
    payload JSONB NOT NULL,
    confidence FLOAT DEFAULT 0.5 NOT NULL,
    reason TEXT,
    source_text_excerpt TEXT,
    decision VARCHAR(16) DEFAULT 'pending' NOT NULL,
    decided_by VARCHAR(64),
    decided_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT now()
);
```

### 3.13 timeline_events（轻量时间线）

```sql
CREATE TABLE timeline_events (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    title VARCHAR(255) NOT NULL,
    summary TEXT NOT NULL,
    order_index INT NOT NULL,
    chapter_index INT,
    event_type VARCHAR(64),
    related_character_ids JSONB DEFAULT '[]',
    related_entity_ids JSONB DEFAULT '[]',
    related_thread_ids JSONB DEFAULT '[]',
    related_location_ids JSONB DEFAULT '[]',
    geo_effects JSONB DEFAULT '[]',
    visibility VARCHAR(20) DEFAULT 'author_only' NOT NULL,
    known_by_character_ids JSONB DEFAULT '[]',
    status VARCHAR(16) DEFAULT 'candidate' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.14 plot_threads（剧情线）

```sql
CREATE TABLE plot_threads (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    name VARCHAR(255) NOT NULL,
    thread_type VARCHAR(32) NOT NULL,       -- main/secondary/hidden/relationship/villain/foreshadowing
    summary TEXT,
    visible_goal TEXT,
    hidden_truth TEXT,
    start_chapter INT,
    planned_payoff_chapter INT,
    current_stage VARCHAR(32),
    related_character_ids JSONB DEFAULT '[]',
    related_entity_ids JSONB DEFAULT '[]',
    related_memory_ids JSONB DEFAULT '[]',
    reader_known_state TEXT,
    author_known_state TEXT,
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.15 outline_arcs（篇章纲）

```sql
CREATE TABLE outline_arcs (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    title VARCHAR(255) NOT NULL,
    arc_index INT,
    start_chapter INT,
    end_chapter INT,
    arc_goal TEXT,
    core_conflict TEXT,
    main_opposition TEXT,
    entry_hook TEXT,
    midpoint_turn TEXT,
    climax TEXT,
    result TEXT,
    next_hook TEXT,
    related_thread_ids JSONB DEFAULT '[]',
    related_character_ids JSONB DEFAULT '[]',
    related_entity_ids JSONB DEFAULT '[]',
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.16 chapter_cards（章节卡）

```sql
CREATE TABLE chapter_cards (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    chapter_index INT NOT NULL,
    title VARCHAR(255),
    arc_id UUID,
    chapter_goal TEXT NOT NULL,
    main_conflict TEXT NOT NULL,
    emotional_point TEXT,
    plot_function VARCHAR(64),
    must_happen JSONB DEFAULT '[]',
    must_not_happen JSONB DEFAULT '[]',
    involved_character_ids JSONB DEFAULT '[]',
    involved_entity_ids JSONB DEFAULT '[]',
    related_thread_ids JSONB DEFAULT '[]',
    visible_progress JSONB DEFAULT '[]',
    hidden_progress JSONB DEFAULT '[]',
    offscreen_progress JSONB DEFAULT '[]',
    foreshadowing_actions JSONB DEFAULT '[]',
    ending_hook TEXT,
    scene_cards JSONB DEFAULT '[]',          -- 场景卡 MVP 放 JSONB
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.17 foreshadowing_plans（伏笔计划）

```sql
CREATE TABLE foreshadowing_plans (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    name VARCHAR(255) NOT NULL,
    summary TEXT,
    surface_meaning TEXT,
    hidden_meaning TEXT,
    planned_seed_chapter INT,
    planned_reinforce_chapters JSONB DEFAULT '[]',
    planned_payoff_chapter INT,
    related_entity_ids JSONB DEFAULT '[]',
    related_thread_ids JSONB DEFAULT '[]',
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.18 reveal_plans（信息揭示计划）

```sql
CREATE TABLE reveal_plans (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    target_type VARCHAR(32) NOT NULL,
    target_id VARCHAR(36) NOT NULL,
    secret_summary TEXT NOT NULL,
    reveal_stages JSONB DEFAULT '[]',
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.19 rag_chunks（RAG 分块）

```sql
CREATE TABLE rag_chunks (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    source_type VARCHAR(32) NOT NULL,
    source_id VARCHAR(36),
    chapter_index INT,
    chunk_index INT,
    start_offset INT,
    end_offset INT,
    char_count INT,
    text TEXT NOT NULL,
    summary TEXT,
    entity_ids JSONB DEFAULT '[]',
    character_ids JSONB DEFAULT '[]',
    thread_ids JSONB DEFAULT '[]',
    visibility VARCHAR(20) DEFAULT 'author_only' NOT NULL,
    importance FLOAT DEFAULT 0.5 NOT NULL,
    index_version VARCHAR(32) DEFAULT 'legacy' NOT NULL,
    embedding_status VARCHAR(32) DEFAULT 'pending' NOT NULL,
    embedding_error TEXT,
    index_warnings JSONB DEFAULT '[]',
    embedding vector(1024),
    metadata JSONB DEFAULT '{}',
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.20 review_reports（复查报告）

```sql
CREATE TABLE review_reports (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    target_type VARCHAR(32) NOT NULL,       -- world_structure/geo_structure/plot_structure/chapter_cards/memory_update/entity_candidates
    target_id VARCHAR(36),
    decision VARCHAR(16) NOT NULL,          -- pass/minor_revision/major_revision/reject
    score FLOAT,
    problems JSONB DEFAULT '[]',
    conflict_warnings JSONB DEFAULT '[]',
    early_reveal_warnings JSONB DEFAULT '[]',
    character_knowledge_warnings JSONB DEFAULT '[]',
    duplicate_entity_warnings JSONB DEFAULT '[]',
    geo_warnings JSONB DEFAULT '[]',
    revision_instructions JSONB DEFAULT '[]',
    created_at TIMESTAMP DEFAULT now()
);
```

### 3.21 writing_drafts（正文草稿）

```sql
CREATE TABLE writing_drafts (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    chapter_index INT NOT NULL,
    chapter_card_id UUID,
    title VARCHAR(255),
    content TEXT,
    version_number INT DEFAULT 1 NOT NULL,
    status VARCHAR(16) DEFAULT 'draft' NOT NULL,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.22 import_records（导入记录 — 原设计以外新增）

```sql
CREATE TABLE import_records (
    id UUID PRIMARY KEY,
    novel_id UUID NOT NULL REFERENCES projects(id),
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(16) NOT NULL,         -- txt/epub/html/mobi
    file_size INT DEFAULT 0 NOT NULL,
    total_chapters INT DEFAULT 0 NOT NULL,
    imported_chapters INT DEFAULT 0 NOT NULL,
    status VARCHAR(32) DEFAULT 'pending' NOT NULL,  -- pending/processing/done/failed
    error_message TEXT,
    created_at TIMESTAMP DEFAULT now(),
    updated_at TIMESTAMP DEFAULT now()
);
```

### 3.23 async_tasks（异步任务 — 基础设施表）

```sql
CREATE TABLE async_tasks (
    id UUID PRIMARY KEY,
    task_type VARCHAR(64) NOT NULL,
    status VARCHAR(16) DEFAULT 'pending' NOT NULL,
    progress FLOAT DEFAULT 0 NOT NULL,
    meta JSONB DEFAULT '{}',
    result JSONB DEFAULT '{}',
    error_message TEXT,
    created_at TIMESTAMP DEFAULT now(),
    started_at TIMESTAMP,
    finished_at TIMESTAMP,
    heartbeat_at TIMESTAMP
);
```

任务领取使用 `FOR UPDATE SKIP LOCKED` 防止 worker 冲突。

## 4. 与原始设计的主要差异

| 项目 | 原始设计 | 实际实现 |
|------|---------|---------|
| import_records | 无此表 | 新增，用于小说文件导入跟踪 |
| async_tasks.task_type | 列出 8 种类型 | 实际注册了 world_entity_extraction |
| character_knowledge | 使用 UUID 存储 target_id | 使用 String(36) 存储 UUID hex |
| relationships | source_id 使用 UUID 类型 | 使用 String(36) 通用引用 |
| entity_aliases | 无 ON DELETE CASCADE | 使用 ON DELETE CASCADE |
| 测试引擎 | 仅提及 | 完整实现：SQLite 内存替代 pgvector（Text 存 JSON 序列化向量）|

## 5. 暂缓表

- scene_cards 独立表：先放 chapter_cards.scene_cards JSONB
- geo_location_states：先放 geo_locations.content_json.era_states
- 多张 memory 子表：先用 memory_records
- entity_embeddings：先把 embedding 放 world_entities/entity_candidates
- 专门 graph/community 表：暂不做复杂 GraphRAG
