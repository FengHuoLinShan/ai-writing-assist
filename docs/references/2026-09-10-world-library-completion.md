# 世界资料库补全执行记录

依据：[补全计划](../product/world-library-completion-plan.md)与[审查报告](2026-09-09-world-library-review.md)。目标为长期作者：找到资料、安全修改、继续此前决定、审阅成果、核对变更影响。真实作者喜好与模型质量仍需单独验证。

## 已实施的能力

| 范围 | 当前行为 |
|---|---|
| R0 修复基线 | 上轮审查修复固定为本地提交 `b00c7f241a9983d834625eccc20dd3559ddaba82`，原工作区未修改。 |
| R1 持续模型 | 19 区域从父成果增量继承，稳定身份、明确废弃、依赖失效与阶段条件；长期决定参与聊天及候选守卫。可按面向缩小本轮读取、选定条目整理为待审设定、导出完整模型。历史成果单独开会话继续。 |
| R2 历史与恢复 | 会话/历史分页、搜索、前后文与显式选入；聊天和模型推演通过原任务恢复。终态消息与可恢复结果在同一 fenced commit 保存，提交后崩溃不重复调用模型或追加回合。未保存推演可从历史回看或明确载入编辑。 |
| R3 影响与复核 | Story 结构、正文精确选段、地图当前版本可回读；原确认实际保留的内容进入语义检查。范围、来源、政策或确认变化使旧回执过期，遗漏独立呈现；World 不改写其他领域资产。 |
| R4 大库与界面 | 首屏只读概览与当前页，正文/工作稿按目标加载，辅助配置按需读取；引用选择分页搜索。规则保存原位更新，失效深链保留可用工作区，动态会话读取不被旧缓存遮挡。 |

复用 PostgreSQL、现有队列、suggestion、Context、Vue bridge 与原生表单；未新增依赖、数据表、Vault 同步或自治 Agent。模型快照与任务完成均不等于 Canon 采用。

## 长资料库性能

合成数据由正式 API 创建：20% 已发布页面、60% 工作稿、20% 实体；长文为 15,600 字。环境为本机 Apple M1 Pro、16 GB、Chromium 151，专用 PostgreSQL 17。冷/热各 20 次，搜索和输入各 20 次；输入测量包含键盘事件到两次动画帧的间隔。

| 资料数 | 冷首屏 p95 | 热首屏 p95 | 搜索 p95 | 输入 p95 | 首屏最大 API 数据量 / 请求数 |
|---|---:|---:|---:|---:|---:|
| 100 | 258.8 ms | 149.9 ms | 105.7 ms | 24.1 ms | 25,315 B / 10 |
| 1,000 | 261.2 ms | 270.9 ms | 832.5 ms | 23.0 ms | 25,324 B / 10 |

达到计划中的 2.5 秒首屏、1 秒搜索、100 毫秒输入目标；首屏未返回长文全文，传输量没有随全文总量增长。该数据是本机开发环境验收，不是生产 SLA。原始数据存于 `/Users/tywww/.codex/artifacts/world-library-completion-20260910/metrics-100.json` 与 `metrics-1000.json`；测试记录 base commit 与工作树状态。

## 验证结果与收尾记录（已完成）

- 后端 World/Evidence/Story/任务相关全量 **1,978 passed，12 skipped，2 deselected**。
- 前端全量 **2,401 passed**；ESLint、Ruff、生产构建、文档门禁和 diff-check 通过。
- 真 PostgreSQL 并发专项 **2 passed**；终态提交后模拟完成前崩溃，恢复不重复 provider 调用或消息写入的测试通过。
- 最终完整视觉 **38/38 passed**。此前 7 项旧基线对应 9 张 PNG 已逐张核对更新；此次另更新 1 张原本含抽屉遮挡的横屏共创基线，像素阈值未变。
- 全站功能完整运行 **264 passed / 4 failed / 2 skipped**，完整作者流程在该轮通过。4 项失败均是生成中心断言仍从旧的 workspace-content 容器查找已 Teleport 的内容；只改为真实抽屉定位后，生成中心完整文件 **21/21 passed**，覆盖这 4 项。因此功能验收合计覆盖 268 个用例，未将该结果描述为单次全量 268/268。2 项显式性能用例沿用前述单独性能验收。
- 390/768/1440px 模型编辑、旧版本保留、依赖失效、刷新恢复及 1,000 条历史定位/选择通过。768px 抽屉入口与滚动后模型内容均已目视；标签、收回按钮的实际命中断言通过，原遮挡已消除。

本轮曾按仓库 AGENTS.md 的“同一测试修复尝试 3 次后仍失败”停止；用户明确“继续修复这两项并完成验收”后恢复。收尾修复如下：

1. 完整作者流程限定页面内“更多工具”，等待健康面板初始化结束，精确匹配直接 summary/状态徽标并按需展开；完成断言只接受已通过、有提示或需修正，不再把校验失败计为完成。专项通过。
2. 共用 AI 抽屉复用 Vue Teleport 挂载到 body，脱离工作区 isolation:isolate；z-index 95 置于侧栏90之上、顶栏100之下。390/768/1440px 增加控件边缘/中心 elementFromPoint 命中检查，旧代码768px先失败、修复后通过，局部修改、父版本保护与刷新恢复通过。组件测试改为读取真实body浮层，不用Teleport替身。
3. 812px 横屏旧共创截图同样包含侧栏遮挡，逐图核对后更新这一张基线，像素阈值不变。写作菜单出现一次黑块；代码与基线均未改，隔离复跑通过，最终全量再次复验。

功能全量日志为 `functional-resume-full.log`，生成中心补验日志为 `generate-resume-final.log`；最终视觉日志为 `visual-resume-final.log`。前端本轮全量、lint 与构建日志分别为 `frontend-resume-final.log`、`frontend-resume-lint.log`、`build-resume-final.log`。

证据目录：`/Users/tywww/.codex/artifacts/world-library-completion-20260910/`。`backend-final.log` 与 `postgres-concurrency.log` 保留后端证据。最新图片为 `world-drawer-768-final.png` 和 `world-model-{390,768,1440}-final.png`；旧遮挡图片、修复前命中失败日志及原黑块图片保留作审查对照。

## 边界

所有验证使用合成资料、专用数据库与本地受限对象存储，未使用真实 Vault 或付费模型。静态引用与定向语义检索不证明全库穷尽；地图语义检查不包含图像几何辨识，完整世界模型也不冒充 Scene/人物可见性投影。代码保留原 schema 容量上限，超限显式处理。

R0 修复已单独提交；R1–R4 补全及两项收尾修复完成本地工程验收，整理为 `codex/world-library-review-fixes` 主题分支提交。本轮未推送、合并或部署；原工作区的助手运行时 WIP 保持不动。后端与性能代码本次未修改，沿用前述已通过证据；本次重验前端、浏览器、视觉、lint、构建与文档门禁。
