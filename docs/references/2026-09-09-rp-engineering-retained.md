# RP 实验终止与工程成果保留（2026-09-09）

用户在完成六组文本评审后决定终止该 worktree，将有价值的部分合入 main 并清理分支。
本次从最新 main `1b0d356d114679484f01cdfa52293740de1f22dd` 选择性提取，不整批合入实验分支。

## 保留范围

- 长期约定：既有回顾 JSON/DTO 中最多4,000字符；省略保留、显式空值清空；自动摘要由代码继承。
- 首次保存的零覆盖、分支归属、epoch/路径校验、草稿与冲突处理，以及包含回顾的导出。
- 摘要 v3：自动生成内容不等于用户逐项确认，真正手工修正继续受旧值防复活保护。
- DeepSeek RP max 兼容：新快照固定 max、65,536总输出与900秒客户端超时；旧快照和其他模型不升级。
- 等待首段时的可停止反馈；完整 JSON 的窄反斜杠恢复、通道字符计数与截断重试不降预算的修复。

故事 Prompt 使用新版本 `interaction-story-v7`，正文风格恢复为实验对照中的 v5，保留约定接入。
未通过的 v6 新增写作要求不进入 main；实验专用输入指纹辅助、质量复核/返修、连续回放 CLI、
状态 oracle、开发语料、比较页及一次性恢复脚本仅归档，不新增生产依赖、表或 migration。

## 结论与局限

小闭环共72次真实 DeepSeek max调用，四条全新旅程各12轮，六组配对完整，四份合成长输入
摘要完成。实际输入640,210、输出281,120 token；按冻结高峰未缓存价格折算USD 0.652771。
68次正文/检查点首字中位数31.620秒、P90 64.681秒；完成中位数47.845秒、P90 79.460秒。
这些支持调用兼容性，不证明多次摘要后的整体故事质量。

Astra/medium独立审查复用8项校准缓存，新增1次，累计25/26。现有策略A和新版B均有4/6
明确要求违规；普通互动自然度2.33→2.67，看海自然度2.33→2.00。用户选择A胜3、B胜1、
相当1、两边都有问题1，没有文字备注。模型与用户偏好门均未通过，停止扩量。

长期约定保护的价值是可靠保存与提供规则，而非保证遵守；正文可能先生成错误，再被摘要
沿用。状态层、自动返修及更多提示词不能据此宣称有效。四项产品实际操作与最终通过没有获得
用户确认，不能将六组文本评审写成本批完整用户验收或声称减少幻觉。

## 保留集验证

在最新 main 上重新验证：相关后端237项通过、2项真实模型用例排除；PostgreSQL约定/并发10项、
前端114项和390px浏览器3项通过，覆盖首次保存、刷新清空、等待与键盘停止、草稿及分支入口。
lint、构建、Prompt契约、文档与差异门按当前保留集执行；本次不新增付费调用。
Evidence仅变更Prompt标识，来源接口、检索、可见性和snapshot语义未变，相关文档逐项核对无影响。

## 归档

完整源码、未跟踪实验文件、请求/结果缓存和用户评价已保存到本地受限目录
`archives/rp-first-batch-20260909/source-and-evidence.tar.gz`，8,615个普通文件逐项校验。
压缩包SHA-256：`4827bf399c8d300d352ab382be869ab0755290bad70a32a1326cc2acaae72a05`。另有完整文件manifest、原始diff和原分支基线SHA。
只排除可重建依赖、Python缓存及Git工作树指针；原始实验数据库保留，不进入版本库。
本批只做本地集成与分支清理，远端推送与生产部署不在此次操作范围。
