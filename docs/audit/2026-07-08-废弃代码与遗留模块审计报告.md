# 废弃代码与遗留模块审计报告（全量沉淀）

> **日期**：2026-07-08（初版）→ 2026-07-08（重扫刷新）→ 2026-07-08（修复落地）
> **范围**：全量后端（`backend/modules/`、`backend/prompts/`）+ 前端 + 文档
> **维度**：废弃代码、已移除模块残留、死代码路径
> **执行方式**：全量扫描 + import 追踪 + 路由验证 + git 历史审计

---

## 一、执行摘要

初始发现 7 项独立问题，无 Critical。经重扫验证，**2 项已自动修复**（空壳目录已清理、`split_chapters_legacy` 已重命名）。后续代码复核确认其中两处需修正：`EntityAliasCreate` 是活跃 alias 创建请求 schema；`timeline` / `geo_relations` 仍是当前 context wire key，不能只按文档重命名。已按复核计划清理确认废弃项。

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 High | 0 | 废弃 scene prefetch/reinforcement pipeline 已删除 |
| 🟡 Medium | 1 | world 模块未引用旧兼容 schema 已清理；context budget key 保留为兼容契约 |
| 🟢 Low | 0 | 废弃 prompt 与废弃 resume handler 已删除 |

无运行时报错风险，无损坏引用，无安全漏洞。

---

## 二、废弃 Scene Pipeline + LLM Adapter（High）

### 2.1 问题

`imports` 模块中曾有**3 条文件 + 配套 LLM adapter 类**构成旧 scene pipeline，被 `DEEP_IMPORT_LEGACY_SCENE_PIPELINE_ENABLED` 守卫：

| 文件 | 行数 | 角色 |
|------|------|------|
| `backend/modules/imports/legacy_scene_pipeline.py` | 27 | 守卫函数 `require_legacy_scene_pipeline_enabled()` |
| `backend/modules/imports/scene_prefetch.py` | 261 | 旧 Phase 0 场景候选预取 |
| `backend/modules/imports/scene_reinforcement.py` | 735 | 旧 Phase 1a 场景候选强化 |
| `backend/modules/imports/workflow_llm_adapters.py:342` | 2 classes | `_Phase0SceneCandidateLLM`、`_Phase1aSceneCandidateLLM` |

默认未设环境变量时，入口直接 raise `RuntimeError`。**~1023 行代码在正常运行中不会执行。**

修复落地后，上述 3 个文件、对应测试，以及 `_Phase0SceneCandidateLLM` / `_Phase1aSceneCandidateLLM` 已删除。`scene_candidates.py` / `scene_fusion.py` 仍被当前兼容/修复路径使用，未纳入删除范围。

### 2.2 影响

- 无运行时开销
- 维护干扰：修改 import 模块时需阅读/跳过这些文件
- 配套测试文件 `test_scene_prefetch.py`、`test_scene_reinforcement.py` 曾只覆盖旧 guard 路径，已随实现删除

### 2.3 修复结果

新 pipeline 已上线并覆盖默认场景：`phase0_plan → phase1a_scene_slicing → phase1b_enrichment → scene_commit`。旧 pipeline 文件、配套测试和专属 LLM adapter 已删除；活跃 Phase 1a/1b/2 adapter 保留。

---

## 三、废弃 Resume 任务处理器（Low，随 pipeline 一起清理）

### 3.1 问题

| 位置 | 说明 |
|------|------|
| `backend/modules/imports/tasks.py:82-88` `handle_deep_import_resume()` | docstring 标注"已废弃"，只打日志，不执行任何逻辑 |

该函数是候选管理时代的产物，深度导入已改为全自动。调用方已不期望它做有效操作。

### 3.2 修复结果

`deep_import_resume` task handler 已删除。用户可见恢复能力仍通过活跃接口 `POST /api/imports/deep/resume` 复用原 task；`/api/tasks` 的 module-only tombstone 保留，避免旧 task type 被普通任务入口提交。

---

## 四、World 模块旧兼容 Schema 类（Medium）

### 4.1 问题

`backend/modules/world/schemas.py:1370-1550` 原先保留了一组标注为「旧/兼容保留」的 Pydantic 模型。复核发现 `EntityAliasCreate` 仍被 `POST /api/world/aliases` 使用，不能按整段废弃删除：

| 类名 | 行号 | 说明 |
|------|------|------|
| `EntityAliasResponse` | 1370 | 旧别名响应 — 兼容保留 |
| `EntityAliasListResponse` | 1386 | 旧别名列表响应 |
| `EntityAliasCreate` | 1393 | 活跃别名创建请求；保留类名和 wire shape |
| `EntityCandidateCreate` | 1431 | 旧候选创建请求 |
| `EntityCandidateUpdate` | 1449 | 旧候选更新请求 |
| `EntityCandidateResponse` | 1465 | 旧候选响应 |
| `EntityCandidateListResponse` | 1496 | 旧候选列表响应 |
| `RelationshipCreate` | 1503 | 旧关系创建请求 |
| `RelationshipUpdate` | 1517 | 旧关系更新请求 |
| `RelationshipResponse` | 1531 | 旧关系响应 |
| `RelationshipListResponse` | 1550 | 旧关系列表响应 |

### 4.2 影响

- 不干扰运行
- 增加 schema 文件维护负担
- 新开发者容易混淆该用新版还是旧版

### 4.3 修复

未引用的旧 alias response/list、旧 entity candidate、旧 relationship schema 已删除；`EntityAliasCreate` 保留并改为当前语义说明。无调用方的 `WorldRepository.create_candidate()` legacy 方法已删除。

---

## 五、废弃 Prompt 文件（Low）

### 5.1 问题

| 文件 | 行数 | 说明 |
|------|------|------|
| `backend/prompts/structure_review_memory.md` | 289 | 对应已移除的 review 模块 |

文件顶部已标注废弃声明，不再被任何代码路径引用。

### 5.2 建议

已删除，并同步更新仍描述“保留历史参考”的文档。

---

## 六、文档 budget 分类旧引用（Medium）

### 6.1 复核修正

| 文档 | 位置 | 问题 |
|------|------|------|
| `docs/modules/09_context.md` | 第 142-144 行 | `timeline`、`geo_relations` 是历史命名，但仍是当前 `CONTEXT_BUDGET` / `budget_used` wire key |

### 6.2 处理结论

本轮不重命名 key，避免破坏 context 契约和现有测试。文档已强化说明：这两个 key 不再表示独立 timeline / geo 模块；如未来要迁移到 `world_events` / `map_relations`，需单独做兼容迁移。

---

## 七、已修复项目（不再计入待处理）

| 项目 | 修复结果 |
|------|----------|
| 已移除模块空壳目录（character/geo/review/timeline） | ✅ 目录已删除，不复存在 |
| `split_chapters_legacy` 命名误导 | ✅ 已重命名为 `split_chapters_from_api` |
| `docs/modules/04_settings.md` 文档缺口 | 需补充，本次扫描范围不包含 |
| `main.py` 注释清理 | 无害，按需处理 |

---

## 八、未发现的问题（本次重扫确认）

- **无** `@deprecated` 装饰器使用
- **无** 损坏的路由或 import 引用（17 个路由器均指向存在的模块）
- **无** 孤立的 Python 文件（`core/`、`shared/`、`infrastructure/` 全部活跃）
- **无** 未使用的 CRUD 方法
- **无** 前端死文件（67 个 JS 文件全部可达；`prototypes/` 是设计产物）
- **无** `TODO: remove` / `TODO: deprecat` 等待清理标记
- **无** 跨模块 import 已移除模块的代码

---

## 九、清理建议优先级

| 优先级 | 操作 | 前置条件 | 工作量 |
|--------|------|----------|--------|
| 🔴 High | 删除废弃 scene pipeline 3 文件 + 配套测试 + LLM adapter 类 | 确认新 pipeline 已覆盖全部场景 | ~1023 行 |
| 🔴 High | 删除废弃 scene pipeline 3 文件 + 配套测试 + LLM adapter 类 | 已完成 | ~1023 行 |
| 🟡 Medium | 清理 world/schemas.py 未引用旧兼容类 | 已完成；`EntityAliasCreate` 保留 | ~150 行 |
| 🟡 Medium | 更新 09_context.md budget 分类说明 | 已完成；不重命名 wire key | 说明修正 |
| 🟢 Low | 删除废弃 prompt structure_review_memory.md | 已完成 | 289 行 |
| 🟢 Low | 删除废弃 resume handler | 已完成 | ~10 行 |
