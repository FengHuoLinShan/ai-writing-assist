# 可优化清单（行动追踪）

> **日期**：2026-07-07
> **来源**：全量代码库三维度审计（性能、安全、架构）
> **用途**：逐项追踪优化执行进度

---

## 状态说明

- ⬜ 待开始
- 🔄 进行中
- ✅ 已完成
- ❌ 已取消

---

## 🔴 High 优先级

### 架构

- [x] ✅ **H1** DI 容器注册函数在 main.py 与 worker.py 中各维护一份，已出现漂移
  - 文件：`app/main.py:72-105`, `infrastructure/tasks/worker.py:72-165`
  - 结果：已提取到 `app/bootstrap.py`；`app/main.py` 与 `infrastructure/tasks/worker.py` 都调用 `register_container_services()`，worker 使用 `ignore_existing=True` 保持幂等
  - 估时：2-3h

- [x] ✅ **H2** imports 模块 50 文件扁平目录急需拆分子包
  - 文件：`modules/imports/`（~21K 行）
  - 建议：拆为 `imports/parsing/`、`imports/workflow/`、`imports/entity_extraction/`、`imports/scene/`
  - 结果：Phase 2 Scene 实体抽取簇已迁入 `modules/imports/entity_extraction/`；顶层仅保留 `scene_entity_extraction.py` 兼容 shell，HTTP API、DB schema、async task wire shape、prompt/timeout/checkpoint 语义不变
  - 估时：8-12h

- [x] ✅ **H3** world 模块 services/ 42 文件无子包分层
  - 文件：`modules/world/services/`（~17K 行）
  - 建议：拆为 `services/core/`、`services/map/`、`services/worldbuilding/`
  - 结果：已拆为 `services/core/`、`services/map/`、`services/worldbuilding/`；通用 helper 迁到 `services/common.py`；`services/__init__.py` 继续作为顶层 re-export hub，`services/map_service.py` 保留为历史兼容导出层；world facade、HTTP wire shape、DB schema 和 Pydantic schema 不变
  - 估时：6-8h

- [x] ✅ **H4** workflow_runtime.py Protocol `__getattr__` catch-all 破坏类型安全
  - 文件：`modules/imports/workflow_runtime.py:8-16`
  - 结果：已移除 `__getattr__` catch-all，改为显式 Protocol surface；旧深度导入测试入口已删除，仍保留生产 resume/abandon API
  - 估时：3-4h

- [x] ✅ **H5** outline facade 直接写 SQLAlchemy 查询违反分层
  - 文件：`modules/outline/facade.py` — `get_active_foreshadowing()`
  - 结果：`get_active_foreshadowing()` 已改为转发 `ForeshadowingPlanService.get_active_dicts()`；查询下沉到 service/repository
  - 估时：1-2h

- [x] ✅ **H6** SceneContract 构造代码在 4 个方法中重复（约 80 行）
  - 文件：`modules/outline/services.py`
  - 结果：已提取 `scene_to_contract()` 并由 SceneService 多处复用
  - 估时：1-2h

- [x] ✅ **H7** 前端缺失 TypeScript 或共享 API 契约校验
  - 文件：`frontend-console/api.js`（150+ 端点）
  - 结果：未引入 TypeScript/codegen/新依赖；已新增 `frontend-console/apiContracts.js`，以 vanilla JS registry 覆盖项目、设置、导入、上下文、世界/地图、写作冲突检查和 RAG 高风险 wrapper 子集的 method/path/query/body/timeout，并让对应 `api.js` wrapper 消费该契约。Vitest 覆盖入口加载顺序、registry key 存在性和代表 endpoint 映射；当前不覆盖响应字段级 schema drift。
  - 估时：4-6h

### 性能

- [x] ✅ **H8** `PHASE1B_ENRICH_CONCURRENCY=200` 对 LLM API 极度危险
  - 文件：`modules/imports/scene_enrichment.py:17`
  - 处理：默认值按当前兼容要求不改；风险由 `LLMProcessLimiter` 的进程级并发/速率/熔断、`TaskWorker` 并发上限和 embedding batch queue 背压共同缓解
  - 估时：0.5h

- [x] ✅ **H9** BgeEmbeddingClient `encode_lock` 串行化所有 embedding 为全局瓶颈
  - 文件：`infrastructure/embedding/client.py`
  - 结果：已替换为按 query/document 分离的 embedding batch queue；保留未启动 worker 时的 direct encode lock
  - 估时：4-6h

- [x] ✅ **H10** 角色位置查找 N+1：全量加载后 Python 过滤
  - 文件：`modules/world/repositories.py:1577`
  - 结果：`find_characters_by_location()` 已改为 DB-side JSON 条件过滤 `meta.location_id`，保留 novel_id/status 隔离和返回 shape
  - 估时：2-3h

- [x] ✅ **H11** 逐实体单独调用 embedding API，调用次数线性增长
  - 文件：`modules/world/services/extraction_service.py`
  - 结果：实体抽取按章节批量生成 query/document embedding；异常、格式异常或长度不匹配时降级为 None，保留候选入库与去重语义
  - 估时：2-3h

- [x] ✅ **H12** JSON 字段全文扫描查找 auto_ingested 实体
  - 文件：`modules/world/repositories.py:628`
  - 结果：`get_recent_auto_ingested()` 已改为 JSON bool 谓词 + PostgreSQL partial recent index，按 `novel_id, created_at DESC, id DESC` 覆盖 canonical auto_ingested 最近查询
  - 估时：1-2h

- [x] ✅ **H13** MapTile.bulk_upsert 逐行 N 次 SQL
  - 文件：`modules/world/map_repositories.py:303`
  - 结果：已改为按 dialect 分块的 multi-values upsert；空列表直接返回 0，同批重复坐标最后一次生效
  - 估时：1-2h

- [x] ✅ **H14** get_breadcrumbs 循环串行查询 N+1
  - 文件：`modules/world/map_repositories.py:237`
  - 结果：已改为递归 CTE 单次查询，并保留 path 防环和深度上限；返回顺序仍为顶层在前、当前在尾
  - 估时：1-2h

- [x] ✅ **H15** 文件上传全量读入内存无流式处理
  - 文件：`modules/imports/api.py:65`
  - 结果：上传 API 改为 1MB 分块读取并在超过 50MB 时提前返回 413；正常路径仍向 ImportService 传完整 bytes，HTTP response shape 不变
  - 估时：3-4h

- [x] ✅ **H16** list_projects_using_defaults 全表扫描后内存过滤
  - 文件：`modules/settings/services.py`
  - 结果：settings 改为构造全字段覆盖项目子查询，并通过 project facade 的分页 projection 做 DB-side exclusion；保留 total/truncated/response shape
  - 估时：1-2h

### 安全

- [x] ✅ **H17** 用户数据直接注入 LLM prompt 无净化
  - 文件：`modules/outline/generation/context_builder.py`
  - 结果：进入 prompt 的动态文本统一经边界包装、单条限长和常见 injection 模式检测；生成 warning 写入上下文并渲染
  - 估时：2-3h

- [x] ✅ **H18** 前端缺少 Content-Security-Policy 头部
  - 文件：`frontend-console/index.html`
  - 结果：`index.html` 已配置 CSP meta baseline；`style-src` 仍保留 `'unsafe-inline'` 作为兼容入口，后续可继续收紧
  - 估时：1-2h

---

## 🟡 Medium 优先级

### 架构

- [x] ✅ **M1** outline 与 writing 模块双向 facade 依赖
  - 建议：引入事件/回调解耦或明确层级，ADR 记录
  - 结果：writing services 不再顶层 import outline facade；断章同步和冲突检查 Scene 读取改为可注入 provider，默认 provider lazy 调用 outline facade 保持旧行为；outline 继续只读消费 writing facade/contracts
  - 估时：4-6h

- [x] ✅ **M2** world/models.py 1189 行混合多领域模型
  - 建议：拆为 models_character.py、models_worldbuilding.py、models_bible.py 等
  - 结果：`modules.world.models` 已拆为 package：`models/core.py`、`models/character.py`、`models/profiles.py`、`models/worldbuilding.py` 与 `models/common.py`；顶层 `__init__.py` 继续注册所有 ORM 表并保留旧 import path、兼容别名、DB schema 和 API/wire shape 不变
  - 估时：4-6h

- [x] ✅ **M3** workflow.py 旧场景流水线代码大量残留
  - 建议：设废弃时间线，1 版本加 warning，之后移除
  - 结果：`workflow.py` 已移除旧 Scene prefetch / reinforcement / single-chapter fallback / fusion wrapper 和 `_segment_scenes` 入口；默认 Scene 阶段只保留 Phase0 plan → Phase1a slicing → Phase1b enrichment → Scene commit 新 seam，legacy 组件文件仍作为显式启用的历史维护入口保留
  - 估时：2-3h

- [x] ✅ **M4** 场景实体提取 1824 行单体类混合路由、编排和委托
  - 建议：提取 StrategySelector 路由类
  - 结果：Phase 2a 路由选择已下沉到 `scene_entity_strategy.py` 的 `SceneEntityExtractionStrategySelector`，覆盖 empty / small-sample parallel / bulk / batched / checkpoint resume；`scene_entity_extraction.py` 保留 LLM、persistence、checkpoint、alias merge、prompt、timeout 和返回契约不变
  - 估时：4-6h

- [x] ✅ **M5** workflow.py 50+ 静态方法薄包装仅委托其他类
  - 建议：Phase runner 直接 import 并构造函数注入依赖
  - 结果：已清理 `workflow.py` 中不属于 `DeepImportWorkflowRuntime` 协议、且无生产调用的薄包装/死代码；`_analyze_structure` 直接调用 `workflow_structure_phase.ensure_minimum_structure_outputs`，测试改测 canonical helper；保留显式 runtime seam 作为 phase runner 协议，PhaseRunner DI 大重构不纳入本项
  - 估时：2-3h

- [x] ✅ **M6** context loader DI 模式不一致
  - 结果：context 具体 loader 已统一为构造函数注入 callable；生产默认 callable 仍委托原 facade/outline 服务来源，未改变 API、DB、schemas、`SCOPE_LOADERS`、loader `name` 或 ContextCompiler 外部 contract
  - 估时：2-3h

- [x] ✅ **M7** writing facade 暴露 API Response Schema 而非契约对象
  - 建议：facade 统一返回 contracts dataclass
  - 结果：`writing.contracts` 不再 re-export `WritingDraftResponse`，facade create 系列统一返回 `WritingDraftContract`；service/repository 补齐 contract 所需字段映射，autosave API 在路由层显式 contract→`WritingDraftResponse` 适配，HTTP response body/wire shape 不变
  - 估时：1-2h

- [x] ✅ **M8** map_service.py 是纯 pass-through 导出文件
  - 结果：map 具体实现已拆到 focused 子服务；`modules.world.services.map_service` 按 README 和现有 API/tests 稳定 import path 保留为兼容导出层，删除会破坏兼容路径
  - 估时：1h

- [x] ✅ **M9** 前端状态管理无模块化隔离
  - 结果：已新增 `frontend-console/stateSlices.js`，迁出项目切换持久化、writing viewState 清理、项目摘要持久化、listener 通知、DOM 同步调度和 `global_settings_cache_version` storage 失效安装；`state.js` 保留 `appState` shape、listener registry、Proxy wiring、`updateUIForState`/topbar help UI，并保持 setter 顺序为赋值 → 副作用 → listener → DOM 同步。Vitest 覆盖 helper 与现有 topbar/state 顺序。
  - 估时：6-8h

- [x] ✅ **M10** AppError 与 DomainError 双重重叠异常层级
  - 结果：`AppError` 无业务引用且已删除，业务异常统一通过 `core.errors.DomainError` 处理
  - 估时：1-2h

### 性能

- [x] ✅ **M11** TaskWorker 串行处理无并发
  - 结果：`TaskWorker.run_forever()` 已按 `TASK_WORKER_MAX_CONCURRENT_TASKS` 维护 in-flight runner 集合并发领取/执行任务
  - 估时：4-6h

- [x] ✅ **M12** LLMClient 无并发控制、限流或熔断
  - 结果：`infrastructure/llm/limits.py` 已提供进程级 semaphore、token-bucket rate limit 和 circuit breaker；`LLMClient.generate()`、stream、structured direct provider path 与远程 embedding 调用已接入
  - 估时：3-4h

- [x] ✅ **M13** 大章节场景实体提取回退串行处理
  - 结果：当前活跃 deep import Phase2 入口已切到 `Phase2WorldExtractor`，按 Phase0 window 计划并发执行，默认 `PHASE2_WORLD_WINDOW_CONCURRENCY=20`；legacy `SceneEntityExtractionService._process_scene_batch_serial` 仅保留旧 batch/helper 语义，batch 内串行是为了 DB/session、rolling memory/context、dedup seen keys 和 progress/checkpoint 顺序稳定
  - 结论：不做代码修复；不恢复旧 real LLM 测试，仅保留现有 unit test 语义

- [x] ✅ **M14** RAG 检索去重 O(n²) 成对余弦相似度比较
  - 结果：当前检索入口已通过 `top_k <= 50`、召回分支 limit、dedupe comparison window 将 embedding 成对比较限制在小窗口内；已有测试覆盖 dedupe window 和公开 facade `top_k` 截断。不引入 numpy/LSH，避免新增运行时依赖和近似召回语义变化
  - 估时：2-3h

- [x] ✅ **M15** write_batch 单次失败整批 chunk 标记失败
  - 结果：`EmbeddingWriter.write_batch()` 批量失败后保留 batch warning 并降级逐片写入；成功 chunk 清理旧错误，失败 chunk 清空 stale embedding；`retry_embeddings()` 按实际成功数统计并保留降级 warning
  - 估时：1-2h

- [x] ✅ **M16** CharactersLoader 重复加载当前 Scene
  - 结果：`CharactersLoader` 优先使用 `bundle.scene.pov_character_id`，仅在缺失时 fallback 查询 scene contract，避免重复加载当前 Scene
  - 估时：1h

- [x] ✅ **M17** 章节文本应用层截断浪费 DB I/O
  - 结果：`writing.facade.list_latest_drafts_for_chapters()` 增加可选 `content_limit`，启用时 repository 用 SQL `substr()` 投影必要契约字段；outline context builder 只多取 1 字符用于截断判断，prompt 正文仍严格裁到上限
  - 估时：1h

- [x] ✅ **M18** EmbeddingCache 驱逐 O(n) 全量扫描
  - 结果：已改为 `OrderedDict` LRU；命中刷新 LRU 顺序但不延长 TTL，覆盖已有 key 不触发误淘汰
  - 估时：1h

- [x] ✅ **M19** 去重算法精确匹配后未短路仍计算全量信号
  - 结果：`WorldEntityFusionService._pair_similarity()` 对规范化精确名称和别名命中直接返回词法结果，不再计算 summary overlap；substring/非精确路径仍保留 summary 比较
  - 估时：1-2h

- [x] ✅ **M20** 状态重放全内存执行大世界可能 OOM
  - 结果：`MemoryService` 从最近快照后的事件改为按 `(chapter_index, sequence, id)` keyset 分页重放，normalize/finalize 只做一次；快照计数、continuity 历史判断和 rebuild 终点改用聚合查询，保留无快照无事件时 panorama 回退 world state 的原行为
  - 估时：3-4h

### 安全

- [x] ✅ **M21** chardet 启发式字符集检测可被利用做 DoS
  - 结果：编码检测样本限制为 64KB；缺失编码或置信度 < 0.7 时回退 UTF-8；TXT 仍保持 strict 解码以保留不可恢复编码失败语义
  - 估时：1h

- [x] ✅ **M22** LLM reranker 响应解析无 schema 校验
  - 结果：reranker 响应已通过 Pydantic 严格校验 `scores`；拒绝 bool/字符串/非有限数值，合法分数保留 clamp、补齐和截断语义
  - 估时：0.5h

- [x] ✅ **M23** AI 草稿内容无输出净化即存储返回
  - 建议：存储前最小净化：移除 HTML 标签、限制内容长度
  - 结果：`WritingGenerationService.generate_candidate()` 在 candidate 入库前净化 title/content，移除 HTML tag 与 script/style 内容，保留普通文本；`provenance_json.content_sanitization` 只记录 `content_html_removed` / `title_html_removed` 布尔摘要，不保存原始 HTML
  - 估时：1-2h

- [x] ✅ **M24** entity_type 字段无白名单校验
  - 建议：在 schema 中用 Literal 限制为预定义类型集合
  - 结果：world entity schema 入口统一通过白名单 `normalize_entity_type()` 做 normalize/validate；白名单保留现有 `organization`/`object`/`ability`/`artifact` 兼容类型；不改 DB schema、ORM column 或 API response shape
  - 估时：1h

- [x] ✅ **M25** 请求路径未脱敏记录于日志
  - 结果：`_TimingMiddleware` 异常日志 path 先经 `_redact_request_path()` 脱敏，UUID、长 hash/slug、纯数字路径段替换为 `<id>` 并限制长度，路由与响应行为不变

- [x] ✅ **M26** 前端错误存储全局共享跨用户可见
  - 建议：添加 novel_id 范围或用 DB 存储替换内存 deque
  - 结果：`errorLogger.js` 改为按 `_errorLog:${projectId || "global"}` 分桶；旧 `_errorLog` 一次性迁移到 `_errorLog:global`；`getAll/getById/clear/latestId` 默认只看当前项目桶，切换项目会刷新 badge 并关闭旧面板；本地 entry 记录 `projectId`，但 `/api/debug/frontend-errors` payload 保持不变
  - 估时：1h

- [x] ✅ **M27** localStorage 明文存储项目对象
  - 建议：仅持久化 projectId 和 title 或改用 sessionStorage
  - 结果：`currentProject` 持久化只写 `{ id, title, name }` 摘要；旧完整 JSON 恢复时投影为 `summaryOnly` 摘要，workbench 路由会继续补拉完整项目元数据；删除当前项目继续清理项目 ID 与摘要两个 key
  - 估时：1h

- [x] ✅ **M28** Settings 写操作 API 缺少 CSRF 保护
  - 建议：添加 SameSite=Strict Cookie + X-Requested-With 验证
  - 结果：settings 写端点与项目 LLM settings 写端点已落地轻量 `X-Requested-With: XMLHttpRequest` header guard；前端统一 request 对非 GET/HEAD 默认带 header，测试覆盖缺 header 403、带 header 通过、GET 不受影响和 FormData 场景。SameSite Cookie 暂不落地，因为当前项目无认证 cookie/session；这不是完整 token CSRF 体系。
  - 估时：1-2h

- [x] ✅ **M29** ProjectService 与 SettingsService 双向依赖
  - 建议：effective settings 计算完全归 settings 模块
  - 结果：已拆除 service 层双向依赖；ProjectService 不再调用 `modules.settings.facade` 或保留 effective LLM merge 算法，SettingsService 不再 import `modules.project.facade`；effective settings 计算与 ProjectContext runtime settings 物化都归 settings service，project API/facade 仅保留兼容路由和跨模块薄委托
  - 估时：2-3h

- [x] ✅ **M30** Alembic migration 硬编码 vector index 类型
  - 建议：migration 中读取 settings 动态生成 SQL
  - 结果：squashed 初始化 migration 的 vector index method 改为由 `Settings.vector_index_type` 白名单控制，仅允许 `hnsw` / `ivfflat`，并按 method 生成不同 index name；`rag_chunks.embedding` 继续使用 `vector_ip_ops` 匹配 `<#>` inner product，`core_entities.embedding` 使用 `vector_cosine_ops` 匹配 `<=>` cosine；未改 runtime 查询 operator、ORM schema 或 migration revision
  - 估时：1h

---

## 🟢 Low 优先级

### 架构

- [x] ✅ **L1** Character 模块移除后缺少 ADR 迁移记录 — 0.5h
  - 结果：新增 `docs/adr/character-module-merge-to-world.md`，确认 character 不再是活跃独立模块，人物能力并入 world，权威入口为 `docs/modules/02_world.md`、world facade 和 `/api/world/characters`
- [x] ✅ **L2** world facade 包含业务编排逻辑 — 2-3h
  - 结果：`modules.world.facade` 已清空 async wrapper，改为纯 re-export hub；实体融合入口下沉到 `entity_facade.py`，worldbuilding activation / context stale 入口下沉到 `worldbuilding_facade.py`，旧 root facade import path 保持可用
- [x] ✅ **L3** imports 多处 load_chapters 无统一 facade — 1-2h
  - 结果：imports 内部 chapter range loader 已统一到 `chapter_loader.py`；未暴露到外部 facade；`scene_entity_text` 的 Scene 关联章节小样本采样/裁剪/片段特化逻辑保留
- [x] ✅ **L4** context_compiler 部分 loader 未更新 budget_used — 1h
  - 结果：`project` / `scene` / `plot_threads` / `outline_arc` loader 均更新 `budget_used`；singleton category 不进入 `CONTEXT_BUDGET`，避免预算告警噪声；`plot_threads` 新增 8 作为风险提示阈值但不截断列表
- [x] ✅ **L5** outline context_builder 内联 Markdown 渲染 — 1-2h
  - 结果：outline context Markdown 渲染拆到 `generation/context_renderer.py`；builder 保留数据加载、外部接口和兼容 helper；测试覆盖 renderer 输出、动态边界、截断、warnings 与 `SceneSummaryText` unpack/cards 兼容行为
- [x] ❌ **L6** SmartDedup 位于 project 模块但编排其他模块 — 1-2h
  - 结果：已取消 / 设计保留。`smart_dedup_scan` 是 project 模块拥有的项目级聚合入口，通过 world/outline facade 调用模块自有去重能力；迁到 world 或 outline 会让跨模块聚合落入单一资产模块，边界更差
- [x] ✅ **L7** DI 容器缺少生命周期管理和作用域 — 2-3h
  - 结果：`core.container` 保留 `register/get/reset/Injected` 兼容语义，新增显式 `register_factory()`、singleton/transient scope、临时 `container_scope()/override()` 和 async `shutdown()`；app lifespan 按 DI 容器 → embedding client → DB manager 顺序关闭，worker 仍使用进程级 singleton 容器
- [x] ✅ **L8** Alembic env.py 重复 config.py 的 .env 解析 — 0.5h
  - 结果：`core.config.load_env_file()` 统一负责 `.env` 加载且不覆盖已有环境变量，Alembic 复用该函数后只从 `DATABASE_URL` 或 `alembic.ini` 取数据库 URL
- [x] ✅ **L9** Migration 用 `sa.func.now()` 未包裹时区 — 0.5h
  - 结果：`20260707_settings_module.py` 与 `20260707_gen_prompt_templates.py` 的 `DateTime(timezone=True)` 默认值改为 `sa.text("CURRENT_TIMESTAMP")`，不改变表/列结构
- [x] ✅ **L10** 前端路由缺少守卫机制 — 1-2h
  - 结果：`router.js` 增加统一 route guard/normalizer，`navigate()` / 首屏初始化 / `popstate` 共用同一套项目作用域与子视图规范化；项目内路由统一落到 workbench hash，`scene` 支持动态 subView，`world/candidates` 保持合法；旧 `context` 路由继续重定向到 `generate?tab=task`，`llm` 按项目状态重定向到全局或项目设置
- [x] ✅ **L11** Makefile 仅覆盖后端 lint/format — 0.5h
  - 结果：新增 `test-frontend` 与 `test-all` 测试入口；`lint` / `format` 后端 ruff 语义保持不变；未引入前端 lint/format 依赖
- [x] ✅ **L12** LLMClient generate_embedding fallback 代码不可达 — 0.5h
  - 结果：移除 BGE 失败后“fallback to OpenAI”的误导文案，保留异常向上传递；测试覆盖 BGE 抛错时不会调用远程 provider

### 性能

- [x] ✅ **L13** _task_belongs_to_novel 取全量任务行含大 JSONB — 0.5h
  - 结果：任务状态/取消查询已把 `task_id + meta.novel_id` owner 条件下推到 SQL，wrong novel 仍返回 404 且不加载任务大 JSON 后再过滤
- [x] ✅ **L14** 进度追踪生成冗余诊断数据 — 1-2h
  - 结果：`progress_events` 原有 200 上限保持不变；新增 `phase_timeline` / `acceptance_checks` / `phase_errors` 上限与 `_result_from_progress()` 兜底裁剪，保留最近诊断并记录 dropped count，避免长流程 payload 无界增长
- [x] ✅ **L15** 场景转换边界检测逐个 pattern rfind 扫描 — 1h
  - 结果：RAG 中文分块边界选择提取 scene/paragraph/sentence helper，缓存段落边界，保持每个 scene pattern 只取窗口内最后一次出现的旧语义并补 golden tests
- [x] ✅ **L16** BGE worker 队列 maxsize=200 可能回压 — 0.5h
  - 结果：BGE worker 队列上限改为 `INFERENCE_WORKER_QUEUE_MAXSIZE` 配置项，默认仍为 200；`BgeEmbeddingClient` 透传 settings，`PHASE1B_ENRICH_CONCURRENCY` 默认值保持 200
- [x] ❌ **L17** 冲突检测三项来源串行执行 — 0.5h
  - 结果：已取消 / 不按 0.5h 并发化。scene/map/memory 当前复用同一个 AsyncSession，且 memory 依赖 scene 与 map_summary；直接 asyncio.gather 不安全。未来优化应另开设计项，拆独立 session/只读边界或重排依赖
- [x] ✅ **L18** token 估算用 `len(content)//4` 近似而非精确 tokenizer — 1-2h
  - 结果：新增 `infrastructure.llm.token_estimation.estimate_token_count()`，业务侧 token metadata / 预算裁剪改为 tiktoken-based estimate；已知 OpenAI 模型使用模型映射，未知 OpenAI-compatible 模型 fallback 到 `cl100k_base` 估算，不承诺所有供应商都精确
- [x] ✅ **L19** 前端 Proxy setter 同步 localStorage 和 DOM 更新 — 0.5h
  - 结果：`state.js` Proxy setter 拆出持久化副作用、监听器通知和 DOM 同步函数，保持原顺序；Vitest 覆盖项目选择 localStorage、writing viewState 清理、listener 先于 DOM sync 且异常不阻断 DOM 更新
- [x] ✅ **L20** Leaflet CSS 全局加载无按需 — 0.5h
  - 结果：`index.html` 不再全局请求 Leaflet CSS/JS；`mapView` 在地图视图首次初始化时按需注入固定 Leaflet 1.9.4 CDN 资源并复用加载 Promise，非地图视图不再加载地图引擎，CSP 对 `https://unpkg.com` 的许可保留
- [x] ✅ **L21** EntityRelation 查询用 selectinload 但其他无 eager loading — 1-2h
  - 结果：EntityRelationRepository 统一复用端点实体 eager-load helper 覆盖 get/list/duplicate/entity-scoped 查询；EntityRelationService get/update/list 返回继续填充 `source_name` / `target_name`
- [x] ✅ **L22** prompt_loader 路径解析依赖脆弱相对路径 — 0.5h
  - 结果：`resolve_prompt_path()` 使用 backend prompts 绝对路径且支持显式 `prompt_dir`，拒绝绝对路径、分隔符和 `..` 名称，`load_prompt()` 兼容现有调用

### 安全

- [x] ✅ **L23** parse_llm_json 暴露原始 LLM 内容于日志 — 0.5h
  - 结果：`parse_llm_json` 失败 preview 先脱敏再截断，覆盖 Authorization/Bearer/api_key/token/secret/sk-* 等常见敏感片段，错误消息保持不变
- [x] ✅ **L24** 用户输入文本无后端 HTML 净化依赖前端 — 1h
  - 结果：writing service 层对用户草稿 create / autosave / update / publish 入库前统一做最小 HTML 净化；发布重复版本比较使用净化后的 title/content，后端不依赖前端 escaping 才能避免持久化 HTML
- [x] ✅ **L25** record_events 无输入长度限制 — 0.5h
  - 结果：`MemoryService.record_events()` 增加单章事件数和单条序列化 payload 字符预算，超限抛 `ValidationError` 且不回显原始 payload，写入与 flush 均不会发生
- [x] ❌ **L26** Memory API 缺少显式用户鉴权检查 — 0.5h
  - 结果：已取消 / 等待全局 auth ADR。仓库当前没有统一 current_user/auth dependency；memory API 单点加假鉴权会引入未经确认的新认证架构。当前保留 novel_id 隔离要求，等全局 auth/authorizer ADR 后处理
- [x] ✅ **L27** API novel_id 参数缺 UUID 类型强制 — 1h
  - 结果：新增 FastAPI 专用 `NovelIdQuery` / `NovelIdPath` / `NovelIdForm` 参数别名，query/path/form API 边界统一 UUID pattern 校验（兼容 hyphenated UUID 与 32 hex）；内部 service/facade/contract 与 request body schema 的 `novel_id` 仍保持现有 str 契约
- [x] ✅ **L28** 文件导入无 chapter range 上限 — 0.5h
  - 结果：上传解析章节数与 deep/stage range 都受 `IMPORT_MAX_CHAPTERS` 默认 1000 限制；上传超限返回 413 并标记导入记录 failed，deep/stage 超限返回 400；成功响应 shape 不变
- [x] ✅ **L29** 章节文本无 prompt injection 净化即注入 LLM — 1h
  - 结果：已核对为现有 outline context renderer 覆盖：章节原文进入 LLM 前使用动态 boundary wrapper、长度限制和 prompt injection warning；`backend/tests/modules/outline/generation/test_context_builder.py` 已覆盖章节原文截断、伪边界逃逸与 injection warning
- [x] ✅ **L30** 开发环境 CORS `allow_origins=['*']` 无生产检查 — 0.5h
  - 结果：`validate_cors_origins()` 在非 development/test/local 环境拒绝空 origins 或 `*` 通配，`app.main` 启动配置前统一校验，具体 origins 行为不变
- [x] ✅ **L31** LLM 结构化输出重试无指数退避 — 0.5h
  - 结果：`generate_structured()` 主修复循环与 format repair 循环均按 LLM retry 配置执行指数退避，最后一次不等待，`base_delay/max_delay <= 0` 时不 sleep
- [x] ✅ **L32** RAG 电路熔断器全局单例无 per-novel 隔离 — 1h
  - 结果：RAG circuit breaker registry 支持按 novel_id 隔离状态，同时保留 legacy 全局 breaker 与零参数注入 provider 兼容；RetrievalOrchestrator 默认按当前 novel_id 获取熔断器

---

## 文档更新追踪

### 归档

- [x] ✅ `docs/modules/character/README.md` → `docs/archive/character-module-removed.md`
- [x] ✅ `docs/modules/geo/README.md` → `docs/archive/geo-module-removed.md`
- [x] ✅ `docs/modules/review/README.md` → `docs/archive/review-module-removed.md`
- [x] ✅ `docs/modules/timeline/README.md` → `docs/archive/timeline-module-removed.md`
- [x] ✅ `docs/architecture/old-scene-pipeline.md` → 归档追踪记录
  - 结果：源文件不存在，已记录为废弃说明替代/无需迁移

### 更新

- [x] ✅ `CLAUDE.md` — 补充 character 移除确认、imports 拆分预告
- [x] ✅ `docs/modules/09_context.md` — context→generate 重定向、context_snapshots
- [x] ✅ `docs/modules/13_imports.md` — 重写为当前三阶段管线
- [x] ✅ `docs/modules/14_frontend.md` — 路由更新、settings 页面
- [x] ✅ `docs/modules/02_world.md` — services 子包分层计划
- [x] ✅ `development-guide.md` — 前端 dev/test 命令与 lint/build 现状
- [x] ✅ `testing-guide.md` — 子包化测试路径约定
- [x] ✅ `frontend-console/README.md` — TS/API 契约、Vite dev/测试脚本、CSP

### 新建

- [x] ✅ `docs/adr/character-module-merge-to-world.md`
- [x] ✅ `docs/adr/outline-writing-bidirectional-dependency.md`
- [x] ✅ `docs/adr/imports-module-decomposition.md`
- [x] ✅ `docs/adr/world-services-subpackage-layout.md`
- [x] ✅ `docs/adr/llm-concurrency-throttling-circuit-breaker.md`
- [x] ✅ `docs/adr/embedding-batch-queue-architecture.md`
- [x] ✅ `docs/security/content-sanitization-policy.md`
- [x] ✅ `docs/frontend/typescript-api-contracts.md`

---

## 估算总工作量

| 批次 | 内容 | 估时 |
|------|------|------|
| 第一批（紧急修复） | H1、H5 已完成；H8 默认不改，由限流/worker/embedding 背压缓解 | ~4h |
| 第二批（高收益） | H9, M11, M12, H18 已完成 | ~14h |
| 第三批（架构重构） | H2, H3, H7, M1 | ~26h |
| 第四批（持续改进） | 其余所有 | ~40h |
| 文档工作 | 归档 + 更新 + 新建 | ~16h |
| **总计** | | **~112-160h** |

---

> 📎 详细分析和建议见 [全量代码库审计报告](./2026-07-07-全量代码库审计报告.md)
> 📎 文档详细分类见 [文档审计报告](./2026-07-07-文档审计报告.md)
