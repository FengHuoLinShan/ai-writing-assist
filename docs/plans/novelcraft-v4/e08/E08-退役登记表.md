# E08 退役登记表：deep_import 编排核销清单

2026-09-21｜基线：分支 `codex/novelcraft-v4-g0-baseline`（E07 完成后）。
本表按 05-TRACEABILITY「E08 deep_import 编排退役：无新请求进入旧独立
owner；重复恢复/调度代码清单核销」建立。**核销条件全部满足前不删除
任何代码**——退役排在 canary 验证与 E09 真实模型质量验收之后（计划
§7.2 顺序），提前删除会让用户失去可用导入能力。

## 退役资产清单（旧编排所有权）

| # | 资产 | 位置 | 角色 | 核销条件 |
|---|---|---|---|---|
| 1 | 深度导入编排入口 | `backend/modules/imports/orchestrator.py`（约 2288 行） | run 所有权、阶段调度、恢复 checkpoint | E07.e 重定向后连续一个观察期无新 run 创建；恢复入口只读 |
| 2 | Phase 编排 | `backend/modules/imports/workflow.py`（约 887 行） | 各 phase runner 组织 | 同上；可调用步骤（解析/抽取 adapter）按 §7.1 迁为可调用步骤保留 |
| 3 | 旧恢复/重试调度 | `deep_import_retry.py`、`orchestrator` 恢复分支 | 独立恢复 owner | evolution 恢复（E06 recovery）承接同等语义并经 T10/T11 验证 |
| 4 | 运行记录 | `import_workflow_runs` 表 + `workflow_runs.py` | 历史 run/费用 | 迁移或适配为 Evolution legacy run 只读视图（§7.1） |
| 5 | 前端阶段面板 | deep-import 进度/阶段 UI（`frontend-console`） | 旧阶段心智 | U 系列统一宿主替换后下线，显示"理解这本书/继续理解" |
| 6 | 旧 API 路由 | `modules/imports/api.py` deep-import 端点 | 独立入口 | E07.e 薄适配返回真实新回执 + deprecation（`legacy_adapter.py` 已备），观察期后移除 |

## 保留资产（不属于退役范围）

- 文件解析、章节识别、上传校验（`parsers.py`、`admission.py`）——Imports
  按计划保留的格式边界。
- 已验证的抽取 adapter、逐字证据校验、身份解析调用——迁为可调用步骤。
- `rollback_deep_import_delta_logs_by_workflow` 等历史审计路径。

## 已完成的替代能力（本分支）

- 运行所有权/游标/预算：`evolution_runs`（E04）+ fencing（E06）。
- 窄提交与恢复：E03c freeze/apply/recover + T10/T11 故障注入。
- 推进顺序与屏障：E04 orchestrator + G2 纵切。
- 在途兼容分类：E07.d `compat.py`（兼容续接 / 保留费用从可验证批次继续）。
- 入口适配：E07.e `legacy_adapter.py` + `evolution_scene_step` handler。

## 开放项（阻断实际核销）

1. 生产 LLM 采样器未接线（E09 真实模型质量验收）。
2. 项目级 canary 未演练（E07.c 的 `switch_project_engine` 已具备，待真实
   项目灰度）。
3. PostgreSQL e2e 与真实模型链路验收未跑（合并前建议项）。
